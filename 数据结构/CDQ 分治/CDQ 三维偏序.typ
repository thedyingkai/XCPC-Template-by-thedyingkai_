==== 维护与产物

- 输入一批三元组，按输入顺序产出每个点的三维偏序计数。
- `count(points)` 一次输入整批三元组，返回每个点被多少个其他三元组逐维不大于；完全相同的点会互相计入。
- 当前板直接处理静态三维偏序；动态逆序对、带修改矩形计数和偏序 DP 都需要按后文改事件或维护量。

==== 接口与边界

- 坐标使用 `i64`，第三维在内部离散化；答案按输入顺序返回。
- 当前先合并完全相同的点。删掉合并会使相同坐标跨分治边界时只统计一个方向。

==== 改法与 trick

- 二维偏序只需排序一维、树状数组维护另一维；四维偏序可在 CDQ 的归并阶段再套一层 CDQ。
- 动态点修改、矩形计数先拆成带正负号的前缀事件；DP 优化则把树状数组中的“计数”换成最大值或最小值。
- 严格不等关系在排序同值组和归并的 `<=` 处统一调整，不能只改一处。

==== 板子题

- 洛谷 P3810「三维偏序（陌上花开）」；P3157「动态逆序对」需按上文拆事件。
