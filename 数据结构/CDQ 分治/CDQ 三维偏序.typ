`count(points)` 对每个点 `i`，统计满足 `x[j]<=x[i]`、`y[j]<=y[i]`、`z[j]<=z[i]` 且 `j!=i` 的点 `j` 的个数。CDQ 分治先按三维坐标排序，再按排序位置分开左右两半，归并时只统计左半对右半的贡献；第二维用归并指针维持单调，第三维交给树状数组。

坐标使用 `i64`，第三维在内部离散化，答案按输入顺序返回。完全相同的点会先合并权重；删掉这一步会使同坐标跨越分治边界时只统计一个方向。

题面里若出现“有多少对象的若干项属性都不超过当前对象”，可以先写成各维不等式，判断是不是偏序计数。这里把第一维通过排序消掉，余下的二维计数在归并过程中完成。按 `(x,y,z)` 字典序排序后，两个不同坐标的点若满足三维都不大于，贡献者一定在前；第一维相同也成立，因为第二、三维会继续决定顺序。完全相同的点无法用前后顺序覆盖彼此，所以先合成权重 `w` 的一个点，并给这一组的每个原点预先加入 `w-1`。

考虑一对合法点，递归中必有唯一一层第一次把它们分在左右两半。这层只计算左对右的贡献；更深的层已经不同时包含它们，更浅的层又把它们放在同一侧，因此每一对恰好计算一次。左右递归返回时各自已经按第二维有序，归并扫描右点 `q` 时，将左半所有 `y<=q.y` 的点加入树状数组，再查第三维前缀 `z<=q.z`。树状数组里记录的是权重和，不能把一组合并点只当作一个点加入。

归并结束后，区间被改排成第二维有序，看起来不再按第一维排序，但它仍只包含最初分配给这个递归区间的点，不会跨越父层的左右边界。第一维约束靠这份固定的分治归属保证，第二维顺序服务于下一次归并。树状数组是各层共用的；本层加入了哪些权重，就要在离开前逐一减回，避免上一层或兄弟区间读到残留数据。

例如输入 `(1,1,1)` 两次，再输入 `(2,1,2)`、`(2,2,1)`，结果依次为 `1,1,2,2`。前两个点互相满足条件，后两个点都接受前两个点的贡献，但后两个点在第二、三维各有一项更大，不能互相贡献。P3810 要的是“答案等于 `t` 的点有多少个”，拿到 `count` 的返回值后再统计频率即可，不能直接把逐点答案按行输出。

设原始点数为 $n$，排序和离散化后有 $O(log n)$ 层，每层各点至多进行常数次 $O(log n)$ 的树状数组操作，总时间 $O(n log^2 n)$，辅助空间 $O(n)$。本板按点数计数，树状数组使用 `int`；改成带权贡献且总权重可能很大时，树状数组、权重和答案要一起改成足够大的类型。

- 二维偏序只需排序一维、树状数组维护另一维；四维偏序可在 CDQ 的归并阶段再套一层 CDQ。
- 动态点修改与矩形计数中，点修改记录位置和增量，矩形询问拆成带正负号的前缀事件。
- 用 CDQ 优化 DP 时，先递归求左半状态，再用左半更新右半，最后递归右半；当前计数板先递归左右两半再归并，必须调整这个顺序。树状数组改为维护最值后，不能再加入负贡献清理，应记录修改并恢复旧值，或将本轮写过的节点恢复为初始值。
- 严格不等关系在排序同值组和归并的 `<=` 处统一调整，不能只改一处。

严格关系应逐维确定处理时机。例如只要求第三维严格小于，将查询改成压缩下标减一；只要求第二维严格小于，归并时只加入 `y<q.y` 的左点。第一维严格小于时，同 `x` 的点不能彼此贡献，必须保证一组同 `x` 的询问全部做完后才让这一组成为贡献者，或以同值组为分治单位重新组织。原来的 `w-1` 只适用于三维都允许相等的情况，存在严格维度后应重新判断同坐标点是否还贡献。

动态矩形计数可将操作时间作为第一维，点坐标作为第二、三维。时刻 `t` 的矩形 `[x1,x2]×[y1,y2]` 拆成四个带系数的前缀询问，统计在它之前发生的增量；一次赋值要先删除旧贡献，再加入新贡献。这里需要区分“修改事件”和“询问事件”，归并时仅左侧修改更新树状数组、右侧询问读取答案，不能把两类事件一律当成本板的点来互相计数。

- 洛谷 P3810「三维偏序（陌上花开）」；P3157「动态逆序对」需按上文拆事件。
