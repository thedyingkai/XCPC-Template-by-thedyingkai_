CDQ 分治按第一维的排序位置分开左右两半，在归并阶段只统计左半对右半的贡献；第二维用归并指针维持单调，第三维交给 Fenwick。当前 `count(points)` 返回每个三元组被多少个其他三元组逐维不大于，完全相同的点先合并权重，从而避免同坐标跨分治边界时漏掉一个方向。

坐标使用 `i64`，第三维在内部离散化，答案按输入顺序返回。完全相同的点会先合并权重；删掉这一步会使同坐标跨越分治边界时只统计一个方向。

- 二维偏序只需排序一维、树状数组维护另一维；四维偏序可在 CDQ 的归并阶段再套一层 CDQ。
- 动态点修改、矩形计数先拆成带正负号的前缀事件；DP 优化则把树状数组中的“计数”换成最大值或最小值。
- 严格不等关系在排序同值组和归并的 `<=` 处统一调整，不能只改一处。

- 洛谷 P3810「三维偏序（陌上花开）」；P3157「动态逆序对」需按上文拆事件。
