==== 维护与产物

- 输入不再修改的数组，预处理后反复返回闭区间的合并结果。
- `SparseTable` 处理最值、GCD 等幂等运算；`DisjointSparseTable` 还能处理区间和、矩阵乘等非幂等结合运算。
- 适合无修改的 RMQ 和静态区间合并题。

==== 接口与边界

- `query(l,r)` 使用 `0` 下标闭区间；`SparseTable` 要求操作满足结合律且幂等，`DisjointSparseTable` 只要求结合律。
- 两份板子都不提供空区间单位元；`Op(a,b)` 的参数顺序不会交换，非交换运算也可用不交稀疏表。

==== 改法与 trick

- RMQ 同时要返回最值位置时，把元素改成 `(value,index)`，在 `Op` 中写清相等时取左还是取右。
- 二维静态 RMQ 可对两维各做一次倍增；它会额外带两维对数表，内存吃紧时改离线扫描或二维线段树。

==== 板子题

- 洛谷 P3865「ST 表」。
