稀疏表用于不再修改的数组。普通 `SparseTable` 用两个可能重叠的长度为 $2^k$ 的块回答询问，因此要求运算满足结合律和幂等性；不交稀疏表让两个预处理块在最高不同位处分界，只要求结合律，可维护区间和、矩阵乘等非幂等信息。

`query(l,r)` 使用 `0` 下标闭区间；两份板子都不提供空区间单位元。`Op(a,b)` 的参数顺序不会交换，因此非交换但满足结合律的运算也可用不交稀疏表。

- RMQ 同时要返回最值位置时，把元素改成 `(value,index)`，在 `Op` 中写清相等时取左还是取右。
- 二维静态 RMQ 可对两维各做一次倍增；它会额外带两维对数表，内存吃紧时改离线扫描或二维线段树。

- 洛谷 P3865「ST 表」。
- #link("https://judge.yosupo.jp/problem/staticrmq")[Library Checker · Static RMQ]
