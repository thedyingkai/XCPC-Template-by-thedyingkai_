这份根号分治解决静态无向图上的“给一个点的所有邻点加值、查询单点值”。轻点修改时直接枚举邻边，重点修改时只记懒标记；查询一个点再汇总与它相邻的所有重点标记。阈值取约 $sqrt(2m)$ 后，修改和询问都只会枚举 $O(sqrt m)$ 个对象。

度数不小于阈值的点记懒标记，其余点枚举邻边；简单图中阈值约取 $sqrt(2m)$。构造后不能增删边。有重边时一次操作会按重边条数重复贡献，重点邻接表也保留重复项；若题意把邻接关系去重，构造前必须先合并重边。

- 邻居询问、单点修改可把“重点懒标记”与“轻点暴力”的方向对调。
- 维护颜色计数时，每个重点保存相邻颜色频次；修改一个轻点枚举重点邻居更新，查询轻点再枚举普通邻边。

- AtCoder ABC219 G「Propagation」：把数值和改成 `(time,color)`，查询点时取直写值与所有相邻重点懒标记中时间戳最大的一个。
