这份根号分治解决静态无向图上的“给一个点的所有邻点加值、查询单点值”。轻点修改时直接枚举邻边，重点修改时只记懒标记；查询一个点再汇总与它相邻的所有重点标记。在有 $m$ 条边的简单图上，阈值取约 $sqrt(2m)$，修改和查询均为 $O(sqrt m)$。

度数不小于阈值的点记懒标记，其余点枚举邻边。构造后不能增删边。有重边时一次操作会按重边条数重复贡献，重点邻接表也保留重复项，单次查询最坏为 $O(m)$；要保留按边数累加的含义并保证根号复杂度，应合并重复的重点邻居，另存边数，查询时用边数乘对应懒标记。若题意只计不同邻点，则在构造前去掉重边。

- 邻居询问、单点修改可把“重点懒标记”与“轻点暴力”的方向对调。
- 单点改色、查询邻居中某种颜色的出现次数时，为每个重点维护邻居颜色计数。任意点改色，都要更新与它相邻的重点的计数表；查询轻点时逐个检查邻居。

- AtCoder ABC219 G「Propagation」：把数值和改成 `(time,color)`，查询点时取直写值与所有相邻重点懒标记中时间戳最大的一个。
