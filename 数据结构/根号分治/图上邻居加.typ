==== 维护与产物

- 输入一张结构不变的无向图和点权，按度数分成重点与轻点。
- `addNeighbors(u,x)` 给 `u` 的所有邻点加 `x`，`query(u)` 返回单点值，操作不包含 `u` 自身。
- 适合静态图上反复“邻居整体修改、单点询问”的题。

==== 接口与边界

- 度数不小于阈值的点记懒标记，其余点枚举邻边；简单图中阈值约取 $sqrt(2m)$。
- 构造后不能增删边；有重边时一次操作会按重边条数重复贡献，且重点邻接表也保留重复项。需要保证单次枚举量时先合并重边及其贡献。

==== 改法与 trick

- 邻居询问、单点修改可把“重点懒标记”与“轻点暴力”的方向对调。
- 维护颜色计数时，每个重点保存相邻颜色频次；修改一个轻点枚举重点邻居更新，查询轻点再枚举普通邻边。

==== 板子题

- AtCoder ABC219 G「Propagation」：把数值和改成 `(time,color)`，查询点时取直写值与所有相邻重点懒标记中时间戳最大的一个。
