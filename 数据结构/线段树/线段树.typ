本目录按维护量拆成五份线段树：区间加与区间和、区间加与区间最大值、模意义下区间乘加与区间和、零初值大值域的动态开点区间和，以及用差分维护的区间加与区间 GCD。先从题目的更新和询问确定节点信息与懒标记，再选最接近的一份；不要把某份题目主程序当成通用接口。

==== 区间加与区间和、最大值

固定数组的区间和板维护 `sum` 与加法标记 `lazy`：标记 `d` 作用到长度 `len` 的节点时有 `sum += d*len`，向儿子复合就是 `child.lazy += d`。最大值板只把作用公式换成 `Max += d`。这两份代码采用“标记先挂起，`pushDown(root)` 时才作用到本节点信息并传给儿子”的写法；改成常见的立即更新写法时，必须同时调整整段更新、下传和父节点合并，不能混用两套时序。

动态开点板也是 `sum += d*len`、`tag += d`，但 `makeTag` 会立即更新本节点，`pushdown` 只负责建儿子并传标记。根节点直接调用 `update(l,r,d)`、`query(l,r)`，与固定数组板需要传根编号的接口不同；查询也会建儿子，且节点不回收。

==== 模意义下区间乘加

乘加板把待执行标记记成 `(mul,add)`，表示 `x -> x*mul+add`。若儿子已有旧标记 `(m,a)`，之后再收到父亲的 `(M,A)`，实际复合为

$ (m,a) -> (m M, a M+A), $

节点和相应变为 `sum -> sum*M+A*len`，所有运算都要取模。`op==1` 挂乘法，`op==2` 挂加法；Library Checker 的一次仿射更新 `x -> b*x+c` 要先调用乘 `b`，再调用加 `c`，不能调换顺序。

==== 区间加与区间 GCD

令 `d[i]=a[i]-a[i-1]`，则

$ gcd(a_l,a_(l+1),...,a_r)=gcd(a_l,d_(l+1),...,d_r). $

给 `[l,r]` 加 `k` 只会令 `d[l]+=k`、`d[r+1]-=k`。代码用 Fenwick 维护每个 `a[l]` 的累计增量，用 GCD 线段树维护差分；查询时组合当前 `a[l]` 与 `d[l+1..r]`，单点区间只取 `abs(a[l])`。

- 均为 `1` 下标闭区间。两份固定数组区间加板和乘加板更新时传根编号；动态开点板从根指针直接调用。
- 最大值和动态开点板初值为 `0`；处理任意初始数组时需修改建树。
- 改维护信息时同步修改节点合并、标记作用和标记复合，不能只改查询函数。
- 新增赋值标记要单独记录 `hasSet`；赋值覆盖旧乘加标记，下传顺序按操作复合。
- 动态开点板支持负增量时将 `u64` 改为 `i64`。

- 查第一个满足前缀条件的位置时直接在线段树上下降；坐标可离线取得时优先离散化。
- 洛谷 P3372「线段树 1」、P3373「线段树 2」；AcWing 246「区间最大公约数」。
- #link("https://judge.yosupo.jp/problem/range_affine_range_sum")[Library Checker · Range Affine Range Sum]
