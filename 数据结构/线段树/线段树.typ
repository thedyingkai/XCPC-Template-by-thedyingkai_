==== 选板

- 区间加、区间和用第一份；区间加、区间最大值用第二份；乘加取模用第三份。
- 值域大、初值全为零且访问稀疏时用动态开点；区间加、区间 GCD 用最后一份差分板子。

==== 接口与改法

- 均为 `1` 下标闭区间；前三份懒标记板更新需传根编号 `update(1,l,r,...)`。
- 最大值和动态开点板初值为 `0`；处理任意初始数组时需修改建树。
- 乘加标记表示 `x -> x*lazy[1]+lazy[0]`，`op==1` 为乘、`op==2` 为加，合成顺序不能交换。
- 改维护信息时同步修改节点合并、标记作用和标记复合，不能只改查询函数。
- 新增赋值标记要单独记录 `hasSet`；赋值覆盖旧乘加标记，下传顺序按操作复合。
- 动态开点查询也会创建儿子且节点不回收；支持负增量时将 `u64` 改为 `i64`。
- GCD 板维护差分：查询 $gcd(a_l,d_(l+1),...,d_r)$，区间加只改 `d[l]`、`d[r+1]`。

==== Trick 与板子题

- 查第一个满足前缀条件的位置时直接在线段树上下降；坐标可离线取得时优先离散化。
- 洛谷 P3372「线段树 1」、P3373「线段树 2」；AcWing 246「区间最大公约数」。
