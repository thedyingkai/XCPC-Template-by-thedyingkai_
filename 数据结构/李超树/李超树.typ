李超树在一个整数横坐标区间上维护若干条直线的上包络。每个节点只保存该区间中点处更优的直线，另一条直线若仍可能获胜，只会进入一侧儿子；因此插入整条直线和单点查询都是 $O(log |R-L|)$。它常把 `dp[i]=max(k[j]x[i]+b[j])` 中的历史转移变成在线查询。

构造时给整数闭区间 `[L,R]`；当前只插入整条直线并维护最大值，`query(x)` 返回 `i128`。空树返回 `-(i128(1)<<126)`；若题意规定空集答案为 `0`，先插入 `y=0`。

识别斜率优化时，先把转移式拆成“只与当前状态有关的项”和“一个历史状态对应的一次函数”。例如 `dp[i]=max(dp[j]-(x[i]-x[j])^2)`，其中 `j<i`，展开后得到 `dp[i]=-x[i]^2+max(2*x[j]*x[i]+dp[j]-x[j]^2)`。每个已算好的 `j` 插入斜率 `2*x[j]`、截距 `dp[j]-x[j]^2` 的直线，再在 `x[i]` 处查询。先查询、算出 `dp[i]`，最后插入当前直线，才能保持 `j<i`；初始可转移状态要在循环前插入。这里斜率和查询横坐标都不要求单调。

一条直线与另一条直线的高低关系至多改变一次。插入时先比较区间中点，让中点更优的直线留在当前节点；剩下那条即使还能胜出，也只可能在中点的一侧胜出。因此只递归到那一侧，无需向两边同时插入。代码记录的 `lef` 和 `cen` 是交换前的新旧直线比较结果，两者不同时走左边，相同时走右边；交换当前直线后再随意重用一半旧比较，会破坏这条判断。

节点里的一条直线不保证在整个区间都最优，不能将整段答案当成这条线。单点查询沿横坐标所属的根到叶路径下降，把途中所有直线的函数值取最大；被换下的候选线若可能在这个横坐标获胜，插入时就会被保留在这条路径上的后续节点。这个性质保证查询没有漏掉真正最优的直线。

例如在 `[0,10]` 中插入 `y=2x+1` 和 `y=-x+10`，两线在 `x=3` 相交。`query(1)=9`，`query(5)=11`；只记某一条“当前最好”的线不能回答两个位置。插入与查询各经过 $O(log(R-L+1))$ 层。当前整线插入每次至多新建一个节点，插入 $A$ 条线后空间为 $O(A)$；若扩展成区间线段插入，次数与空间上界也需重新按分解到的节点数估算。

- 求最小值可插入 `-k,-b` 并将答案取负。
- 插入线段时，先按横坐标区间分解，完全覆盖的节点再执行直线插入。
- 横坐标离散时在线段树上存下标，比较直线仍代入原坐标 `xs[pos]`。
- 需要返回方案时给 `Line` 加 `id`，纵坐标相同按题目规定比较编号。
- 相同斜率只需保留截距较大的直线；当前代码已处理。

插入线段 `[ql,qr]` 时，先像线段树区间修改一样把它拆成 $O(log(R-L+1))$ 个完整覆盖区间，在每个区间执行一次李超直线插入，总插入时间为 $O(log^2(R-L+1))$，查询仍为对数。竖直线段不对应一个有限斜率的一次函数，若题目查询它在固定横坐标的最高交点，应单独作为这个横坐标上的点更新处理。离散化只需收集所有可能询问的横坐标，但这要求这些横坐标能预先得到；中点比较、左右端点比较都应代入原坐标，不能误用压缩编号。

本实现以 `i128` 计算 `k*x+b`，但保存的斜率、截距仍是 `i64`。从 DP 式算出的平方、乘积以及 `dp` 也应先扩宽，再确认系数装得下；中间表达式若先在 `i64` 中溢出，之后转成 `i128` 已经无效。取负求最小时，`k` 或 `b` 等于 `LLONG_MIN` 不能直接在 `i64` 中取负，应改为直接维护最小值的比较与空值，或扩宽系数存储。无直线和合法极小值不能仅凭返回的数值可靠区分，需由调用方记录是否已经加入直线。

- 洛谷 P4254「Blue Mary 开公司」：系数改为实数，第 `1` 天收益对应截距 `S-P`。
- 洛谷 P4097「[HEOI2013] Segment」：增加线段插入、实数比较和编号判平。
- #link("https://judge.yosupo.jp/problem/line_add_get_min")[Library Checker · Line Add Get Min]
