对顶堆把可重集合拆成较小的一半 `less` 与较大的一半 `greater`，并始终保持前者与后者等大或多一个。这样插入、按值删除都是 $O(log n)$，`get_middle()` 直接得到下中位数；滑动窗口中位数和动态调整目标排名都可从这个不变量出发。

`less` 比 `greater` 等大或多一个，且 `less` 中每个数不大于 `greater` 中每个数。空集合不能调用 `get_middle()`；`del(x)` 只删一个 `x`，不存在则不动，多测前调用 `init()`。

- 上中位数：两边等大时取 `*greater.begin()`；两个中位数的平均值先转 `i128` 或 `long double`。
- 第 $k$ 小：保持 `less` 恰有 $k$ 个元素，答案为 `*less.rbegin()`。

- 洛谷 P1168「中位数」；P1801「黑匣子」需按当前询问的排名调整两边大小。
