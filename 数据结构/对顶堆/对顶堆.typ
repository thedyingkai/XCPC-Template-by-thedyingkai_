==== 维护与产物

- 输入一个会插入和按值删除的可重集合，始终维护两半部分。
- 支持插入、删除一个指定值，`get_middle()` 返回下中位数。
- 适合滑动窗口中位数，调整两堆大小后也可取动态第 $k$ 小。

==== 接口与边界

- `less` 比 `greater` 等大或多一个，空集合不能调用 `get_middle()`。
- `del(x)` 只删一个 `x`，不存在则不动；多测前调用 `init()`。

==== 常改

- 上中位数：两边等大时取 `*greater.begin()`；两个中位数的平均值先转 `i128` 或 `long double`。
- 第 $k$ 小：保持 `less` 恰有 $k$ 个元素，答案为 `*less.rbegin()`。

==== 板子题

- 洛谷 P1168「中位数」；P1801「黑匣子」需按当前询问的排名调整两边大小。
