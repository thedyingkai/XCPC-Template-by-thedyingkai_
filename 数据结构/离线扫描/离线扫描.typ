==== 维护与产物

- 输入静态数组和若干 `1` 下标闭区间，按右端点离线扫描。
- `OfflineDistinct::count(a,query)` 返回每个区间内不同数的个数，答案顺序与询问输入一致。
- 适合静态区间不同数；也可将“某坐标不超过阈值”的批量询问改成通用事件扫描。

==== 扫描顺序

- 把修改与询问按同一个单调坐标排序；扫到询问时，数据结构中只保留已满足该坐标限制的对象。
- 相同坐标的先后决定严格或非严格关系：统计 `<=x` 时先加入再询问，统计 `<x` 时先询问再加入。
- 矩形询问拆成若干前缀事件并带正负号；撤销困难时不要回头扫，复制事件后换方向再扫一次。

==== 改法

- 当前板子用“每个值只在最后出现位置放 `1`”维护不同数。
- 带修改的区间不同数改莫队、CDQ 或带时间维的数据结构；不要继续沿用只保留全局最后位置的静态扫描。

==== 板子题

- SPOJ DQUERY「D-query」；洛谷 P1972「HH 的项链」。
