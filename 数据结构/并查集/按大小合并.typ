给定一批点，并查集解决“只会合并、不会拆开”的连通性问题。`unite(a,b)` 合并两个连通块并返回这次是否真的合并，`find` 给出代表元，`size(x)` 给出所在块大小；Kruskal、逐步加边以及把删边操作倒序处理都可直接落到这三个接口。

`DSU(n)` 的下标为 `0..n-1`；使用 `1..n` 时构造 `DSU(n+1)`。当前 `find` 有路径压缩，不能用于撤销。

- 连通块数初始化为 `n`，每次 `unite` 成功后减一；块权值、边数等信息只存在根上并在合并时更新。
- 差值、奇偶或模关系用带权并查集；需要回滚时用可撤销并查集。
- 倒序恢复时先建立最终状态，再按原操作的逆序加回点或边；Kruskal 只在 `unite` 成功时选边。

- 洛谷 P3367「并查集」；P1197「星球大战」用于倒序加点。
- #link("https://judge.yosupo.jp/problem/unionfind")[Library Checker · Unionfind]
