==== 维护与产物

- 输入离散点集，在只合并不拆分的过程中维护连通性和连通块大小。
- `unite(a,b)` 合并两块并报告是否真正发生合并；`size(x)` 返回 `x` 所在块的大小。
- 适合 Kruskal、只增连通性和倒序处理删点、删边。

==== 接口与边界

- `DSU(n)` 的下标为 `0..n-1`；使用 `1..n` 时构造 `DSU(n + 1)`。
- 当前 `find` 有路径压缩，不能用于撤销。

==== 常改与 trick

- 连通块数初始化为 `n`，每次 `unite` 成功后减一；块权值、边数等信息只存在根上并在合并时更新。
- 差值、奇偶或模关系用带权并查集；需要回滚时用可撤销并查集。
- 倒序恢复时先建立最终状态，再按原操作的逆序加回点或边；Kruskal 只在 `unite` 成功时选边。

==== 板子题

- 洛谷 P3367「并查集」；P1197「星球大战」用于倒序加点。
