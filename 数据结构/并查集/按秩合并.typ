==== 用途与接口

- 维护连通性、连通块大小；`DSU(n)` 的下标为 `0..n-1`，使用 `1..n` 时构造 `DSU(n + 1)`。
- `unite(a,b)` 仅在两块合并时返回 `true`；`size(x)` 返回所在连通块大小。
- 当前 `find` 有路径压缩，不能用于撤销。

==== 常改与 trick

- 连通块数初始化为 `n`，每次 `unite` 成功后减一；块权值、边数等信息只存在根上并在合并时更新。
- 差值、奇偶或模关系用带权并查集；需要回滚时用可撤销并查集。
- 离线删点、删边可从最终状态倒序恢复；Kruskal 只在 `unite` 成功时选边。

==== 板子题

- 洛谷 P3367「并查集」；P1197「星球大战」用于倒序加点。
