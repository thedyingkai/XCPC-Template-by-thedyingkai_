可撤销并查集用于递归进入一批“临时生效”的边、退出时恢复现场。`snapshot()` 记住历史栈长度，若干次 `unite` 后调用 `rollback(snap)` 就能回到入口状态；线段树分治维护边的生效时间、版本树 DFS 和分治判定最常用这一结构。

点编号为 `1..n`。递归入口记 `snap=snapshot()`，退出前执行 `rollback(snap)`。`find` 不做路径压缩，只按大小合并；每次成功合并记录被接到父亲下的根和父根旧大小，失败的 `unite` 不写历史，因此必须按快照位置而不是调用次数撤销。

处理可持久化并查集一类“每次操作基于某个旧版本”的题时，把每个版本看成节点，从它所依赖的版本向它连一条父子边，得到版本树。DFS 进入版本 `v` 时先记快照，再执行该版本新增的合并或查询；遍历完所有子版本后回滚到快照。这样根到 `v` 的 DFS 路径恰好包含该版本应当继承的全部修改。失败合并没有历史记录，但按栈长度快照回滚仍然正确。

- 维护奇偶关系时增加到父亲的异或权，并把旧权值和矛盾计数一起压入历史。
- 维护块权值时，修改根之前把旧值压入历史。

#link("https://judge.yosupo.jp/problem/persistent_unionfind")[Library Checker · Persistent Unionfind]
