==== 用途与接口

- 用于线段树分治等需要退出递归时恢复状态的场景。
- 点编号为 `1..n`。递归入口记 `snap = snapshot()`，退出前执行 `rollback(snap)`。
- `find` 不做路径压缩，只按大小合并；`find/unite` 为 $O(log n)$，撤销一次成功合并为 $O(1)$。
- 失败的 `unite` 不写历史，不能按调用次数撤销。

==== 改法与 trick

- 维护奇偶关系时增加到父亲的异或权，并把旧权值和矛盾计数一起压入历史。
- 维护块权值时，修改根之前把旧值压入历史。
