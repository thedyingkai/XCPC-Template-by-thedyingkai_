==== 维护与产物

- 输入只有合并的连通性状态，同时保留恢复到任意快照的能力。
- `unite` 合并，`snapshot` 记录版本，`rollback` 撤回该版本之后的成功合并。
- 适合线段树分治或其他需要在退出递归时恢复连通状态的离线题。

==== 接口与边界

- 点编号为 `1..n`。递归入口记 `snap = snapshot()`，退出前执行 `rollback(snap)`。
- `find` 不做路径压缩，只按大小合并。
- 失败的 `unite` 不写历史，不能按调用次数撤销。

==== 改法与 trick

- 维护奇偶关系时增加到父亲的异或权，并把旧权值和矛盾计数一起压入历史。
- 维护块权值时，修改根之前把旧值压入历史。
