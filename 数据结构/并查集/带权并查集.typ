==== 维护元素间的相对关系  

例如：食物链（A吃B，B吃C，C吃A），判断给定关系是否矛盾。每个节点到根节点维护一个权值（如0/1/2表示同类、捕食、被捕食），通过路径压缩和权值合并来推演任意两点之间的关系。

==== 计算元素间的差值或距离  
   
例如：区间和问题中，已知某些子区间的和，询问某一段的和。利用带权并查集将每个位置的前缀和视为节点，通过合并时记录差值，查询时计算两节点到根的距离之差即可得到区间和。

==== 维护模运算下的偏移量  

例如：奇偶性游戏（某区间内1的个数是奇数还是偶数）、天平平衡问题（两边重量差）。权值可以定义为0/1（偶数/奇数）或任意整数模M，合并时对权值进行模运算。

==== 动态连通性中的度量传递  

例如：在图中动态加边，询问两点之间路径上的权值异或和、和或积等（要求满足结合律和可逆性）。带权并查集可维护节点到根节点的某种运算结果，从而快速得到任意两点间的运算值。

==== 核心思想

将集合中的每个元素与根节点建立一种可传递的函数关系（如距离、偏移量）。合并两个集合时，通过已知的两个元素之间的关系来推导两个根之间的关系，从而更新权值。查询时，只需计算两元素到各自根节点的权值，再结合根之间的关系即可得出答案。
