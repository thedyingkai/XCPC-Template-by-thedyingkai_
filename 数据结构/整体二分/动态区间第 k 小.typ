整体二分同时二分一批询问的答案值域，并在每一层按原时间顺序扫描修改与询问。当前把一次单点修改拆成“删除旧值、加入新值”，用树状数组统计区间内落在左半值域的元素数，据此把询问和剩余的 `k` 分到左右递归；适合操作可离线的动态区间第 $k$ 小。

构造 `OverallKth(initial)` 时直接传入 $n$ 个元素，不留下标 `0` 的占位；`change`、`addQuery` 的位置从 `1` 开始，询问区间闭合，`k` 从 `1` 开始。所有修改值统一离散化。事件全部加入后只调用一次 `solve()`；分治会重排事件，并把进入右半值域的询问之 `k` 减去左半值域元素数，所以原事件数组不能再当输入顺序使用。

第 `k` 小可以用计数判断：若区间内不超过候选值的元素已经有 `k` 个，答案不会比候选值大，否则答案在右侧。给每个询问单独二分会反复重放修改；整体二分让答案处于同一个候选值域的一批询问共用一次扫描。分治的是答案的值域，不是数组下标，也不是操作时间；树状数组仍建在位置 `1..n` 上。

进入值域 `[vl,vr]` 的一个递归节点时，保留下来的修改事件都属于这段值域，询问的 `k` 表示“排除已经确定更小的值后，还要找第几个”。按原操作顺序扫描，值在左半区间的修改才更新树状数组。扫到询问时，树状数组因此恰好表示当时数组中落在左半值域的那些元素：左半数量为 `cnt`，若 `k<=cnt` 就递归左半；否则递归右半并令 `k-=cnt`。到单个离散值时，这一组询问的答案已经确定。

分组必须稳定，即同一侧事件的相对时间顺序不变。删除旧值与加入新值可能被分到不同的值域，它们本来就表示各自值域中的一次计数变化，后续递归仍能按原时间恢复正确状态。分组前先撤销本层对树状数组的全部修改，使每个子递归都从空计数开始；漏撤销会让父层计数在子层重复出现。

例如初始数组为 `[5,1,4]`，调用 `addQuery(1,3,2)`，再 `change(2,6)`，再调用 `addQuery(1,3,2)`，最后 `solve()` 返回 `[4,5]`。两次询问虽然位置相同，扫到它们时的修改集合不同。`addQuery` 返回从 `0` 开始的答案编号，便于把答案放回混合输出；无需在求解后重新找询问在事件数组中的位置。

设修改次数为 $U$、询问次数为 $Q$、离散值数为 $V$，事件数为 $M=n+2U+Q$。每层扫描的树状数组操作共 $O(M log n)$，总时间 $O(M log V log n)$，另加离散化排序。当前实现的 `goLeft` 在递归返回前保留，各层缓冲最坏累计 $O(M log V)$ 空间，此外树状数组与值域表占 $O(n+V)$；不要把所有局部数组都按只有一层来估算内存。

- 修改拆成删除旧值与加入新值两个带符号事件；每层分治必须保持事件原来的时间顺序。
- 第 $k$ 大可改成按值从大到小分治，或转成第 `len-k+1` 小。
- 二维矩形第 $k$ 小要让点修改保存两个位置坐标，并将计数结构改为二维；矩形内元素个数由四次二维前缀查询相减得到，不能只给当前一维事件增加字段。

强制在线题中，后续操作要用前一次答案解密，无法预先收齐这份事件序列，不能直接用本板。静态区间第 `k` 小可用主席树；带修改又要在线，则需考虑树状数组套权值线段树等结构。若元素带非负权、询问的是累计权重达到 `k` 的最小值，可将 `delta`、树状数组和 `k` 一起改成权重类型；允许负权后，计数随值域扩大不再单调，这套二分依据就不成立。

P3527 二分的是每个国家首次达标的操作时刻，判定需要重放区间加、累计国家所属位置的贡献；它用到相同的整体二分思想，但本板的“值域计数后减去 `k`”接口不能直接照搬到该题。

- 洛谷 P2617「Dynamic Rankings」；P3527「MET-Meteors」。
