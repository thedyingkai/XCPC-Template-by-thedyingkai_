==== 维护与产物

- 输入初始数组、按时间发生的单点改值和闭区间第 $k$ 小询问。
- `change(pos,value)` 记录修改，`addQuery(l,r,k)` 记录询问，`solve()` 按原询问顺序返回答案。
- 适合操作可全部离线、答案可二分且一批询问能共享判定扫描的动态第 $k$ 小题。

==== 接口与边界

- 位置为 `1` 下标、区间闭合；所有修改值统一离散化。
- 所有事件加入后只调用一次 `solve()`；分治会重排事件并修改询问中剩余的 `k`。

==== 改法与 trick

- 修改拆成删除旧值与加入新值两个带符号事件；每层分治必须保持事件原来的时间顺序。
- 第 $k$ 大可改成按值从大到小分治，或转成第 `len-k+1` 小；二维矩形第 $k$ 小把点修改换成二维前缀事件。

==== 板子题

- 洛谷 P2617「Dynamic Rankings」；P3527「MET-Meteors」。
