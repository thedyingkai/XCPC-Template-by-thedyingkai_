整体二分同时二分一批询问的答案值域，并在每一层按原时间顺序扫描修改与询问。当前把一次改单点拆成“删除旧值、加入新值”，Fenwick 统计区间内落在左半值域的元素数，据此把询问和剩余的 `k` 分到左右递归；适合操作可离线的动态区间第 $k$ 小。

位置为 `1` 下标、区间闭合，所有修改值统一离散化。事件全部加入后只调用一次 `solve()`；分治会重排事件，并把进入右半值域的询问之 `k` 减去左半值域元素数，所以原事件数组不能再当输入顺序使用。

- 修改拆成删除旧值与加入新值两个带符号事件；每层分治必须保持事件原来的时间顺序。
- 第 $k$ 大可改成按值从大到小分治，或转成第 `len-k+1` 小；二维矩形第 $k$ 小把点修改换成二维前缀事件。

- 洛谷 P2617「Dynamic Rankings」；P3527「MET-Meteors」。
