==== 维护与产物

- 输入一片会动态加边、删边的无环森林，维护每个连通块的整树聚合信息。
- `link`、`cut`、`connected` 修改或询问森林；`setValue` 改点权，`componentSum`、`componentSize` 查连通块信息。
- 适合动态连通性和整棵树可交换信息聚合；路径查询优先用 Link-Cut Tree。

==== 接口与边界

- 每个点有一个自标记，每条树边增加两个有向标记；`cut(u,v)` 只能删除此前直接加入的边。
- 当前隐式 Treap 不回收被删除边的节点，操作总数决定内存上界。

==== 选用与改法

- 当前聚合为点权和与点数。维护其他可交换连通块信息时同步修改节点聚合；路径信息不能从这份欧拉游走序列直接得到。
- 结构仅维护森林；一般动态图需把非树边另外保存，并在删树边后寻找替代边。
