Euler Tour Tree 把一棵树的欧拉游走序列放进隐式 Treap，适合动态森林中的加边、删边、连通性和整棵连通块聚合。`link/cut` 修改森林，`setValue` 改点权，`componentSum/componentSize` 读取当前连通块；若题目问路径而不是整块，应优先改用 Link-Cut Tree。

每个顶点在序列中有一个保存点权的节点，每条树边对应两个边节点，分别记录沿两个方向经过这条边。点权和与点数只统计顶点节点；`cut(u,v)` 只能删除此前直接加入的边。被删边的 Treap 节点不回收，空间按操作总数估算。

把树上的一次完整游走看成一个环：沿边走到邻点，遍历完那一侧后再沿反方向回来，每条边正好出现两次。从哪个顶点开始不影响环的含义。代码额外给每个顶点放一个唯一标记 `self[v]`，只有这个标记贡献一次点权；不能像普通 DFS 序那样，在每次再次经过顶点时都加入点权，否则度数较大的点会被重复统计。

Treap 维护这个环被切开后得到的线性序列。`reroot(v)` 找到顶点标记的位置，把序列拆成 `A+B`，再接成 `B+A`，让游走从 `v` 开始；这只是循环移动序列。`link(u,v)` 先把两个环分别转到 `u`、`v`，再依次拼接“第一棵树的游走、边 `u->v`、第二棵树的游走、边 `v->u`”。删边时，两次经过该边之间的一段恰好是一侧分量，环上剩余部分是另一侧分量；删除两个边节点并拼回外侧两段，就得到了两棵树。

同一棵树的所有标记都在同一棵 Treap 中，因此 `connected` 比较两个顶点标记所在的 Treap 根，连通块查询读取该根的聚合。这里的 `parent` 始终是 Treap 父亲，`position` 借助父亲和左子树大小求当前排名；分裂、合并时必须维护双向父子关系，不能套用一份不维护父指针的普通隐式 Treap。

构造 `EulerTourTree(n)` 后使用 `1..n` 号点，初始点权为 `0`。例如点权为 `2,3,5`，加入 `1-2`、`2-3` 后，`componentSum(2)=10`、`componentSize(2)=3`；删去 `2-3` 后，`componentSum(2)=5`，点 `3` 的连通块和也为 `5`。`setValue(v,w)` 是赋值，单点加需要保存旧值后传入新值。`link` 遇到自环或已经连通的两点返回 `false`，`cut` 遇到不存在的边也返回 `false`。

随机优先级使每次操作的期望时间为 $O(log n)$，极端情况下没有确定的最坏对数保证。若成功加边总次数为 $A$，节点数组最终占 $O(n+A)$ 空间；临时切边后重新连回也算一次成功加边。当前存在的树边仍至多有 $n-1$ 条，但旧边节点不会复用，不能按这个数估算整段操作的内存。

- 当前聚合为点权和与点数。维护其他可交换连通块信息时同步修改节点聚合；路径信息不能从这份欧拉游走序列直接得到。
- 结构仅维护森林；一般动态图需把非树边另外保存，并在删树边后寻找替代边。

- 询问边 `(v,p)` 下以 `p` 为父亲的 `v` 子树时，临时 `cut(v,p)`，读取 `componentSum(v)` 后再 `link(v,p)`。

这种子树查询要求 `p` 确实是当前指定根下 `v` 的父亲。切开的是子树和外部之间唯一的边，取 `v` 所在分量正好得到子树；若 `v` 就是根，直接读整个连通块。点数、最大值、按位或等可交换聚合也可这样处理。若要整块加权，给 Treap 子树加懒标记时，和应增加 `delta*vertexCount`，自身只有顶点标记才改 `value`，两个方向的边节点仍不能算作点；当前板子尚未实现这种修改。

- #link("https://judge.yosupo.jp/problem/dynamic_tree_vertex_add_subtree_sum")[Library Checker · Dynamic Tree Vertex Add Subtree Sum]
