静态拓扑 Top Tree 先把固定树形分解成高度为 $O(log n)$ 的 cluster 合并树，再把全树 DP 写成 `rake` 与 `compress`。当前状态维护非负边权树的直径：`setEdge(id,w)` 只重算对应叶子到根的一条链，`diameter()` 直接读取全局答案；它不负责 `link/cut`、链修改或子树修改。

普通树形 DP 改一个点或一条边后，可能需要沿原树的一条长链重新计算。静态 Top Tree 将计算顺序改成平衡的二叉合并树：叶子保存局部数据，内部节点用固定公式合并两个子结果。适合“树形不变，单点或单边修改后需要全树 DP 答案”的问题；能否套用取决于局部状态能否用常数规模的信息从边界合并，原题是树形 DP 并不自动意味着可以直接使用这里的直径状态。

这里将 cluster 称为簇，即一段连通的树边及其附带分支，另标出至多两个用于后续连接的边界点。它不一定是原树中某个点的完整子树。当前每个簇都记录有顺序的两个边界，即使其中一个边界此后不再接新边，也可以保留它作为计算状态的端点。

- 边编号是构造时 `treeEdges` 的下标；树形变化用 Link-Cut Tree、Euler Tour Tree 或完整动态 Top Tree。
- 输入必须是 $n >= 1$ 的连通树，边权必须非负；`root` 只改变分解，不改变直径。

==== Cluster 边界

- 叶子 `v` 表示 `parent[v]` 与 `v` 之间的边；根叶子多一条权为 $0$ 的虚边。
- `compress(L,R)` 合并 $(a,b)$ 与 $(b,c)$，新边界为 $(a,c)$。
- `rake(main,side)` 合并 $(a,b)$ 与 $(a,c)$，合并后只保留 `main` 的 $a,b$ 作为边界，原来的边界 $c$ 成为内部顶点。因此 `rake` 不可随意交换参数。

两种合并都要求两簇除公共边界外没有共同顶点，因此任何跨簇的简单路径都只经过这个公共点一次。`compress` 将两段主链首尾相接；`rake` 把一棵旁支接到主簇左边界，主簇两边界之间的距离不变。构造时先找重链，把每个重链点的轻子树逐步 `rake` 到向下的主边，再沿重链 `compress`。轻子树用按高度合并的优先队列，链上用保序的栈安排合并，不能改成从头到尾依次合并，否则一条长链会得到线性高度，修改也退化为线性时间。

==== 改 DP

- `Cluster` 的 `length/fromLeft/fromRight/diameter` 分别是两边界距离、从左/右边界出发的最远距离、cluster 内直径。改题时一起替换 `Cluster`、`makeEdge`、`rake`、`compress`。
- 根的虚边会调用 `makeEdge(0)`；新状态必须让它成为合法单位元。有方向的信息要分开左到右和右到左，不能把 `compress` 当成可交换操作。
- 单边修改映射到定根后的子端点叶子，只重算该叶子到 Top Tree 根的祖先；增加可修改数据时也要保留这个定位映射。

直径公式可以从端点落在哪个簇推出来。对 `compress(L,R)`，答案可能完全在左簇、完全在右簇，或跨过公共边界；第三种的最远长度就是 `L.fromRight + R.fromLeft`，与前两种取最大。新左边界到最远点也分两类：留在左簇时是 `L.fromLeft`，进入右簇时是 `L.length + R.fromLeft`。右边界的公式对称，两边界距离则直接相加。于是每次合并只需要这四个数，不用重新查看内部顶点。

`rake(main,side)` 的跨簇直径为 `main.fromLeft + side.fromLeft`。新右边界若走进旁支，先从主簇右边界走到左边界，再进入旁支，距离为 `main.length + side.fromLeft`；新左边界的最远点则在两个簇的 `fromLeft` 中选择。单边权为 `w` 时四个量均为 `w`，依赖边权非负；允许负权、要求路径必须包含一条边时，空路径和叶子的初值都要重新定义，不能仅移除断言。

例如三条边 `0-1`、`1-2`、`1-3` 的权依次为 `2,3,4`，最长路径是 `2-1-3`，长度为 `7`。以这三条边的顺序构造后，调用 `setEdge(0,10)`，最长路径改成 `0-1-3`，长度为 `14`。边编号从 `0` 开始，点编号为 `0..n-1`，`setEdge` 是赋值。构造的合并节点数恰为 `2*n-1`，该实现建树时间为 $O(n log n)$、空间为 $O(n)$，单边修改为 $O(log n)$，读取直径为 $O(1)$；距离加法仍须保证不超出 `i64`。

把其他 DP 放上来时，先写出“一个簇与外部还会怎样连接”，再决定两个边界各需留下什么状态。比如链上的转移是一次函数 `f(x)=a*x+b`，`compress` 就应按链方向做函数复合；旁支贡献则由原题的树形 DP 规则合并。若状态需要统计顶点权，公共边界会同时出现在两个簇中，必须规定其权值归哪一侧，避免重复计算。虚边也应与这个规定一致，不能让额外的虚顶点多贡献一次点权。要求输出直径端点时，可为每个最远距离附带取得它的顶点，并在三种候选中同步选择端点，而不是只替换 `diameter()` 的返回类型。

- 固定树形、单边改权、全树直径。
- AtCoder ABC351 G「Hash on Tree」：保留 Top Tree 结构，重写 cluster DP。
- Library Checker 的 Tree Diameter 还要求输出直径路径。先从任意顶点遍历，找到最远点 `s`；再从 `s` 遍历，找到最远点 `t` 并记录父节点，沿父节点恢复路径 `s..t`。这是额外的两次树遍历，当前 Top Tree 只维护长度，`diameter()` 可用于核对路径长度。
- #link("https://judge.yosupo.jp/problem/tree_diameter")[Library Checker · Tree Diameter]
