Link-Cut Tree 把实树路径动态改造成若干棵 Splay，解决森林中的加边、删边、换根、单点修改和路径聚合。`queryPath(u,v)` 暴露路径后返回点数、和、最大值与异或和；整棵连通块的可交换信息更适合 Euler Tour Tree。

树链剖分依赖预先确定的树形，删边、连边后原来的重链可能失效。LCT 随询问调整链的划分，适合“边会变，但图始终是森林，询问落在两点路径上”的题目。这里的实树指题目中的森林，辅助树指表示一条链的 Splay；辅助树的左右儿子不是实树中的左右儿子。一个点可以在实树中有许多儿子，在辅助树中仍至多有两个儿子。

构造 `LinkCutTree(n)` 后点编号为 `1..n`，初始都是孤立点且权值为 `0`；先用 `setValue` 设置点权，再逐条 `link`。例如点权为 `2,5,7`，连上 `1-2` 和 `2-3` 后，`queryPath(1,3).sum` 为 `14`；`setValue(2,1)` 后变为 `10`。执行 `cut(2,3)` 后应先检查 `connected(1,3)`，不能继续查询这条已不存在的路径。单点改权是赋值；题目要求加上 `delta` 时，另存当前点权并传入新值。

==== 路径暴露

节点的 `parent` 既可能指向辅助树父亲，也可能在当前节点是辅助树根时指向实树中的路径父亲；`isAuxRoot` 用父亲是否把自己列为儿子来区分。`access(x)` 沿实树父链向上：每次把已经处理好的链设为当前点的右儿子，从而改写首选边；结束并 `splay(x)` 后，`x` 的辅助树按中序恰好表示当前实树根到 `x` 的路径。

`access` 中被换下来的旧右儿子还连在实树上，只是不再属于当前辅助树，因此不能把它的 `parent` 清零。沿祖先向上处理时，新接上的右侧已经表示通向 `x` 的那一段路径；处理完实树根后，整条根到 `x` 的路径接在同一棵辅助树里。链外的分支保留路径父亲，但不计入这棵辅助树的 `Info`。这也是路径和不会误加旁支点权的原因。

`makeRoot(x)` 先 `access(x)`，再给整条根到 `x` 的链打翻转标记，于是 `x` 成为实树的新根，森林中的无向边没有变化。代码没有单列 `split` 函数；标准的 `split(u,v)` 就是 `makeRoot(u); access(v)`，此后 `v` 的辅助树信息恰好覆盖路径 `u..v`，`queryPath` 正是这个顺序。

暴露 `u..v` 后，中序第一个点是 `u`，最后一个点是 `v`。此时删边还必须确认两点直接相邻；仅仅连通并不代表存在边 `(u,v)`。代码用 `v` 的左儿子是否为 `u`、`u` 是否没有右儿子检查这一点。Splay 旋转前从辅助树根往下传翻转标记，否则尚未落实的左右方向会导致旋转和相邻关系判断错误。

每次操作的均摊时间为 $O(log n)$，空间为 $O(n)$；均摊不等于每次 `access` 都只经过对数个点，也不能简单把它看成对数次独立 Splay 来相乘。这里使用 Splay 的整体均摊界，个别操作可能较慢。路径和、懒标记作用后的乘积若超过 `i64`，需要同步扩宽点权和聚合类型。

- 非法加边或删除不存在的直接边时，`link/cut` 返回 `false`；`queryPath` 断言两点连通。
- 边权问题把每条边新建成一个点，再把它分别连到两个端点；删除边时切断这两个连接。

==== 改聚合

- 当前聚合量可交换。维护矩阵、字符串等有方向的信息时，节点需同时保存正序和逆序答案，翻转标记交换两者。
- `makeRoot` 只改变辅助树表示，不改变森林中的边；子树信息不能直接由当前路径聚合得到。
- 调用前必须保证点编号在构造范围内；辅助树祖先标记使用显式栈从上到下下传。

维护路径加法时，在 `makeRoot(u); access(v)` 后给 `v` 打加法标记，并同时更新自身点权、路径和与最大值；和增加 `delta*size`，最大值增加 `delta`。现有异或和不能由“旧异或和、点数、加数”直接更新，保留这个字段就还需要额外设计，不能只给 `sum` 加懒标记后宣称所有查询仍正确。边权转点权后，若只统计边，原顶点在和、异或中取 `0`，在最大值中应取负无穷；路径点数也需另存边节点数量，不能直接使用当前 `size`。

有方向的合并要按“左儿子、自身、右儿子”的顺序计算。当前 `pull` 先取自身再循环左右儿子，是因为和、最大值、异或都可交换；直接把运算符换成矩阵乘法会改变顺序。要维护实树子树和，还需记录虚子树贡献，并在 `access` 换右儿子时加入旧链、扣除新链；这已经超出当前路径聚合的接口，且子树归属必须指定实树根。

- 洛谷 P3690「Link Cut Tree」。
- #link("https://judge.yosupo.jp/problem/dynamic_tree_vertex_add_path_sum")[Library Checker · Dynamic Tree Vertex Add Path Sum]
