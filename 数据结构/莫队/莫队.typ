莫队把静态区间询问重新排序，使相邻询问的左右端点只移动较短距离。只要答案能在加入或删除一个位置时 $O(1)$ 或较小复杂度更新，就能用 `add/del` 维护区间不同数、频次平方和、相等数对等信息；`answer(id)` 按原询问编号保存结果。

==== 窗口不变量

`work` 开始时 `l=1,r=0` 表示空窗口。整个过程中，回调维护的对象必须始终恰好是闭区间 `[l,r]`：左端向左先 `add(--l)`，右端向右先 `add(++r)`；左端向右先 `del(l)` 再 `l++`，右端向左先 `del(r)` 再 `r--`。四组循环结束后窗口才等于当前询问 `[ql,qr]`，此时调用 `answer(id)`。若改成半开区间，四个移动式和初始空区间必须一起改，不能只改询问端点。

- 数组和询问使用 `1` 下标闭区间。`addQuery(l, r)` 返回询问编号。
- 询问会按莫队顺序重排，但 `id` 保持输入顺序；答案数组按 `id` 写入即可。
- 当前是普通莫队，不支持修改和树上路径。

- 数值范围大时先离散化，否则计数数组无法直接开。
- 带修改莫队：查询增加时间字段，维护修改指针并支持应用、撤销一次修改；块长通常取 $n^(2/3)$。
- 树上路径莫队：用入栈、出栈各一次的欧拉序，端点移动时切换该点是否计入；LCA 不在区间内时答询问前临时加入，答完移除。
- 回滚莫队：同一左端点块内右端点只增；每个询问临时加入左侧后立即撤销，换块时清空并重建右侧状态。

- 维护 `sum(cnt[x]^2)`：加入 `x` 前执行 `ans += 2*cnt[x]+1`，删除前执行 `ans -= 2*cnt[x]-1`。
- 维护相等数对数：加入贡献当前 `cnt[x]`，删除前先减一再扣除新的 `cnt[x]`。

- 洛谷 P2709「小 B 的询问」；P1494「小 Z 的袜子」。
- #link("https://judge.yosupo.jp/problem/static_range_count_distinct")[Library Checker · Static Range Count Distinct]
