==== 接口

- 维护非负 `i64` 的第 `0..60` 位；`insert(x)` 插入，`can(x)` 判断能否表示，`max_xor()` 求最大子集异或值。
- `min_xor()` 只处理允许空集的定义，因此固定返回 `0`；非空子集最小异或值未实现。
- `kth(k)` 返回含 `0` 的第 $k$ 小不同异或值，$k$ 从 `1` 开始，越界返回 `-1`；每次调用会做 $O((log C)^2)$ 化简。

==== 常改与 trick

- 求初值 `x` 能达到的最大异或值，将 `max_xor` 的 `res` 初始化为 `x`。
- 值域改变时同步修改 `MAXB` 和整数类型。
- 秩为 $r$ 时有 $2^r$ 个不同异或值；合并两个线性基时逐个插入非零基向量。

==== 板子题

- 洛谷 P3812「线性基」。
