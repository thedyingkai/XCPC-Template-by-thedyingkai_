线性基对一批非负 `i64` 做异或高斯消元，每个最高位只保留一个基向量。`insert` 判断新数是否增加秩，`can` 判断能否由已有数异或得到；把基进一步化成简化阶梯形后，还能按二进制系数顺序枚举所有不同异或值。子集异或、环空间和图上最大异或路径都依赖这个不变量。

- 当前处理第 `0..62` 位，即全部非负 `i64`。
- `min_xor()` 允许空集，因此固定返回 `0`；`min_nonempty_xor()` 求非空子集最小异或，未插入元素时返回 `-1`。
- `kth(k)` 的序号类型是 `u64`，返回含 `0` 的第 $k$ 小不同异或值，$k$ 从 `1` 开始，越界返回 `-1`；无符号序号使秩为 $63$ 时的最后一个位置也能表示。每次调用都会先化简当前基。

==== 枚举、合并与计数

- 求初值 `x` 能达到的最大异或值，将 `max_xor` 的 `res` 初始化为 `x`。
- 若要处理完整 `u64`，同步把存储、返回值和 `kth` 参数改成 `u64`，并单独处理秩为 $64$ 时的计数上界。
- 秩为 $r$ 时有 $2^r$ 个不同异或值；逐个插入另一线性基的非零基向量只会合并可表示值集合。
- 若合并后还要使用表示次数或 `min_nonempty_xor()`，总插入数应取两边 `insertedCount` 之和，并令 `dependent = insertedCount > rank()`；仅复制基向量会丢失原集合内部的线性相关。
- 插入了 $m$ 个向量时，每个可表示值恰有 $2^(m-r)$ 种子集表示；限制非空子集时，只有异或值 `0` 需减去空集这一种。
- `rank()` 返回秩；插入过程出现线性相关时 `dependent=true`，此时非空子集可以异或出 `0`。
- 图上最大异或路径：任选生成树求根异或距离，每条非树边产生 `dis[u] xor w xor dis[v]`，把这些环异或值插入线性基，再用 `dis[s] xor dis[t]` 贪心增大。

- 洛谷 P3812「线性基」。
