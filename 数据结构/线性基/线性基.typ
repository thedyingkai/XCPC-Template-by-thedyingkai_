线性基对一批非负 `i64` 做异或高斯消元，每个最高位只保留一个基向量。`insert` 加入一个数并更新秩与相关性，`can` 判断能否由已有数异或得到；把基进一步化成简化阶梯形后，还能按二进制系数顺序枚举所有不同异或值。子集异或、环空间和图上最大异或路径都依赖这个不变量。

将每个数看成由 `0/1` 组成的向量，异或就是模 `2` 加法，选择一个子集就是给各向量选 `0/1` 系数。线性基保存能生成同一批异或值的一组独立向量。数组 `a[i]` 非零时，其最高的 `1` 恰在第 `i` 位；插入 `x` 从高到低消元，遇到已有主元就异或掉这一位，遇到空主元就留下剩余向量。异或已有基向量只改变表达方式，不改变加入新数后能够表示的集合。

若最后消成 `0`，原数已能由之前的数表示，这次插入不会增加秩；也因此找到了一种非空子集异或为 `0` 的方式。当前 `insert` 返回 `void`，结果反映在 `rank()` 和 `dependent` 中，不能把它当作返回 `bool` 的接口。插入 `0`、插入重复值同样会产生相关关系；删除一个数却不能简单清掉与它最高位对应的基向量，因为这个基向量可能已是多个原数的异或组合。

`can(x)` 用同样的消元顺序检查表示是否存在。最高未消去的位找不到主元时，低位向量不可能再改变它，因而无解；全消为 `0` 就可表示。求最大异或值时也从高到低尝试加入基向量，高位改善优先于所有低位之和，而后续基向量无法改变已经决定的更高位，所以逐位取更大值是正确的。

- 当前处理第 `0..62` 位，即全部非负 `i64`。
- `min_xor()` 允许空集，因此固定返回 `0`；`min_nonempty_xor()` 求非空子集最小异或，未插入元素时返回 `-1`。
- `kth(k)` 的序号类型是 `u64`，返回含 `0` 的第 $k$ 小不同异或值，$k$ 从 `1` 开始，越界返回 `-1`；无符号序号使秩为 $63$ 时的最后一个位置也能表示。每次调用都会先化简基的局部副本，不修改已保存的基。

==== 枚举、合并与计数

直接按普通阶梯形基的系数从 `0` 数到 `2^r-1`，异或结果未必递增，因为高主元向量可能还带有别的低主元位。`kth` 先消去每个向量中较低的主元位，再按主元从低到高取出 `r` 个向量。此时两个系数组合的大小由它们最高的不同系数位决定，数值的最高不同位也正是那个主元，所以系数 `k-1` 的二进制位可以直接给出第 `k` 小的不同异或值。

例如插入 `3,5,6`，其中 `3 xor 5=6`，秩为 `2`，不同结果只有 `0,3,5,6`。`max_xor()` 为 `6`，`kth(3)` 为 `5`，`min_nonempty_xor()` 为 `0`，因为三个数全部选中就异或为零。只插入 `3,5` 时，可表示值仍是这四个，但非空最小值变成 `3`。线性基本身确定的是“哪些值能表示”，原始元素数量与相关性还决定“能表示几次、零能否非空表示”。

- 求初值 `x` 能达到的最大异或值，将 `max_xor` 的 `res` 初始化为 `x`。
- 若要处理完整 `u64`，同步把存储、返回值和 `kth` 参数改成 `u64`，并单独处理秩为 $64$ 时的计数上界。
- 秩为 $r$ 时有 $2^r$ 个不同异或值；逐个插入另一线性基的非零基向量只会合并可表示值集合。
- 若合并后还要使用表示次数或 `min_nonempty_xor()`，总插入数应取两边 `insertedCount` 之和，并令 `dependent = insertedCount > rank()`；仅复制基向量会丢失原集合内部的线性相关。
- 插入了 $m$ 个向量时，每个可表示值恰有 $2^(m-r)$ 种子集表示；限制非空子集时，只有异或值 `0` 需减去空集这一种。
- `rank()` 返回秩；插入过程出现线性相关时 `dependent=true`，此时非空子集可以异或出 `0`。
- 图上最大异或路径：在无向连通分量中任选生成树求根异或距离，每条非树边产生 `dis[u] xor w xor dis[v]`，把这些环异或值插入线性基，再用 `dis[s] xor dis[t]` 贪心增大。

图上这个用法允许重复经过顶点和边。任意一条从 `s` 到 `t` 的走法与生成树路径异或后，余下的边奇偶集合可以分解为环；反过来，沿树绕到某个环再走回来，来回经过的连接边互相抵消，就能在原异或值上加入这个环。不同非树边的基本环生成全部环异或，因此所有可能值正好是 `dis[s] xor dis[t]` 加上线性基的可表示值。若题目要求不重复顶点的简单路径，任意环组合不一定能形成合法简单路径，不能直接套用；跨连通块的两点也应先判无路。

令位数为 $B$，本板 $B=63$，插入、可表示性和最大值查询均为 $O(B)$，第 `k` 小和非空最小值由于每次复制并化简为 $O(B^2)$，空间为 $O(B)$。大量第 `k` 小询问且期间没有插入时，可以预先化简一次并保存按主元排序的向量；再次插入后需要重新生成。若要求非空子集的第 `k` 小不同值：存在相关关系时仍含 `0`，直接使用本接口；没有相关关系时要跳过空集对应的 `0`，改查 `kth(k+1)`，并先检查序号上界。

- 洛谷 P3812「线性基」。
