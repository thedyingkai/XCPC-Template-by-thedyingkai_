动态位集是在运行时确定长度的布尔数组，按 `u64` 整块处理。当前接口提供置位、询问、计数以及 `bits |= bits << w`，最直接的用途是 01 背包可达和；只要转移能够写成整块移位或按位运算，也能把原来逐状态的循环压成每次处理 64 个状态。

`set(x)`、`test(x)` 操作第 `0..size-1` 位。`shiftOr(w)` 原地完成 `bits |= bits << w`，内部必须从高位向低位更新，否则同一物品会被重复使用。两个长度相同的动态位集可直接 `a |= b`；长度不同会触发断言。一般固定上界问题直接使用 `std::bitset<N>` 更短。

- 多重背包可按二进制拆分后反复 `shiftOr`；询问所有可达和时先 `set(0)`。
- 图的传递闭包：按中间点枚举，若 `reach[i][k]` 为真则执行 `reach[i] |= reach[k]`。
- LCS 可用位并行公式，但减法需跨字长借位，不能直接拿本板的 `shiftOr` 代替。

- CSES 1745「Money Sums」。
