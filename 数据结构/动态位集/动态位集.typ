==== 维护与产物

- 输入运行时才确定长度的布尔位集，维护某个位是否可达。
- `set(x)` 置位，`test(x)` 询问，`shiftOr(w)` 执行 `bits |= bits << w`。
- 可直接产出 01 背包的全部可达和，也可作为位并行转移的底层容器。

==== 接口与边界

- `set(x)`、`test(x)` 操作第 `0..size-1` 位。
- `shiftOr` 必须从高位向低位更新，否则同一物品会被重复使用。
- 当前只实现左移并集。一般布尔矩阵、字符串匹配可直接使用编译期大小的 `std::bitset<N>`。

==== Trick

- 多重背包可按二进制拆分后反复 `shiftOr`；询问所有可达和时先 `set(0)`。
- 图的传递闭包：按中间点枚举，若 `reach[i][k]` 为真则执行 `reach[i] |= reach[k]`。
- LCS 可用位并行公式，但减法需跨字长借位，不能直接拿本板的 `shiftOr` 代替。

==== 板子题

- CSES 1745「Money Sums」。
