主席树为静态数组的每个前缀建立一个持久化权值线段树版本。版本 `root[r]-root[l-1]` 恰好表示区间 `[l,r]` 的值域频率，因此可以沿左右儿子的计数差下降求第 $k$ 小，也可统计一个闭值域中的元素数；带修改问题不能继续只用两个前缀版本相减。

`ChairmanTree(a)` 要求 `a[0]` 占位，实际数据放在 `a[1..n]`。`kth(l,r,k)` 的位置和排名从 `1` 开始；`count(l,r,low,high)` 统计位置在 `[l,r]`、值在 `[low,high]` 内的元素数，两个区间均闭合。

普通权值线段树的叶子表示一个值，节点记录落在一段值域中的元素个数，沿计数下降即可求整个集合的第 `k` 小。主席树把这个集合依次取成前 `0,1,...,n` 个元素，每加入一个新值就保存一个新版本。两棵前缀树的每个同位置节点代表同一段值域，所以相减可以得到任意位置区间的频率，而不必为每个区间单独建树。

持久化依靠路径复制。由 `root[i-1]` 加入 `a[i]` 时，只复制根到这个值所在叶子的节点，在新节点上把计数加一；没有经过的子树继续与旧版本共用。旧节点创建后不能再改，否则所有引用它的历史版本都会一起变化。编号 `0` 是全零空节点，儿子也指向 `0`，因此无需预先建出一整棵空树。重复值走向同一个离散叶子，但每次出现都会给新版本增加一次计数。

==== 版本差与下降

这里的“相减”不是修改树，而是查询时始终同时拿两个根 `u=root[l-1]`、`v=root[r]`。当前值域节点的区间频数为 `sum[v]-sum[u]`；左半频数是 `sum[left[v]]-sum[left[u]]`。若 `k` 不超过左半频数就让两个根一起走左儿子，否则先从 `k` 中减去左半频数，再一起走右儿子。走到叶子时，该离散值就是第 $k$ 小。区间计数同理，只把两个版本覆盖到询问值域的节点和相减。

例如数组为 `[5,1,5,3]`，构造参数应为 `{0,5,1,5,3}`。查询 `[2,4]` 时，两根之差中 `1,3,5` 的频次各为一，`kth(2,4,2)=3`，`count(2,4,2,5)=2`。压缩值域为 `[1,3,5]`，返回答案时需映射回原值，不能直接输出叶子下标。`count` 的上下界不必在原数组出现：代码用 `lower_bound(low)` 和 `upper_bound(high)` 找到闭区间内存在的值，没有离散值落在里面时返回 `0`。

设不同值个数为 $V$，建树时间和新增节点数为 $O(n log V)$，每次第 `k` 小与闭值域计数查询均为 $O(log V)$；还需保存 $n+1$ 个版本根与离散表。版本根本身只是整数下标，复制根不会复制整棵树，内存主要花在每次插入的新路径上。数组至少应含下标 `0` 的占位；没有实际元素时不能发起非空区间询问。

- 当前用两个前缀版本之差处理静态区间，不支持任意历史版本单点修改。

- 第 $k$ 大改查第 `r-l+2-k` 小；前 $k$ 小之和需在节点增加权值和。
- 树上路径第 $k$ 小要先改变版本的建立方式：按父亲先于儿子的顺序，让 `root[v]` 从 `root[parent[v]]` 插入 `v` 的点权得到，树根的父版本为空；查询时对 `root[u]+root[v]-root[lca]-root[parent[lca]]` 四个版本同时下降。
- 带修改区间第 $k$ 小需树状数组套动态权值线段树，不能只改一个前缀根。
- 节点数约为 `n*(log V+1)`；重复值只增加叶子计数。

统计区间中严格小于 `x` 的数量可以在离散表中找 `lower_bound(x)`，查询它左侧的叶子；严格大于则用区间长度减去 `<=x` 的数量，避免直接计算 `x-1` 或 `x+1`。求前 `k` 小之和时，下降到右儿子前把左儿子的权值和差加入答案，并扣掉左侧数量；到叶子后再加入“剩余 `k` 乘叶子原值”，因为该值可能重复出现。

版本相减适用于可以相减的统计量，例如次数与权值和；区间最大值不能用两个前缀最大值相减得到。第 `k` 小还要求所组合出的每段值域计数都非负，随意拿两个无包含关系的历史版本相减可能产生负数，不能再按数量下降。树上四版本公式的作用正是把公共祖先部分扣去一次，同时保留 LCA 自身一次：在建立版本与准备 LCA 后，各层也须同时组合四个左儿子的计数。

- 洛谷 P3834「可持久化线段树 2」；P2633「Count on a Tree」用于树上四版本查询。
- #link("https://judge.yosupo.jp/problem/range_kth_smallest")[Library Checker · Range Kth Smallest]
