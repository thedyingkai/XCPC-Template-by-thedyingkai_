==== 维护与产物

- 输入 `1` 下标静态数组，按前缀建立持久化权值线段树版本。
- `kth(l,r,k)` 返回闭区间第 $k$ 小，`count(l,r,low,high)` 返回区间内落在闭值域中的元素数。
- 适合静态区间第 $k$ 小和值域计数；当前不支持带修改区间第 $k$ 小。

==== 接口与边界

- 当前用两个前缀版本之差处理静态区间，不支持任意历史版本单点修改。

==== 常改

- 第 $k$ 大改查第 `r-l+2-k` 小；前 $k$ 小之和需在节点增加权值和。
- 树上路径第 $k$ 小组合 `root[u]+root[v]-root[lca]-root[parent[lca]]` 四个版本。
- 带修改区间第 $k$ 小需树状数组套动态权值线段树，不能只改一个前缀根。
- 节点数约为 `n*(log V+1)`；重复值只增加叶子计数。

==== 板子题

- 洛谷 P3834「可持久化线段树 2」；P2633「Count on a Tree」用于树上四版本查询。
