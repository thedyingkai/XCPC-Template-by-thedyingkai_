主席树为静态数组的每个前缀建立一个持久化权值线段树版本。版本 `root[r]-root[l-1]` 恰好表示区间 `[l,r]` 的值域频率，因此可以沿左右儿子的计数差下降求第 $k$ 小，也可统计一个闭值域中的元素数；带修改问题不能继续只用两个前缀版本相减。

==== 版本差与下降

这里的“相减”不是修改树，而是查询时始终同时拿两个根 `u=root[l-1]`、`v=root[r]`。当前值域节点的区间频数为 `sum[v]-sum[u]`；左半频数是 `sum[left[v]]-sum[left[u]]`。若 `k` 不超过左半频数就让两个根一起走左儿子，否则先从 `k` 中减去左半频数，再一起走右儿子。走到叶子时，该离散值就是第 $k$ 小。区间计数同理，只把两个版本覆盖到询问值域的节点和相减。

- 当前用两个前缀版本之差处理静态区间，不支持任意历史版本单点修改。

- 第 $k$ 大改查第 `r-l+2-k` 小；前 $k$ 小之和需在节点增加权值和。
- 树上路径第 $k$ 小组合 `root[u]+root[v]-root[lca]-root[parent[lca]]` 四个版本。
- 带修改区间第 $k$ 小需树状数组套动态权值线段树，不能只改一个前缀根。
- 节点数约为 `n*(log V+1)`；重复值只增加叶子计数。

- 洛谷 P3834「可持久化线段树 2」；P2633「Count on a Tree」用于树上四版本查询。
- #link("https://judge.yosupo.jp/problem/range_kth_smallest")[Library Checker · Range Kth Smallest]
