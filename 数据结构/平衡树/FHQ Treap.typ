FHQ Treap 同时满足二叉搜索树的值序与随机优先级的堆序，通过 `split/merge` 维持期望 $O(log n)$ 高度。当前是可重有序集合，支持插入、删除一个值、排名、第 $k$ 小、严格前驱和严格后继；若把分裂条件改成左侧元素个数，就得到维护序列的隐式 Treap。

这份板子按数值排序，适合插删数值后查排名、分位数或相邻值，插入顺序不会保留下来。每个节点保存一个值，左侧都不大于它，右侧都不小于它；重复值可以出现在两侧。另给节点独立随机优先级，并要求父亲的优先级不大于儿子。值决定中序顺序，优先级决定在这个顺序上选谁为根，从而避免有序插入直接退化成链。

==== split 与 merge

`splitLess(root,x,a,b)` 保证 `a` 中所有值严格小于 `x`、`b` 中所有值不小于 `x`；`splitLE` 则分成 `<=x` 与 `>x`。`merge(a,b)` 的前提是 `a` 的每个值都不大于 `b` 的每个值，它只按随机优先级决定新根，不能拿两棵值域交错的树直接合并。插入先按 `<x` 分开再把新点夹在中间；删除用两次分裂隔离所有等于 `x` 的节点，合并该子树根的左右儿子只删掉其中一个。

以按 `<x` 分裂为例：根值小于 `x` 时，根和整棵左子树都属于左结果，只需继续分裂右子树；根值不小于 `x` 时，对称地只需分裂左子树。每层只走一边，未受影响的整棵子树直接保留。合并时选择优先级更小的根；若选左树根，其左子树原样保留，只需将它的右子树与另一棵树合并。两种操作都在回溯时重算大小，不需要额外的旋转过程。

第 `k` 小依靠中序数量下降：设左子树大小为 `s`，`k<=s` 时去左边，`k=s+1` 时就是根，否则去右边找第 `k-s-1` 小。排名查询统计严格小于给定值的元素个数，所以即使这个值不在集合中也有定义。例如依次插入 `4,1,4`，`rank(4)=2`、`kth(3)=4`、`count(4)=2`；执行一次 `erase(4)` 后还剩一个 `4`，而 `predecessor(4)=1`，`successor(4)` 无答案。

- 重复值各占一个节点，`erase(x)` 只删一个；`rank(x)` 为“小于 `x` 的数量加一”。
- `kth`、`predecessor`、`successor` 无答案时返回 `nullopt`；节点不回收，空间按插入总数预留。

- 文艺平衡树把 `split` 改为按子树大小分裂，再加懒标记和 `pushdown`。
- 每个值只建一个节点时增加 `cnt`；可持久化时复制 `split/merge` 沿途节点。
- 取值域 `[l,r]`：先按 `<l` 分裂，再对右半棵按 `<=r` 分裂。
- 前驱严格小于、后继严格大于；题目边界不同要同步修改比较符号。

分裂会直接修改节点连接，使用完临时拆出的几棵树后，要按原值序合并回去并更新 `root`。统计值域 `[l,r]` 内元素个数可以直接用 `countLE(r)-countLess(l)`，也可拆出中间一棵读大小；增加权值和后，值域和也能读取中间子树的聚合。不要用 `l-1` 或 `r+1` 来替代严格比较，`i64` 极值会溢出，本板分别提供 `<` 与 `<=` 分裂正是为了避免这个边界。

改成序列平衡树时，节点顺序由原序列位置决定，`split(root,k)` 应分出前 `k` 个元素，不能再按节点值比较。区间 `[l,r]` 先分出前 `l-1` 个，再分出 `r-l+1` 个；对中间部分打翻转标记，交换左右儿子，再合回三段。递归分裂、合并前要下传懒标记；维护不可交换的区间信息时还要同时保存正反两个方向，翻转时一起交换。按值有序的这份 Treap 不能随意翻转中间子树，否则搜索树的值序会被破坏。

各项操作期望 $O(log n)$，最坏高度仍可能线性。当前每次插入都新建节点，删除不回收，所以空间取决于累计插入次数，约为 $O(I)$，并非只看最终集合大小。改为每种值一个节点并保存 `cnt` 时，子树大小应加 `cnt`，第 `k` 小的根区间也应改成 `s+1..s+cnt`，删除则先减少 `cnt`，归零后才移除节点。

- 洛谷 P3369「普通平衡树」；P6136 用于检查重复值和强制在线。
- #link("https://judge.yosupo.jp/problem/predecessor_problem")[Library Checker · Predecessor Problem]
