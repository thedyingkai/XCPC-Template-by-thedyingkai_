==== 维护与产物

- 输入一个可加数组或离散频率表，维护其前缀和。
- `update(x,k)` 单点加，`query(x)` 求 `[1,x]`，两个前缀和相减得到区间和。
- 适合单点加、区间和、差分区间加查单点和逆序对；频率第 $k$ 小需另补按最高二进制位下降的 `lower_bound`。

==== 接口与边界

- 下标从 `1` 开始。
- `countInv(n,a)` 要求 `a` 为 `1` 下标，统计严格逆序对；重复值不计，原数组不会被修改。

==== 常改与 trick

- 区间加、单点查：在差分位置 `l` 加 `k`、`r+1` 减 `k`。
- 区间加、区间和：维护 `B1,B2`，前缀和为 `x*sum(B1)-sum(B2)`；差分位置 `p` 分别加 `d` 和 `d*(p-1)`。
- 频率前缀第 $k$ 小可按最高二进制位向下跳；频率必须非负。
- 逆序对先离散化；当前元素贡献为“已加入数量减去小于等于它的数量”。

==== 板子题

- 洛谷 P3374「树状数组 1」；P1908「逆序对」可直接调用 `countInv`。
