==== 输入与产物

- 输入 `i64` 标签，`RandomFingerprint` 稳定地为每个标签生成一对 `u64` 指纹。
- 对指纹求和可代表无序多重集，用异或可只保留出现次数的奇偶性。
- 适合区间、路径上的集合或多重集概率比较；不能代替必须确定正确的判定。

==== 接口与边界

- 比较对象时必须共用两个种子；允许删除的多重集可对指纹做无符号减法。
- 两维分别使用独立种子，只是降低误判概率。

==== Trick 与边界

- 区间多重集比较可对元素指纹做前缀和；树上路径同理套点差分或根前缀。
- 判断集合而非多重集时先去重；直接相加会保留出现次数。
- 需要复现反例时显式传入两个固定种子；交互或可构造数据不要公开固定种子，能做确定性判定时优先用确定性算法。
