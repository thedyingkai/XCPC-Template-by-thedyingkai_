随机指纹用两个独立种子把 `i64` 标签映射成一对 `u64`。指纹相加保留多重集中的出现次数，相减可撤销元素，异或只保留次数奇偶性；前缀、树上路径或可合并集合因而能在常数时间做概率相等判断。它始终存在碰撞，不能替代要求确定正确的算法。

- 比较对象时必须共用两个种子；允许删除的多重集可对指纹做无符号减法。
- 两维分别使用独立种子，只是降低误判概率。

- 区间多重集比较可对元素指纹做前缀和；树上路径同理套点差分或根前缀。
- 判断集合而非多重集时先去重；直接相加会保留出现次数。
- 需要复现反例时显式传入两个固定种子；交互或可构造数据不要公开固定种子，能做确定性判定时优先用确定性算法。
