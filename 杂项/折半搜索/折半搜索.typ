折半搜索把约四十个元素拆成两半，各自枚举 $2^(n/2)$ 个状态，再通过排序、二分或子集 DP 配对。当前提供不超过上界的最大子集和、满足上界的子集个数，以及不超过 40 个点的最大独立集；它适合完整枚举 $2^n$ 太大、但半边状态仍能放进内存的题。

- 最大子集和接口允许负数且计入空集，无可行子集时返回空。
- 计数接口把产生相同和的不同子集分别计数。
- 最大独立集把点集分两半：右半预处理每个集合内的最优独立子集，左半枚举独立子集后删掉右半中与之相邻的点。当前实现时间为 $O(n times 2^(n/2))$，空间为 $O(2^(n/2))$；时间中的 $n$ 来自右半按二进制位进行的子集 DP。
- 最大独立集要求 `adjacency` 表示无自环的对称简单图，调试构建会检查这一点。
- 子集和必须能装入 `i64`；元素更多但答案状态少时优先用背包或状态压缩 DP。

- 精确和用两个半边和的哈希表或双指针；需要方案时同时保存半边掩码。
- 四数和、最近点积等问题也可把变量分成两半，分别枚举贡献后排序配对。

- AtCoder ABC184 F「Programming Contest」；CSES 1628「Meet in the Middle」。
- #link("https://judge.yosupo.jp/problem/maximum_independent_set")[Library Checker · Maximum Independent Set]
