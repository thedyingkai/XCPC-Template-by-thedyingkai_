==== 输入与产物

- 输入 `a[1..n]`，原地归并排序并产出数组的严格逆序对数。
- `sort(n)` 排好数组并把结果写入 `inv_count`，相等元素不计逆序对。
- 适合直接求逆序对，也可将合并阶段的计数改成其他跨半区间贡献。

==== 接口与坑

- 构造 `MergeSorter(n)` 后把数据写入 `a[1..n]`，调用 `sort(n)`；`a[0]` 不参与排序。
- `sort` 每次调用前都会重置 `inv_count`。
- 元素类型固定为 `int`，逆序对答案为 `i64`；值超出 `int` 时同步修改 `a`、`temp` 的类型。

==== 板子题

- 洛谷 P1908「逆序对」。
