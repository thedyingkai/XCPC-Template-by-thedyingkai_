==== 用途

- 十进制读写有符号 `i128`。

==== 接口与坑

- 输入支持可选的 `+`、`-`，不检查非法字符和越界；当前没有 `u128` 的流重载。
- 表达式必须在运算前转换：写 `(i128)a * b`；先按原类型计算再赋给 `i128` 仍会溢出。
- `std::abs`、`std::to_string` 不支持 `i128`，需要时另写。

==== Trick

- 交叉相乘写成 `(i128)a*b < (i128)c*d`，两边都要在乘法前转换。

==== 板子题

- 洛谷 P1005「矩阵取数游戏」：数值范围可用 `i128`，输出需要当前重载。
