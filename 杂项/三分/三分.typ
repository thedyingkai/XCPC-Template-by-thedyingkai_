连续三分用于已知区间上的单峰函数。取两个三等分点比较函数值，可以排除不含极大值的一侧；`ternarySearch(l,r,f,eps)` 返回近似极大点，极值再计算一次 `f(x)`。函数不单峰、平台的取舍有额外要求，或自变量本来是整数时，都不能直接照搬这一浮点接口。

- 当前求最大值；求最小值时反转比较符号。
- 端点和误差必须是有限的 `double`。默认按区间长度小于 `1e-7` 停止；若浮点精度已无法继续缩短区间，也会直接结束，避免死循环。
- 分点用 C++20 的 `lerp`，返回点用 `midpoint`，即使端点同号很大或跨过零点也不会因为直接相加、相减先溢出。
- 精度要求高时改用 `long double` 并固定迭代约 `100` 至 `200` 次。
- 自变量为整数时不要用当前浮点板子：当 `r-l` 较大时三分缩区间，最后枚举剩余整数点。

- 洛谷 P3382「三分法」；P1883「函数」。
