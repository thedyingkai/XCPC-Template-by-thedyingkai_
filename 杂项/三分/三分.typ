==== 输入与产物

- 输入已知区间 `[l,r]` 和可求值的连续单峰函数 `f`。
- `ternarySearch(l,r,f,eps)` 返回极大值对应的横坐标，需要极值时再计算 `f(x)`。
- 适合单峰连续优化；非单峰函数和纯整数取值不能直接套当前板子。

==== 接口与改法

- 当前求最大值；求最小值时反转比较符号。
- 默认按区间长度小于 `1e-7` 停止，类型固定为 `double`。
- 精度要求高时改用 `long double` 并固定迭代约 `100` 至 `200` 次。
- 自变量为整数时不要用当前浮点板子：当 `r-l` 较大时三分缩区间，最后枚举剩余整数点。

==== 板子题

- 洛谷 P3382「三分法」；P1883「函数」。
