`ArbitraryModComb::init(m)` 把任意正模数分解成两两互质的质数幂，`C(n,k)` 分别求 $C(n,k) mod p^q$ 后用 CRT 合并。质数幂部分把阶乘中的 $p$ 因子次数和与 $p$ 互质的单位部分分开；若 $v_p(C(n,k))>=q$，该分量直接为零，否则单位部分可求逆，再乘回 $p$ 的次数。这样即使分母含模数质因子也不会错误求逆。

预处理时间和空间为 $O(sum p^q)$，当前分解又使用 $O(sqrt(m))$ 试除，所以它只适合每个质数幂因子都能整段预处理的模数。模数含很大的质数幂时，不能只提高数组上限；应按题目约束换分块阶乘等算法。大量询问同一模数只初始化一次。`m=1` 时答案恒为零，`k<0` 或 `k>n` 返回零。

质数模数且最大 $n<p$ 时普通阶乘组合数更短；大 $n$、小质数模数用 Lucas。本板针对合数模数，洛谷 P4720「扩展 Lucas」可直接检验。
