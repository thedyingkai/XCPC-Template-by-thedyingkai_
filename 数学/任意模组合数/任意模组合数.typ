==== 用途

- 输入很大的 $n,k$ 和任意正模数 $m$，求 $C(n,k) mod m$；输出由各质数幂下的组合数经 CRT 合并得到，允许分母含有 $m$ 的质因子。

==== 接口与边界

- `ArbitraryModComb::init(mod)` 分解任意正模数为互质的质数幂，`C(n,k)` 先在每个 $p^q$ 下计算，再用 CRT 合并；支持很大的 `n`，单次复杂度与 $log_p n$ 及质因子数有关。
- 预处理时间和空间为 $O(sum p^q)$，因此只适用于模数各质数幂因子可预处理的情况。模数本身很大且含大质因子时不能直接套用。
- 当前分解使用试除，复杂度 $O(sqrt(mod))$；`mod` 接近 `i64` 且需要多次初始化时，改用已有 Pollard-Rho 分解并缓存质数幂。
- `mod=1` 时答案恒为 `0`；要求 `n>=0`，非法的 `k` 返回 `0`。

==== 改板

- 只有质数模数且预处理上界小于模数时，普通组合数板更短；大 `n`、小质数模数时使用 Lucas。本板用于分母可能含模数质因子的合数模情形。
- 质数幂部分先从阶乘中剥离质因子 $p$，分别计算单位部分和 $v_p(n!)$；若 $v_p(C(n,k))>=q$，该余数直接为 `0`。
- 大量相同模数询问只调用一次 `init`。若 `sum p^q` 超过内存限制，应按题目结构换算法，不能只提高数组上限。

==== 板子题

- 洛谷 P4720「扩展 Lucas」。
