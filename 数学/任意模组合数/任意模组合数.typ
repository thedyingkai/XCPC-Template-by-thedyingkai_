`ArbitraryModComb::init(m)` 把任意正模数分解成两两互质的质数幂，`C(n,k)` 分别求 $C(n,k) mod p^q$ 后用中国剩余定理合并。质数幂部分把阶乘中的 $p$ 因子次数和与 $p$ 互质的单位部分分开；若 $v_p(C(n,k))>=q$，该分量直接为零，否则单位部分可求逆，再乘回 $p$ 的次数。这样即使分母含模数质因子也不会错误求逆。

组合数在整数里能约分，不代表阶乘在模意义下能直接求逆。例如 $binom(6,2)=15$，模 $8$ 为 $7$，但分母 $2!4!$ 与 $8$ 不互质，套阶乘逆元会失败。设当前模数为 $M=p^q$，把 $n!=p^(e(n))U(n)$，其中 $U(n)$ 是每个因子反复除去 $p$ 后的乘积。指数由
$e(n)=floor(n/p)+floor(n/p^2)+floor(n/p^3)+...$
给出，因为每个 $p$ 的倍数贡献一次，每个 $p^2$ 的倍数再贡献一次，依此类推。组合数的指数为 $e=e(n)-e(k)-e(n-k)$，其余因子全部与 $M$ 互质，于是
$binom(n,k) equiv U(n)U(k)^(-1)U(n-k)^(-1)p^e \(mod M\)$。

计算 $U(n)$ 时，先乘上 $1,...,n$ 中不被 $p$ 整除的数；剩下那些 $p$ 的倍数，各除去一个 $p$ 后变为 $1,...,floor(n/p)$，所以还要递归乘 $U(floor(n/p))$。设 $H(t)=product_(1<=i<=t, p "不整除" i)i$，按长度 $M$ 分块，每个完整块模 $M$ 后的单位部分相同，得到
$U(n) equiv H(M)^(floor(n/M))H(n mod M)U(floor(n/p)) \(mod M\)$。
`prefix` 保存 $H$，`factorialWithoutP` 对应这条递推。递归层数为 $O(log_p n)$，本实现每层另做一次快速幂，因此单个质数幂的查询时间可按 $O(log_p(n+1) log(n+1)+log M)$ 估计；预处理后的查询仍不只是常数时间。

例如模 $12$ 时先分为 $4$ 与 $3$，$binom(6,2)$ 分别余 $3$ 与 $0$，再合并为模 $12$ 余 $3$。合并时已知 $x equiv a \(mod R\)$，令 $x=a+R t$，新条件给出 $R t equiv b-a \(mod M\)$。由于 $R,M$ 互质，求出 $t$ 后便得到模 $R M$ 的唯一答案，这就是外层循环的 `step`。

预处理时间和空间为 $O(sum p^q)$，当前分解又使用 $O(sqrt(m))$ 试除，所以它只适合每个质数幂因子都能整段预处理的模数。模数含很大的质数幂时，不能只提高数组上限；应按题目约束换分块阶乘等算法。大量询问同一模数只初始化一次。`m=1` 时答案恒为零，`k<0` 或 `k>n` 返回零。

质数模数且最大 $n<p$ 时普通阶乘组合数更短；大 $n$、小质数模数用卢卡斯定理。本板针对合数模数，洛谷 P4720「扩展卢卡斯定理」可直接检验。
