Min_25 筛用于求 $sum_(i=1)^n f(i)$，其中 $f$ 是乘性函数、$f(p)$ 能写成质数 $p$ 的低次多项式，并且每个 $f(p^e)$ 可以快速计算。`Min25Sieve(n,mod,{c0,c1,c2},primePower).solve()` 约定 $f(1)=1$、$f(p)=c_0+c_1p+c_2p^2$，返回答案模 `mod`；回调 `primePower(p,e,peMod)` 返回 $f(p^e)$，第三个参数是 $p^e mod mod$，且 `e=1` 时必须与系数给出的 $f(p)$ 一致。

板子先对所有不同的 $floor(n/i)$ 筛出 $sum_(p<=x)1$、$sum p$、$sum p^2$，再按最小质因子递增枚举乘性分解。递归状态只允许使用不小于指定下标的质数，因此每个大于一的整数按其最小质因子恰好统计一次；这就是 `dfs` 不重不漏的原因。典型时间为 $O(n^(3/4)/log n)$、空间为 $O(sqrt(n))$，不同 `n` 需分别构造实例。

判断能否套用时，先把题目中的权值写成质因数分解的形式。例如约数个数满足 $f(p^e)=e+1$，欧拉函数满足 $f(p^e)=p^(e-1)(p-1)$；不同质数幂的贡献相乘，才符合乘性要求。若有容易求前缀和的卷积恒等式，也应与杜教筛比较。Min_25 筛需要同时知道质数处的表达式和任意质数幂处的表达式，不能仅凭“函数可以线性筛”就直接套用。

质数幂和的筛法可以从筛掉合数理解。对 $t=0,1,2$，初始令 $G_t(x)=sum_(v=2)^x v^t$。处理质数 $p$ 前，尚未筛掉的数包括所有质数，以及最小质因子不小于 $p$ 的合数。待删除的数写成 $p v$，其中 $v>=p$，且 $v$ 没有更小的质因子。因此，当 $x>=p^2$ 时作转移
$G_t(x)<-G_t(x)-p^t (G_t(floor(x/p))-sum_(q<p, q "为质数") q^t)$。
减去前面质数的幂和，是为了排除 $v<p$ 的质数；例如处理 $p=3$ 时，不能再次删除已经被 $2$ 筛掉的 $6$。同一轮按 $x$ 从大到小更新，使右侧较小的状态仍是本轮更新前的值。

$floor(n/i)$ 只有 $O(sqrt(n))$ 个不同取值，且 $floor(floor(n/i)/p)=floor(n/(i p))$，所以转移不会产生表外状态。`smallId` 直接按小取值定位，`largeId` 按 `n/x` 定位；`primeSum(x)` 的 `x` 必须是本实例预处理的整除商，不能当作任意前缀查询接口。

完成筛法后，`dfs(x,j)` 统计 $2<=v<=x$ 且最小质因子下标不小于 $j$ 的 $f(v)$。初值先计入这些质数；合数唯一写成 $p^e u$，其中 $u=1$ 或 $u$ 的所有质因子均大于 $p$。代码中的 `fpe*dfs(x/pe,i+1)` 统计 $u>1$，单独的 `fnext` 统计 $p^(e+1)$，因此指数循环从一次幂开始，却在独立质数幂项中从二次幂开始。最外层再补上 $f(1)=1$。

本实现不使用模逆，正模数不要求为质数。若 $f(p)$ 次数高于二次，必须同步扩展系数、幂和初值、质数前缀和筛除循环，不能只改回调。回调只计算一个质数幂，不要再枚举其他质因子。例：$f(p^e)=p^e(p^e-1)$ 时系数为 `{0,-1,1}`，回调用 `peMod*(peMod-1)` 并规范化。

实际改题时，约数个数的系数取 `{2,0,0}`、回调返回 `e+1`；欧拉函数的系数取 `{-1,1,0}`，回调计算 $p^(e-1)(p-1)$。后者不能写成 `peMod/p*(p-1)`，因为第三个参数已经取模，整数除法不能恢复 $p^(e-1)$；模数也可能与 $p$ 不互质，应另行计算所需幂。回调中的乘法仍需用 `i128` 或板内的模乘，避免在传回结果前溢出。

只统计不超过 $n$ 的质数个数时，构造 `Min25Sieve(n,mod,{1,0,0},...)` 后直接调用 `primeSum(n)`；它返回 $pi(n) mod mod$。需要精确值时应选大于答案的正 `i64` 模数，例如 `(1LL<<62)-1` 足以覆盖本接口的有符号 `i64` 范围。不要把高次质数幂的回调设为零后调用 `solve()`：`solve()` 统计的是完整乘性函数，仍会把含多个不同质因子的合数计入。

#link("https://judge.yosupo.jp/problem/counting_primes")[Library Checker · counting_primes]

#link("https://judge.yosupo.jp/problem/sum_of_multiplicative_function")[Library Checker · sum_of_multiplicative_function]
