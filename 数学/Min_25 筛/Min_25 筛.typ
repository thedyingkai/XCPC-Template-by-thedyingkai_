==== 用途

- 输入上界 $n$ 和一个由质数值、质数幂值确定的乘性函数 $f$，求 $sum_(i=1)^n f(i)$；适合 $f(p)$ 是低次多项式且 $n$ 大到不能逐项筛的题目。

==== 接口与约束

- `Min25Sieve(n,mod,{c0,c1,c2},primePower).solve()` 返回 $sum_(i=1)^n f(i) mod mod$，约定乘性函数 $f(1)=1$、$f(p)=c_0+c_1p+c_2p^2$。
- `primePower(p,e,peMod)` 必须返回 $f(p^e)$；第三个参数是 $p^e mod mod$。当 `e=1` 时，回调结果必须和前三个系数给出的质数值一致。
- 要求 `n>=1,mod>=1`。本板不用模逆，模数不要求为质数；模乘使用 `i128`，模数须放在正 `i64` 范围内。
- 当前只维护质数的 $1,p,p^2$ 三种前缀和。若 $f(p)$ 次数更高，必须同步增加系数、幂和初值、质数前缀与筛除循环，不能只改回调。

==== 改板与 trick

- 经典 $f(p^e)=p^e(p^e-1)$ 可取系数 `{0,-1,1}`，回调写成 `peMod*(peMod-1)` 后规范到模数内。
- 回调只应计算单个质数幂的函数值；乘性组合已由 `dfs` 枚举，不能在回调中再枚举其他质因子。
- 预处理保存所有 $floor(n/i)$ 的不同取值，时间复杂度通常为 $O(n^(3/4)/log n)$、空间 $O(sqrt(n))$；多组不同 `n` 需分别构造实例。
- 若题目函数在质数处不是低次多项式，或质数幂值无法快速计算，本板不能直接套用；先确认函数条件，再考虑杜教筛或其他方法。

==== 板子题

- 洛谷 P5325「Min_25 筛」。
