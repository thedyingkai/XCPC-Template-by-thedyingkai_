==== 接口与边界

- `Matrix<T>` 的整数算术按 `1000000007` 取模，但读入和直接赋值不会自动规范化；换模数需修改 `MOD`。
- 乘法不检查维数；`qp(base, k)` 要求方阵且 $k>=0$。
- 仿射转移可增加一个恒为 $1$ 的状态；初始状态用列向量时计算 $A^k v$。

==== 板子题

- 洛谷 P3390「矩阵快速幂」、P1962「斐波那契数列」。
