==== 用途

- 输入线性方程组的系数矩阵和一组或多组右端项，得到秩、简化行阶梯形和主元位置；据此判定无解、唯一解或多解并恢复解。

==== 接口与边界

- 依赖 `矩阵运算.cpp`；`Gauss` 原地化为简化行阶梯形并返回秩，`aug` 会同步行变换，`w[c]` 是第 `c` 列主元所在行。
- 整数类型按 `MOD` 做费马逆元，只能用于质数模数；浮点类型按绝对值选主元。
- 区分无解与多解时检查矛盾行和秩；多组右端项可放进 `aug` 的多列一次消元。
- 异或方程改用 `bitset` 高斯消元。

==== 板子题

- 洛谷 P3389「高斯消元法」。
