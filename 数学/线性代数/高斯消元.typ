==== 方程组如何落到消元

把每条线性约束写成一行系数，右端常数放进增广矩阵；`Gauss` 原地化成简化行阶梯形并返回秩，`w[c]` 记录第 `c` 列主元所在行。给 `aug` 传入一列或多列右端项，可让它们和系数矩阵接受完全相同的行变换。正确性来自三种初等行变换不改变方程组解集，而每次只用一个新主元消去该列的其他项。

整数 `T` 按 `MOD` 用费马逆元，只能在质数模数的域上使用；浮点 `T` 按绝对值选主元并用误差判断零。消元后若某行系数全零而右端非零，则无解；无矛盾但秩小于未知数个数，则有自由元；只有秩等于未知数个数时才是唯一解。异或方程应把行换成 `bitset` 并在 $F_2$ 上消元，不要继续做普通乘除。

Library Checker 的线性方程组题还要求输出一个特解和齐次解空间的一组基。调用 `A.Gauss(&aug,&w)` 后，若无矛盾，令全部自由变量为零，主元列 `c` 的特解就是 `aug[w[c]][0]`。对每个自由列 `f` 再构造一个齐次基向量：令第 `f` 项为 `1`、其他自由项为 `0`，每个主元列 `c` 取 `-A[w[c]][f]` 并按模数规范化。当前 `solveLinear` 只返回唯一解；遇到多解时应按上面的 `w` 和消元后矩阵自行组织输出，不能直接调用它。

建模时先给未知量编号，再把每条关系整理成 $sum_j a_(i j)x_j=b_i$。随机游走期望常写成 $E_u-sum_v p_(u v)E_v=c_u$，电路或流量平衡也会得到类似方程；概率和转移费用必须是已知系数，未知量相乘或出现在分母中就不是线性方程组。实数问题用浮点消元，题目明确要求在质数模数下求解时用模域消元；实数意义下有解，不保证任意取模后仍有同样的秩。

每次在尚未确定的列中找一个非零主元，把它所在行换到当前处理位置，再把这一行除以主元，使主元等于一。随后用这一行消掉其余各行该列的系数。此前的主元列已经消成单位列，新主元行在那些列上为零，所以不会破坏已完成部分。若某列找不到主元，就暂留为自由列而继续看下一列，不能因为一个对角位置为零就宣告无解。

消元后的每个非零行都可写成“一个主元变量，加上若干自由变量的线性组合，等于右端常数”。例如 $x+y=3$、$2x+2y=6$ 只给出一个独立约束，可写成 $(x,y)=(3,0)+t(-1,1)$；若第二行右端换成 $7$，消元产生 $0=1$，才是无解。一般地，无矛盾时有 $m-r$ 个自由变量，$r$ 为秩；在有限域 $F_p$ 上共有 $p^(m-r)$ 组解，在实数域中自由变量可任意取实数。

当前 `Gauss` 会直接覆盖 `A` 和 `aug`，需要原矩阵做残差检查或处理其他任务时先保留副本。`aug` 应有与系数矩阵相同的行数，且每行右端项数量一致；多个右端项共享一次行变换，但每一列右端仍要各自检查零行是否矛盾。求逆可把单位矩阵作为多列右端同时消元，系数矩阵满秩时右侧就是逆矩阵。行列式还要乘回归一化前的主元，并记录交换行引起的符号变化，不能直接对消元后的全一主元相乘。

设有 $n$ 个方程、$m$ 个未知量、$q$ 列右端，当前消去其他所有行的实现时间为 $O(n min(n,m)(m+q))$，方阵单右端时为 $O(n^3)$。浮点版选择绝对值最大的可用主元以减轻除法误差，但 `1e-12` 是绝对阈值，系数尺度悬殊或矩阵接近奇异时仍可能误判；可以先按行缩放，并用原方程检查残差。不要把浮点近似解四舍五入后就当作整数方程的精确解，整数解还带有整除性要求。

#link("https://judge.yosupo.jp/problem/matrix_rank")[Library Checker · matrix_rank]

#link("https://judge.yosupo.jp/problem/system_of_linear_equations")[Library Checker · system_of_linear_equations]
