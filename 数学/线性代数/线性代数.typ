==== 用途

- 输入方阵可求行列式或逆矩阵，输入 $A x=b$ 可求唯一解；失败结果再结合秩和矛盾行区分不可逆、无解与多解。

==== 接口与边界

- 依赖 `矩阵运算.cpp` 和 `高斯消元.cpp`。
- `det()` 只接受方阵；`inv()` 在非方阵或不可逆时返回空矩阵。
- `solveLinear` 仅在唯一解时返回 `true`；无解和多解都返回 `false`。需要区分两者时直接调用 `Gauss` 检查矛盾行与秩。

==== 板子题

- 洛谷 P4783「矩阵求逆」。
