康托展开把 `1..n` 的排列映射为从 $1$ 开始的字典序排名。第 $i$ 位若还有 $c_i$ 个未使用元素比它小，就贡献 $c_i(n-1-i)!$；`encode` 用树状数组统计这些 $c_i$。逆变换依次把排名除以阶乘，得到每一位应选择的第几个未使用元素。

`CantorExpansion(max_n)` 要求 `0 <= max_n <= 20`，因为排名和阶乘存于 `i64`。`encode` 会检查输入恰为 `1..n` 的排列，`decode(rank,n)` 会检查 `1 <= rank <= n!`；编码为 $O(n log n)$，当前解码线性寻找第 $k$ 个未使用数，为 $O(n^2)$。大 $n$ 只求模意义排名时可把阶乘和答案取模，但这样的余数不能再用于解码；若要精确解码更大排列，应先更换大整数类型，并用树状数组二分把选数降到 $O(n log n)$。

洛谷 P5367 只要求模意义的编码，需按上述方式去掉精确 `i64` 排名限制。
