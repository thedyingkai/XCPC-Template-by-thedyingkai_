==== 用途

- 输入一个 `1..n` 的排列，得到它在全排列字典序中的排名；输入合法排名可反向恢复对应排列。

==== 接口与边界

- `encode`、`decode` 都使用从 $1$ 开始的排名；排列必须恰为 `1..n`，且 `n <= max_n <= 20`。
- `decode` 不检查排名是否在 `1..n!` 内，当前复杂度为 $O(n^2)$；用树状数组二分第 $k$ 个未使用数可改为 $O(n log n)$。
- 元素不连续时先离散化；有重复元素时需改为多重集排列计数。
- 只求排名模某个数时可把阶乘和答案取模，但该结果不能用于解码。

==== 板子题

- 洛谷 P5367「康托展开」：删去 `max_n <= 20` 限制，把阶乘和累计排名改为题目模数下计算，只保留编码。
