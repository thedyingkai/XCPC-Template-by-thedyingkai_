==== 用途

- 输入一个有符号 `i64` 范围内的正整数，先判定其是否为质数，再在需要时输出完整质因数分解及各指数。

==== 接口与边界

- `isPrime` 是有符号 `i64` 范围的确定性 Miller--Rabin；不要删改当前测试底数。
- `factor(n)` 只处理 `n>1`，返回 `map<质因子, 指数>`；负数先处理符号，`0` 必须特判。
- `pollard` 使用随机数和 Floyd 判环，运行时间有波动；卡常时可换成 Brent 判环和批量 `gcd`。

==== 板子题

- 洛谷 P4718「Pollard-Rho」。
