`isPrime(n)` 用确定性 Miller--Rabin 判断正 `i64` 范围内的整数是否为质数；当前七个测试底数覆盖整个 64 位范围，不能随意删减。`factor(n)` 对 `n>1` 反复用 Pollard--Rho 找非平凡因子，再递归到质数，返回 `map<质因子,指数>`。负数应先单独记录符号，`0` 和 `1` 必须按题意特判。

少量大整数的判质、分解，以及由质因数求约数、欧拉函数、质因子指数时使用这份板。若大量询问的数都落在可以预筛的值域内，筛最小质因子后逐次相除通常更合适；只需要判质时直接调用 `isPrime`，无需完整分解。函数都在 `PollardRho` 命名空间内，文件末尾的 `main` 只是“质数输出 Prime，否则输出最大质因子”的入口，多组分解或输出所有因子时需替换对应读写。

Miller--Rabin 将 $n-1$ 写成 $d 2^s$，其中 $d$ 为奇数。对每个固定底数 $a$，先算 $x=a^d mod n$；若 $x$ 为 $1$ 或 $n-1$，本轮通过。否则最多再平方 $s-1$ 次，出现 $n-1$ 才通过，始终未出现就判为合数。一个合数可能通过某个底数的检验，当前七个固定底数共同保证上述范围内的判定正确。

检验依据是质数模数下的两个性质。若 $n$ 为奇质数且 $a$ 不被 $n$ 整除，费马小定理给出 $a^(n-1)=1$；同时 $z^2=1$ 只有 $z=1,-1$ 两个解，因为 $(z-1)(z+1)=0$ 且模质数的域中没有零因子。考察 $a^d,a^(2d),...,a^(2^s d)$：若首项不是一，那么第一次平方变为一之前必为负一。缺少这两种情况时，$n$ 就不可能是质数。合数模数可能有其他一的平方根，或偶然通过某一底数，所以单次费马检验、单个 MR 底数都不够。

代码先处理 $n<2$ 和小质数的整除情况，再检测七个底数。底数大于 $n$ 时仍要取模后测试；`a % n == 0` 只跳过当前这一轮，不能因此直接宣布 $n$ 为质数。固定底数集合的覆盖范围与实现的数值范围是两回事：这份接口仍只接收不超过 `LLONG_MAX` 的整数，并不支持把 $2^63$ 以上的数强转成 `i64`。

Pollard--Rho 在模 $n$ 的伪随机序列中寻找两项差与 $n$ 的非平凡公因子。若 `gcd` 的结果等于 $n$，就重新选择随机参数，因此运行时间会有波动。大量接近上界的半素数需要降低常数时，可改用 Brent 判环和批量 `gcd`；Miller--Rabin 的七个底数应保留。

设 $p$ 是尚不知道的一个质因子。取 $f(x)=x^2+c mod n$，从随机起点不断迭代。将整条序列再对 $p$ 取模，相当于直接在模 $p$ 下迭代同一个多项式，因为同余在加法、乘法下保持。若两项在模 $p$ 下相等，它们的差就被 $p$ 整除，即使两项在模 $n$ 下还不相等，$gcd(|x-y|,n)$ 也能把这个共同因子找出来。算法无需提前知道 $p$，这正是每轮求最大公约数的作用。

模 $p$ 只有有限个状态，迭代序列最终会先经过一段不重复的前缀，再进入环。板子使用 Floyd 判环的两个速度，每轮让 `x` 走一步、`y` 走两步，从而比较同一条序列的第 $i$ 项和第 $2i$ 项；进入环后，两者的相对位置每轮推进一格，最终会在模 $p$ 下相遇。这里不用保存整条序列，单次寻找因子的额外空间为 $O(1)$。

每轮的最大公约数有三种结果：等于一时继续走；严格介于一和 $n$ 之间时找到非平凡因子；等于 $n$ 时，这次碰撞没有把 $n$ 拆开，重新随机选择 `c` 和起点。最后一种情况不能当作“已经证明是质数”。内部 `pollard(n)` 应只对已知合数使用；外层先作 MR 判定，就是为了避免对质数反复寻找根本不存在的非平凡因子。

例如分解 $91$，取 $c=1$、起点为二。第一轮慢指针到 $5$，快指针到 $26$，于是 $gcd(26-5,91)=7$，得到 $91=7 times 13$。接着分别判定七和十三为质数，最终返回 `{7:1,13:1}`。一次找到的因子也可能仍是合数，所以代码必须对 `d` 和 `n/d` 都递归；重复出现同一质因子时累加指数，不能只记录一次 gcd 的结果就结束。

常用的速度估计来自随机映射模型：模一个质因子 $p$ 的序列，通常经过约 $O(sqrt(p))$ 步出现碰撞，类似从 $p$ 个值中抽样的生日悖论。合数至少有一个不超过 $sqrt(n)$ 的质因子，因此单次找因子常按 $O(n^(1/4))$ 轮估计；每轮还有模乘与 gcd 的代价，若按欧几里得算法估计 gcd，可再计入 $O(log n)$ 因子。这是随机性假设下的常用估计，不是每个参数、每个输入的严格最坏时间保证，完整分解还包括重试、递归与各层判质。固定七个底数的 MR 使用 $O(log n)$ 次模乘。

乘积取模前必须先扩到 `i128`；先用 `i64` 相乘再转型已经来不及。对于正 `i64` 模数，两个归一化余数的乘积能放进有符号 `i128`，函数迭代中的平方加 `c` 也在扩宽后计算。若要支持完整 `u64` 范围，需统一重写无符号边界与 `u128` 模乘，不能只改参数类型。负输入取绝对值时还要避开 `abs(LLONG_MIN)` 溢出；`factor(n)` 对 `n<=1` 仅返回空表，并不会自动完成负数分解或处理零的约数含义。

#link("https://judge.yosupo.jp/problem/primality_test")[Library Checker · primality_test]

#link("https://judge.yosupo.jp/problem/factorize")[Library Checker · factorize]
