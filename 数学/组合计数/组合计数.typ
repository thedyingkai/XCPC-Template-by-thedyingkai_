==== 用途

- 输入规模上界 $n$ 和模数，得到 Catalan、错排、Bell、整数拆分、两类 Stirling 数和欧拉数表；按对象是否有标号、盒子是否有标号及是否允许为空选择对应序列。

==== 接口与边界

- `CombinatorialNumbers::init(n,mod)` 用 $O(n^2)$ 时间和空间集中预处理 Catalan 数、错排数、Bell 数、整数拆分数、第一类/第二类 Stirling 数和欧拉数，要求 `n>=0`、`mod>0`；适合中小 `n`，不要为只需一个数开满二维数组。
- 只求整数拆分数时调用 `partitionNumbers(n,mod)`，使用五边形数递推，时间 $O(n sqrt(n))$、空间 $O(n)$。
- 所有数组均已按 `mod` 取模；递推只用加法与乘法，`mod` 不要求为质数。

==== 常用计数

- Catalan 数：$c_n=C(2n,n)/(n+1)=C(2n,n)-C(2n,n-1)$，也满足 $c_n=sum_(i=0)^(n-1) c_i c_(n-1-i)$。用于合法括号、不过对角线的格路和二叉树形态。
- 错排：$D_0=1,D_1=0,D_n=(n-1)(D_(n-1)+D_(n-2))$，也有 $D_n=n!sum_(i=0)^n (-1)^i/i!$。
- 第二类 Stirling 数满足 $S_2(n,k)=S_2(n-1,k-1)+k S_2(n-1,k)$，把 $n$ 个有标号元素分成 $k$ 个非空无标号集合；满射数为 $k! S_2(n,k)$。
- 无符号第一类 Stirling 数满足 $S_1(n,k)=S_1(n-1,k-1)+(n-1) S_1(n-1,k)$，统计含 $k$ 个置换环的排列；有符号版本再乘 $(-1)^(n-k)$。
- Bell 数为 $B_n=sum_k S_2(n,k)$，统计集合划分。
- 欧拉数满足 $A(n,k)=(n-k)A(n-1,k-1)+(k+1)A(n-1,k)$，统计恰有 $k$ 个下降位置的排列。
- `partition[n]` 统计把 $n$ 拆成无序正整数之和的方案数。恰拆成 $k$ 份可用 $p(n,k)=p(n-1,k-1)+p(n-k,k)$；总拆分数还满足 $p(n)=sum_(k>=1)(-1)^(k-1)(p(n-k(3k-1)/2)+p(n-k(3k+1)/2))$，负下标项视为零。

==== 改板与 trick

- Catalan 的除法公式在合数模数下不能直接求逆；当前代码使用卷积递推保证任意模数可用。质数模且已预处理组合数时改用差式可降为 $O(1)$。
- 满射优先用容斥 $sum_(i=0)^k (-1)^i C(k,i)(k-i)^n$；需要整行 Stirling 数时再使用递推或卷积算法。
- 元素有标号、盒子有无标号、盒子是否允许为空会改变公式；套公式前先写清四个条件。

==== 板子题

- 洛谷 P1754「球迷购票问题」、HDU 1465「不容易系列之一」。
