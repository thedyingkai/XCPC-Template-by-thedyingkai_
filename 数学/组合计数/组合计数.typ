`CombinatorialNumbers::init(n,mod)` 用 $O(n^2)$ 时间和空间预处理卡特兰数、错排数、贝尔数、无符号第一类和第二类斯特林数、欧拉数，并用五边形数递推得到整数拆分数；这些递推只有加法和乘法，正模数不要求为质数。只求整数拆分时调用 `partitionNumbers(n,mod)`，复杂度 $O(n sqrt n)$，可省去其余二维表。

卡特兰数统计合法括号、不过对角线的格路和二叉树形态：
$C_n=C(2n,n)/(n+1)=C(2n,n)-C(2n,n-1)$，也满足
$C_n=sum_(i=0)^(n-1) C_i C_(n-1-i)$。板子使用最后一条卷积递推，因此合数模数也可用；质数模下若已经预处理组合数，用差式即可 $O(1)$ 求单项。

错排满足 $D_0=1,D_1=0,D_n=(n-1)(D_(n-1)+D_(n-2))$，也可由容斥直接写成 $D_n=n! sum_(i=0)^n (-1)^i/i!$。第二类斯特林数把 $n$ 个有标号元素分成 $k$ 个非空无标号集合：
$S_2(n,k)=S_2(n-1,k-1)+k S_2(n-1,k)$，满射数为 $k! S_2(n,k)$。无符号第一类斯特林数统计有 $k$ 个置换环的排列：
$S_1(n,k)=S_1(n-1,k-1)+(n-1)S_1(n-1,k)$；有符号版本再乘 $(-1)^(n-k)$。贝尔数是 $sum_k S_2(n,k)$。

欧拉数 `eulerian[n][k]` 统计恰有 $k$ 个下降位置的排列，递推为
$A(n,k)=(n-k)A(n-1,k-1)+(k+1)A(n-1,k)$。`partition[n]` 统计无序正整数拆分；恰拆成 $k$ 份时有 $p(n,k)=p(n-1,k-1)+p(n-k,k)$。只求总拆分数时，五边形数递推是
$p(n)=sum_(k>=1)(-1)^(k-1)(p(n-k(3k-1)/2)+p(n-k(3k+1)/2))$，负下标项按零处理。

套表前先写清元素和盒子是否有标号、盒子是否允许为空。合数模数下不要直接除以 $n+1$。满射也可用容斥 $sum_(i=0)^k (-1)^i C(k,i)(k-i)^n$，只问单点时通常比整张斯特林表省空间。
