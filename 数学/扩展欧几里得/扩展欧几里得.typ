`exgcd(a,b,x,y)` 求 $a x+b y=gcd(a,b)$ 的一组贝祖系数。递归从 $gcd(b,a mod b)$ 回代，恒等式 $a mod b=a-floor(a/b)b$ 给出新系数；因此线性方程 $a x+b y=c$ 有整数解当且仅当 $gcd(a,b)$ 整除 $c$，一组特解乘上 $c/g$ 后，全部解为 $x=x_0+k(b/g)$、$y=y_0-k(a/g)$。

当前主程序对应洛谷 P5656，先读测试组数 `T`，再逐组读入正整数 `a,b,c`，按题目格式输出正整数解范围。改成非负解时要把参数区间下界从一改为零；允许负输入时必须使用数学意义的 `floor_div`、`ceil_div` 推参数范围，不能依赖 C++ 向零截断。求 $a x equiv c \(mod m\)$ 时令第二个系数为 $m$，先判整除，再把特解规范到模 $m/g$ 的最小非负代表元。系数乘 $c/g$ 可能越过 `i64` 时，应把 `x,y` 和回代中间量一起换成 `i128`。
