==== 用途

- 输入 $a,b,c$，判断并求出 $a x+b y=c$ 的整数解范围；同一组系数也可用于解线性同余 $a x equiv c \(mod m\)$。

==== 接口与改板

- `exgcd(a,b,x,y)` 给出 $a x+b y=gcd(a,b)$ 的一组系数；当前主程序只处理正整数 $a,b,c$，输出格式对应洛谷 P5656。
- 有正整数解时依次输出解数、`xmin ymin xmax ymax`；只有整数解但没有正整数解时输出 `xmin ymin`，无整数解输出 `-1`。
- 求非负解时把参数范围的边界由 $1$ 改为 $0$；涉及负数时用数学意义的 `floor_div`、`ceil_div`，不能直接套 C++ 截断除法。
- 解 $a x equiv c \(mod m\)$ 时先判 $gcd(a,m)$ 是否整除 $c$，再把特解规范到模 $m/gcd(a,m)$ 的最小非负代表元。
- 系数乘 $c/gcd(a,b)$ 可能溢出时改用 `i128`。

==== 板子题

- 洛谷 P5656「二元一次不定方程」。
