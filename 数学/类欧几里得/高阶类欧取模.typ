==== 用途

- 输入等差取整序列 $q_i=floor((a i+b)/m)$ 和奇数模数，求 $sum q_i$、$sum i q_i$、$sum q_i^2$ 的模值；适合精确和会溢出 `i128` 的题目。

==== 接口与边界

- 令 $q_i=floor((a i+b)/m)$。`highFloorSumsMod(n,m,a,b,mod)` 返回 $i=0..n-1$ 上的 $sum q_i$、$sum i q_i$、$sum q_i^2$，字段依次为 `sumFloor`、`sumIndexFloor`、`sumFloorSquare`。
- 要求 `n>=0,m>0`，`a,b` 可为负数；`mod` 必须为正奇数，不要求是质数。返回值均已规范到 `0..mod-1`。
- 递归的 `n,m,a,b` 保持精确，三项统计全程取模。平方和中的除以 $6$ 先在整数因子中约掉，因此即使 `mod` 是 $3$ 的倍数也能使用。
- 偶数模数不能只删掉断言：互换递推含除以 $2$，需要另行保存足够的整除信息。

==== P5170

- 洛谷 P5170 的求和范围是 $i=0..n$，调用时传 `n+1`、`c`、`a`、`b` 和 `998244353`。
- 题目输出顺序为 `sumFloor`、`sumFloorSquare`、`sumIndexFloor`，与结构体字段顺序不同。
- 当前文件可直接承受 P5170 的值域；原“高阶类欧”精确版只在确认所有中间量不溢出 `i128` 时使用。

==== 板子题

- 洛谷 P5170「类欧几里得算法」。
