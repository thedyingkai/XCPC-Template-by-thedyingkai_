`highFloorSumsMod(n,m,a,b,mod)` 对 $q_i=floor((a i+b)/m)$ 返回 $sum q_i$、$sum i q_i$、$sum q_i^2$ 的模值，范围为 `i=0..n-1`。要求 `n>=0`、`m>0`，`a,b` 可负，`mod` 必须是正奇数但不要求为质数。递推的几何参数保持精确，三项统计全程取模；平方和公式中的除以六先在整数因子上约掉，所以模数可被三整除。

递推与前一份高阶类欧相同，取模只作用于统计量。决定分支和下一层范围的 $floor((a(n-1)+b)/m)$、`a/m`、`b/m` 都必须按原整数计算；例如把 $a$ 先对答案模数取模，会改变直线的位置，连数到哪些整点都变了。代码用 `i128` 保存这部分几何参数，用模加、模乘处理 $F,G,H$，因此不需要先算出可能远超 `i128` 的精确统计结果。

模数为奇数时，$2$ 的逆元可直接取 $(mod+1)/2$，无需费马小定理。对于 $S_2=n(n-1)(2n-1)/6$，代码先取 `n % (6*mod)`，保留除以六所需的信息，再从三个整数因子里约去 $2$ 和 $3$，最后取模相乘。这与把三个因子先分别模 `mod` 后直接除以六不同，后者会丢掉整除信息，尤其在模数含因子 $3$ 时出错。

区间平移、余数和及余数平方和仍用前一份说明中的展开式，每次加减乘都在答案模数下进行。`mod=1` 的三项均为零；多次查询相同模数时可复用 `HighFloorSumsModSolver` 并调用 `solve`。浮点近似商不能替代整数除法，边界上一个单位的误差会影响整层统计。

奇数限制来自互换递推中的除以二。偶数模数不能删除断言后继续运行，必须额外保存足够的二进制整除信息或改用别的推导。洛谷 P5170 的范围是 `i=0..n`，调用时传 `n+1,c,a,b,998244353`；题目输出顺序为一次和、平方和、带下标和，与结构体字段顺序不同。
