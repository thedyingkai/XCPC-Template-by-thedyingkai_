令 $q_i=floor((a i+b)/m)$。`highFloorSums(n,m,a,b)` 一次返回 $sum q_i$、$sum i q_i$、$sum q_i^2$，字段依次为 `sumFloor`、`sumIndexFloor`、`sumFloorSquare`，求和范围是 `i=0..n-1`。要求 `n>=0`、`m>0`，`a,b` 可以为负，三项均为精确 `i128`。

这类式子常出现在直线下整点计数、等差数列取模后的和，以及含整除商的加权统计中。$q_i$ 可理解为第 $i$ 列的高度；一次和数整点，带下标和给每列乘横坐标，平方和则与每列高度的平方有关。例如 $n=4,m=3,a=2,b=1$ 时，$q_i$ 依次为 $0,1,1,2$，三项依次为 $4,9,6$，可用来确认字段和求和范围。

先拆出 $a,b$ 除以 $m$ 的整数部分，使剩余参数满足 $0<=a,b<m$。若 $n=0$ 或 $a=0$，剩余三项均为零；否则令 $N=n-1$、$y=floor((a N+b)/m)$，再令 $t_j=floor((m j+m-b-1)/a)$，其中 $0<=j<y$。对固定的 $j$，满足 $q_i>j$ 的下标恰好是 $t_j+1,...,N$。交换枚举顺序后，只需递归求 $t_j$ 的三种和，这对应代码中的 `highFloorNonnegative(y,a,m,m-b-1)`。

若递归结果为 $F=sum t_j$、$G=sum j t_j$、$H=sum t_j^2$，则原来的三项为
$sum q_i=N y-F$，
$sum i q_i=y N(N+1)/2-(F+H)/2$，
$sum q_i^2=N y^2-2G-F$。
第一式逐层统计高度；第二式对每层的下标区间求等差和；第三式使用 $q_i^2=sum_(j=0)^(q_i-1)(2j+1)$。最后把最初拆出的 $q_i=q'_i+q_a i+q_b$ 代回三项，展开后用下标和与下标平方和补齐。

补回整数部分时，记余数问题的答案为 $F',G',H'$，并记 $S_1=sum_(i=0)^(n-1)i$、$S_2=sum_(i=0)^(n-1)i^2$，则
$F=F'+q_a S_1+q_b n$，
$G=G'+q_a S_2+q_b S_1$，
$H=H'+q_a^2 S_2+2q_a q_b S_1+q_b^2 n+2q_a G'+2q_b F'$。
第三式的交叉项说明了为什么平方和递推还需要同时维护带下标和。负参数也按向下取整求 $q_a,q_b$，才能让余数落在 $[0,m)$；C++ 的整数除法朝零截断，不能直接代替 `highFloorDiv`。

区间 $i=L,...,R$ 可以先作两次前缀相减。也可令 $i=L+j$，将 `n` 改为 `R-L+1`、`b` 改为 `a*L+b`；此时返回的带下标和是 $sum j q_(L+j)$，要再加 $L sum q_(L+j)$ 才是原下标的结果。若求 $r_i=(a i+b) mod m$ 的非负余数和，用 $r_i=a i+b-m q_i$；求余数平方和则展开为
$sum r_i^2=a^2 S_2+2a b S_1+n b^2-2m(a G+b F)+m^2 H$。
平移参数和展开式的中间乘法也必须检查范围。

交换递推使参数像欧几里得算法一样缩小，递归层数与其同阶。只求第一项时使用 `floorSum`；需要取模且精确中间量可能越界时，应直接用下一份取模递推。使用精确版前需按 $n,a,b,m$ 估算所有乘积是否装入 `i128`，仅在返回值处取模不能避免中间溢出。
