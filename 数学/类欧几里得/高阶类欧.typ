令 $q_i=floor((a i+b)/m)$。`highFloorSums(n,m,a,b)` 一次返回 $sum q_i$、$sum i q_i$、$sum q_i^2$，字段依次为 `sumFloor`、`sumIndexFloor`、`sumFloorSquare`，求和范围是 `i=0..n-1`。要求 `n>=0`、`m>0`，`a,b` 可以为负，三项均为精确 `i128`。

递推与普通 `floorSum` 一样先拆掉 $a,b$ 的整除部分，再交换横纵方向；同时维护一次、带下标和平方三种统计，回代时用等差和与平方和补回仿射部分。递归层数与欧几里得算法同阶。只求第一项时应使用短板；需要取模且精确中间量可能越界时，应直接用下一份取模递推，不能只在返回值处取模。使用精确版前需按 $n,a,b,m$ 估算所有乘积是否装入 `i128`。
