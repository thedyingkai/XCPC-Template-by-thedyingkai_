==== 用途

- 输入等差式 $a i+b$、除数 $m$ 和项数 $n$，一次得到 $sum floor((a i+b)/m)$、$sum i floor((a i+b)/m)$ 与平方和的精确值。

==== 接口与语义

- 令 $q_i=floor((a i+b)/m)$。`highFloorSums(n,m,a,b)` 同时返回 $sum_(i=0)^(n-1) q_i$、$sum_(i=0)^(n-1) i q_i$、$sum_(i=0)^(n-1) q_i^2$。
- 要求 `n>=0,m>0`；`a,b` 可以为负数。三项字段依次为 `sumFloor`、`sumIndexFloor`、`sumFloorSquare`，均为 `i128`。
- 当前板返回精确整数，只能在三项答案和递推中间量都能装入 `i128` 时使用；大范围取模题使用下一份取模板。

==== 改板与 trick

- 递归层数与欧几里得算法同阶；只求第一项时直接使用短板 `floorSum`，不要携带另外两项。
- 不能只在返回值处取模：平方和中间量可能先溢出；需要取模时直接换用取模版递推。
- 负参数依赖 `highFloorDiv` 的向下取整规范化；若题目保证 `a,b>=0`，可删去公开接口外层的这一步。
- 平方和量级还受 `a,b,m` 影响，并不只由 $n^3$ 控制；改大参数前先估算上界。
