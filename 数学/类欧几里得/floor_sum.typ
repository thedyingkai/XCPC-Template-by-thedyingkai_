==== 用途

- 输入 $n,m,a,b$，求 $i=0..n-1$ 上 $floor((a i+b)/m)$ 的总和；可直接表示直线下整点数、周期计数和等差分子整除和。

==== 接口与边界

- `floorSum(n,m,a,b)` 返回 $sum_(i=0)^(n-1) floor((a i+b)/m)$，要求 `n>=0,m>0`；`a,b` 可以为负数。
- 参数、返回值及中间乘法均为 `i128`。若题目接口是 $i=0..n$，调用时把首参改为 `n+1`。
- C++ 的负数除法向零截断；本板先用数学意义的向下取整规范化 `a,b`，不要直接删掉 `floorDiv`。

==== 改板与 trick

- 递归层数与欧几里得算法同阶。
- 区间 $i=L..R$ 可分别计算长度为 `R+1` 与 `L` 的两个前缀再相减。
- 只需非负参数且确认答案不溢出时，可以把接口收窄为 `i64`；不要只改返回类型而保留 `a*n+b` 的 `i64` 乘法。

==== 板子题

- Library Checker `floor_sum`、AtCoder `practice2_c`。
