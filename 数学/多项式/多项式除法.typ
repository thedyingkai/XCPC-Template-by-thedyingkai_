`divideWithRemainder(A,B)` 求唯一的 $Q,R$，使 $A=B Q+R$ 且 $deg R<deg B$；除式必须非零。把两个多项式反转后，商的高次部分转成形式幂级数除法，因此可以用求逆和 NTT 在 $O(n log n)$ 内得到商，再回代求余数。`divide`、`modulo` 分别只返回其中一项，结果会删去末尾零系数。

这里反转后要求可逆的是原除式的最高次项，所以原常数项可以为零。形式幂级数的截断除法是另一件事：只求 $A/B mod x^n$ 时应计算 `multiply(A,inverse(B,n),n)`，并要求 `B[0] != 0`。线性递推的特征多项式取模和乘积树多点求值使用的是本文件的整式余数。

设 $deg A=n$、$deg B=m$，且 $n>=m$，则商有 $n-m+1$ 项。记 $A^*(x)=x^n A(1/x)$，其系数就是把 $A$ 反转；同样定义 $B^*$ 和按次数 $n-m$ 反转的 $Q^*$。由 $A=B Q+R$ 得到 $A^*=B^* Q^*+x^n R(1/x)$。因为 $deg R<m$，最后一项的最低次数至少为 $n-m+1$，所以在这一精度下被截掉，留下 $Q^*=A^* / B^* mod x^(n-m+1)$。

因此先反转并截出所需长度，乘上 `inverse(B*,n-m+1)`，再反转回来得到商。回代 $R=A-B Q$，保留次数小于 $m$ 的部分即可。若 $deg A<m$，商为零、余数就是 $A$；若 $B$ 是非零常数，余数为零。例如 $A=x^3+1$、$B=x+1$，得到 $Q=x^2-x+1$、$R=0$，数组中的负系数按模数表示。

用于线性递推时，若特征多项式为 $C(x)=x^d-c_0 x^(d-1)-...-c_(d-1)$，在模 $C$ 下可把 $x^d$ 替成低次组合。不断做“相乘后取余”，就能计算 $x^k mod C$，余数的 $d$ 个系数再与初值组合成第 $k$ 项。这里约掉的是固定特征多项式，不能误写成只保留前 $d$ 项。

接口会先清理输入尾零，以实际次数决定商长；返回的零多项式用空数组表示。需要访问常数项前先判断是否为空，输出要求固定长度时再自行补零。

#link("https://judge.yosupo.jp/problem/division_of_polynomials")[Library Checker · division_of_polynomials]
