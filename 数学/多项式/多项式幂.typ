==== 用途

- 输入多项式 $f$、非负整数 $k$ 和长度 $n$，求 $f(x)^k mod x^n$ 的前 $n$ 项；适合需要整段形式幂而不是只问某个极高次系数的题目。

==== 接口与边界

- `polynomialPower(f,k,n)` 返回 $f(x)^k mod x^n$，`k` 可传非负 `i64` 或十进制字符串，复杂度 $O(n log n)$。
- 自动处理前导零和任意非零首项；`k=0` 返回常数多项式 `1`，零多项式的正次幂返回零。
- 字符串指数会分别保留次数平移所需的截断值、模 `998244353` 的值和模 `998244352` 的值；不要把三者混成同一个余数。

==== 改法与 trick

- 求有理指数时需先保证常数项为 `1`，再把 `k` 换成模意义下的分数；首项非 `1` 时还要单独定义其幂。
- 只求一个极高次系数不要展开整份形式幂，优先改成线性递推或 Bostan–Mori。

==== 板子题

- 洛谷 P5245「多项式快速幂」。
