`polynomialPower(f,k,n)` 求 $f(x)^k mod x^n$，指数可传非负 `i64` 或非负十进制字符串。实现先找到最低非零项 $c x^t$：若 $t k>=n$，答案全零；否则提出 $x^(t k)c^k$，把剩余常数项归一成一，再计算 $exp(k ln f)$。这样能正确处理前导零、任意非零首项和零多项式，复杂度 $O(n log n)$。

字符串指数同时维护次数平移所需的截断值、模 $998244353$ 的系数倍数和模 $998244352$ 的首项指数，三者不能混成同一个余数。`k=0` 返回常数多项式一。有理指数只有在常数项和根的定义满足题意时才有意义，不能直接把参数类型换成分数。

写成 $f=x^t c h$ 后，$h_0=1$，于是 $f^k=x^(t k)c^k exp(k ln h)$。这三个部分分别决定最低次数、首项系数和后续系数。比如 $f=2x+2x^2$、$k=3$，先提出 $8x^3$，再算 $(1+x)^3$，结果为 $8x^3+24x^4+24x^5+8x^6$；若只求模 $x^3$ 的结果，则整段都已落在截断范围之外，直接为零。

指数很大时，次数平移仍要比较真正的 $t k$ 与 $n$，不能先对模数取余。字符串接口把这部分截在 $n$，足以判断是否全部越界；$ln h$ 的系数乘以 $k$ 是域内乘法，所以只需 $k mod p$；$c$ 非零，根据费马定理，$c^k$ 只需指数模 $p-1$。例如 $h=1+x$ 且 $k=p$，在目标长度小于 $p$ 时，结果只有常数项一，这与特征为 $p$ 的域中 $(1+x)^p=1+x^p$ 一致。

该接口适合指数很大、需要全部前 $n$ 项的情形；指数很小时也可用若干次普通卷积。`k=0` 的分支先执行，所以零多项式的零次幂也按一处理；正指数下零多项式返回全零。若需要负整数幂，先确认常数项非零，再求逆并取正幂；存在前导零时不再是普通形式幂级数，当前接口不处理。

整数指数接口的多项式部分为 $O(n log n)$；十进制字符串接口还要加上读取指数各位的时间。寻找首项会扫描传入的 `f`，若它远长于目标精度，可在调用前截到 $n$ 项，否则还要计入扫描输入长度的代价。输出固定为 $n$ 项，即使末尾是零也会保留，后续若当作整式使用，应按需要去掉尾零。

#link("https://judge.yosupo.jp/problem/pow_of_formal_power_series")[Library Checker · pow_of_formal_power_series]
