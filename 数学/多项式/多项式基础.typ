==== 用途

- 输入两个系数数组可求卷积，输入一个多项式可求形式导数或积分；输出均为从常数项开始排列、可按需要截断的系数数组。

==== 接口与边界

- 本章所有多项式使用从 $0$ 开始的系数数组和模数 $998244353$；`multiply(a,b,need)` 只保留前 `need` 项，省略参数则返回完整卷积。
- `derivative`、`integral` 分别求形式导数、积分，积分常数固定为 $0$；最大 NTT 长度为 $2^23$。
- 这套接口与已有 NTT 的 `1` 下标包装不同，不能直接混用；需要完整多项式运算时统一使用本章的零下标接口。

==== 改法与 trick

- 更换模数时必须同时确认原根和所需二次幂长度；多项式长度达到模数时，积分中的整数逆元不再全部存在。
- 只做一次普通卷积时继续用原 NTT 板更短；本基础文件用于后续求逆、对数、指数、平方根和多点求值。
