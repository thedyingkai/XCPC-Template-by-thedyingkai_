本组接口把系数按常数项到高次项的顺序放在零下标数组中，模数固定为 $998244353$。`multiply(a,b,need)` 求卷积，给出 `need` 时只保留前 `need` 项；`derivative` 和 `integral` 分别求形式导数与积分，积分常数固定为零。所有后续求逆、对数、指数、幂和多点求值都复用这里的 NTT。

多项式乘法对应“把两个独立部分的大小相加”。若 $a_i$、$b_j$ 分别记录大小为 $i$、$j$ 的方案数，乘积的第 $k$ 项就是 $sum_(i+j=k) a_i b_j$。例如 `{1,2}` 与 `{3,4}` 分别表示 $1+2x$、$3+4x$，乘积为 `{3,10,8}`。先从题目的求和式中找出下标相加的关系，再决定哪些量放进系数；只是出现两层循环，并不意味着能直接卷积。

$F=G mod x^n$ 表示二者次数小于 $n$ 的系数相同，所有 $x^n$ 及更高项都不再关心。这里的 $x$ 是记录下标的形式符号，不需要给它一个实数值，也不用考虑级数是否收敛。乘法中高次项不会影响低次项，所以可以随时截去不需要的尾部；导数会把次数降低一，求前 $n$ 项导数时却需要原式的前 $n+1$ 项。数组长度 $n$ 对应最高次数 $n-1$，这两个量不要混用。

求逆、对数、指数等接口的目标是“前 $n$ 项”，不是对整条无限序列完成运算。做生成函数题时，先推导等式，再把加法、乘法、求逆、求导等操作逐一替换成接口；例如 $F=1+x F^2$ 是方程，不能只算一次右端就当作解。牛顿迭代可以由短答案逐次扩展精度，后续模板分别给出对应的更新公式。

最大 NTT 长度为 $2^23$，限制的是补到二次幂后的变换长度，不是某一个输入数组的长度；一次普通卷积需要至少容纳两数组长度之和减一。积分需要 $1,2,...$ 的逆元，而当前可用长度远小于模数。更换模数时必须同时更换原根并核对长度，不能只改常量。单次卷积若使用外层 NTT 板，要注意它是 `1` 下标，不能与这里的数组直接混用。

#link("https://judge.yosupo.jp/problem/convolution_mod")[Library Checker · convolution_mod]
