==== 用途

- `poly998::squareRoot(f,n)` 输入多项式 $f$ 和长度 $n$，求 $g(x)^2 equiv f(x) mod x^n$；有解时返回一种平方根的前 $n$ 项，无解返回 `nullopt`，复杂度 $O(n log n)$。

- 最低非零项次数必须为偶数，最低项系数必须是模 $998244353$ 的二次剩余；零多项式返回全零。
- 常数平方根使用 Tonelli--Shanks，并固定取两个常数根中较小者；将返回值逐项取负仍是一个根。最低非零次数大于 $0$ 时可能有更多根，本接口只返回固定一种。
- 返回值只保证模 $x^n$ 意义下正确。洛谷 P5205「多项式开根」。
