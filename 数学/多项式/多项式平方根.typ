`poly998::squareRoot(f,n)` 求一种 $g(x)^2 equiv f(x) \(mod x^n\)$ 的解，无解返回 `nullopt`。最低非零项次数必须为偶数，且其系数必须是模 $998244353$ 的二次剩余；提出对应的 $x^(2t)$ 后，常数平方根由 Tonelli--Shanks 求得，其余项用 $g'=(g+f/g)/2$ 倍增。

零多项式返回全零。常数根固定取两个根中较小者，将整个结果取负仍是另一个根；最低非零次数大于零时可能还有由截断产生的其他根，本接口只返回固定一种。返回值只保证模 $x^n$ 意义下正确。

先只看目标精度以内的项。若 $f=x^d h$ 且 $h_0!=0$，平方会把最低非零次数翻倍，所以 $d$ 为奇数时无解；$d=2t$ 时令 $g=x^t q$，问题化成 $q^2=h mod x^(n-2t)$。常数项还要求 $q_0^2=h_0$。例如 $x$ 没有这种平方根，而 $4x^2+4x^3+x^4$ 可以取 $2x+x^2$。

常数根在模域中求，不是对整数调用 `sqrt`。代码先用欧拉判别检查非零 $h_0$ 是否为二次剩余，再用 Tonelli--Shanks 求根：把 $p-1$ 分成奇数部分和二的幂，用一个二次非剩余逐次消去剩余误差的二幂阶。固定模数下，这部分只在开始执行一次；后面所有系数由多项式迭代求出。

对已归一到常数项非零的 $h$，假设当前 $q^2-h$ 的前 $m$ 项为零，取 $q_("new")=(q+h/q)/2$。直接展开可得 $q_("new")^2-h=(q^2-h)^2/(4q^2)$，分母可逆，误差次数至少翻倍。初始使用选定的常数根，每轮求逆、卷积并保留前 $2m$ 项，总复杂度为 $O(n log n)$；这里模数为奇质数，二的逆元存在。

调用后先检查 `optional` 是否有值，再使用系数。若 $t>0$，精度只确定 $q$ 的前 $n-2t$ 项，代码把答案中仍可自由选择的高次项设成零；不能据此宣称“所有根只有正负两个”。若题目指定某个非零常数根，可把当前答案整体取负以切换分支；常数项为零时还要按题目要求处理未被截断条件确定的项。

#link("https://judge.yosupo.jp/problem/sqrt_of_formal_power_series")[Library Checker · sqrt_of_formal_power_series]
