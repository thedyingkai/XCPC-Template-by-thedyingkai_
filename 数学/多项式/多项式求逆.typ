==== 用途

- `poly998::inverse(f,n)` 输入常数项非零的多项式 $f$ 和长度 $n$，返回满足 $f(x)g(x) equiv 1 mod x^n$ 的前 $n$ 项；复杂度 $O(n log n)$，也是多项式除法、对数和指数的基础。

- 输入不足 $n$ 项时自动把高次项视为 $0$；返回值固定有 $n$ 项。
- 若常数项为 $0$，应先按最低非零次数提取 $x^k$，不能直接调用求逆。
- 洛谷 P4238「多项式乘法逆」。
