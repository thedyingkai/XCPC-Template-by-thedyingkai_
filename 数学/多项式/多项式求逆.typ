`poly998::inverse(f,n)` 求唯一的 $g$，使 $f(x)g(x) equiv 1 \(mod x^n\)$，要求 `n>=0` 且常数项非零，返回恰好 $n$ 项。牛顿迭代使用 $g'=g(2-f g)$；若旧答案在模 $x^m$ 下正确，误差会平方，因此每轮把正确长度翻倍，复杂度 $O(n log n)$。

输入不足 $n$ 项时高次项视为零。常数项为零的多项式没有形式幂级数逆元；若题目允许洛朗级数，应先提出最低次的 $x^k$ 并单独处理下标，不能直接调用本接口。

常数项条件来自乘积的第零项 $f_0 g_0=1$，所以必须先取 $g_0=f_0^(-1)$。再比较第 $k$ 项，有 $f_0 g_k+sum_(i=1)^k f_i g_(k-i)=0$；已知前面的系数后，$g_k$ 被唯一确定。这也给出一个 $O(n^2)$ 的朴素算法，可用来检查小数据。快速算法把这些系数成批求出，不需要改变逆元的定义。

设已有 $f g=1-e$，且 $e$ 的前 $m$ 项全零。取新答案 $h=g(2-f g)$，就有 $f h=(1-e)(1+e)=1-e^2$。因为 $e^2$ 的前 $2m$ 项全零，正确长度从 $m$ 翻倍到 $2m$。初始只知道常数项，每轮卷积后截断到当前目标长度，直到得到前 $n$ 项。各轮长度成倍增长，总工作量与最后一轮同阶。

逆元常用于解卷积递推。若 $f_0=1$，且对 $i>=1$ 有 $f_i=sum_(j=1)^i a_j f_(i-j)$，令 $A(x)=sum_(j>=1) a_j x^j$，则 $F=1+A F$，所以 $F=(1-A)^(-1)$。实际数组先放常数项 `1`，再放 `-a[j]`，调用求逆后取所需系数。例如 $1/(1-x-x^2)$ 的前五项是 `{1,1,2,3,5}`。有额外初始贡献 $B$ 时，式子变成 $F=B/(1-A)$，再乘一次 $B$ 即可。

普通整数意义下的除法与这里不同；系数都在模 $998244353$ 的域中运算。若改为合数模数，常数项非零仍可能不可逆，现有费马逆元和 NTT 也都要重新处理。

#link("https://judge.yosupo.jp/problem/inv_of_formal_power_series")[Library Checker · inv_of_formal_power_series]
