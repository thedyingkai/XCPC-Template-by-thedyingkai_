==== 用途

- 输入形式幂级数 $f$ 和截断长度 $n$，得到 $ln f$ 或 $exp f$ 的前 $n$ 项；用于把乘法结构与加法结构互换，并继续构造多项式幂。

==== 接口与边界

- `logarithm(f,n)` 计算 $ln f(x) mod x^n$，要求 `f[0] == 1`；`exponential(f,n)` 计算 $exp f(x) mod x^n$，要求 `f[0] == 0`。
- 两者均返回恰好 $n$ 项，复杂度 $O(n log n)$，依赖多项式求逆。

==== 常用关系

- $ln f = integral(f'/f)$；`exp` 使用 Newton 迭代。赛时先检查常数项，不要靠给所有系数整体平移修补条件。
- 多项式幂可在常数项满足条件时用 $f^k=exp(k ln f)$；含前导零时先提取最低次幂并单独处理常数。
- 洛谷 P4725「多项式对数函数」、P4726「多项式指数函数」。
