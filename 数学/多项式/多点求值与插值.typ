==== 用途

- 输入多项式 $f$ 和一批任意横坐标，批量得到所有 $f(x_i)$；反过来输入互异点 $(x_i,y_i)$，恢复唯一的低于点数次数的多项式。

==== 接口与边界

- `ProductTree(xs).evaluate(f)` 在所有 `xs[i]` 处求值；`interpolate(ys)` 返回通过 `(xs[i],ys[i])` 的次数小于点数的多项式。
- 多点求值允许重复横坐标，但插值要求横坐标两两不同；全部运算模 $998244353$，复杂度 $O(n log^2 n)$。
- `modulo(a,b)` 求多项式余数，要求除式非零；乘积树对象会保存所有节点多项式，内存为 $O(n log n)$ 量级。

==== 选用

- 取值点是连续的 $0..k$ 且只询问一次时，优先用连续点拉格朗日的 $O(k)$ 板；点任意或要批量求值时再用本板。
- 插值后需要更高次数项没有唯一答案；输入点数量和目标次数必须先按题意确认。

==== 板子题

- Library Checker「Multipoint Evaluation」「Polynomial Interpolation」。
