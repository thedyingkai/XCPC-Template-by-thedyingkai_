`ProductTree(xs).evaluate(f)` 在任意一批横坐标求 $f(x_i)$；`interpolate(ys)` 从点 $(x_i,y_i)$ 恢复唯一的次数小于点数的多项式。乘积树每个节点保存该区间的 $product(x-x_i)$，向下取余即可批量求值；插值则用根多项式导数在 $x_i$ 的值作为分母，再从叶到根合并。令点数为 $n$、输入多项式系数个数为 $m$，取 $N=max(n,m)$，求值时间可按 $O(N log^2 N)$ 估计，不能只看点数；插值时间为 $O(n log^2 n)$。

求值允许重复横坐标；插值要求横坐标在模 `998244353` 意义下两两不同，否则导数分母为零，例如 `0` 和 `998244353` 不能作为两个不同的插值点。对象保存整棵乘积树，空间为 $O(n log n)$。已知连续点 `0..k` 的值且只求少数点时，可用连续点拉格朗日插值；横坐标任意或需要整批结果时使用乘积树。

直接把每个点代入一次需要 $O(n m)$。批量求值利用余式定理：$f mod (x-x_i)$ 是常数 $f(x_i)$。树上节点的多项式是其所有叶子因子的乘积，某个父节点模多项式能被子节点模多项式整除，所以先对父亲取余、再对子节点取余，与直接对子节点取余相同。根先把 $f$ 降到次数小于 $n$，然后逐层把余式传下去，到叶子便读出答案。每层多项式总长度为 $O(n)$，用快速除法控制每层代价。

插值从拉格朗日公式出发。令 $P(x)=product_i (x-x_i)$，则 $P'(x_i)=product_(j!=i)(x_i-x_j)$，需要的多项式为 $F(x)=sum_i (y_i/P'(x_i)) dot P(x)/(x-x_i)$。各项在自己的点取值为 $y_i$，在其他点都为零；总次数小于 $n$，因此也满足唯一性。先对根多项式求导并批量求值，便能一次得到全部分母。

叶子存权值 $w_i=y_i/P'(x_i)$。向上合并时，若左右区间的插值表达式为 $F_L,F_R$、因子乘积为 $P_L,P_R$，则当前结果为 $F_L P_R+F_R P_L$。左边各项原本只缺自己的因子，补乘右半全部因子后，就变成当前区间需要的表达式；右边同理。这样逐层合并，避免对每个点单独做一次高次乘法。

例如在 $x=0,1,2$ 处给出 $y=1,2,5$，`ProductTree({0,1,2}).interpolate({1,2,5})` 返回 `{1,0,1}`，即 $1+x^2$。横坐标不要求有序，`evaluate` 返回的函数值按传入点序排列；`interpolate` 返回的系数则始终按次数从低到高排列。改变点序时必须同时改变 `ys` 的顺序，这样恢复出的多项式不变。相同点集要对多个多项式求值，可以复用同一个对象，不必重建乘积树。

两个相同横坐标即使给出相同纵坐标，也不满足这个插值接口的前提；已知重复点只是冗余约束时可以先合并，而同一点要求不同纵坐标则无解。若重复点表示导数条件，问题变成埃尔米特插值，需要另写相应算法，不能只删除重复项。

#link("https://judge.yosupo.jp/problem/multipoint_evaluation")[Library Checker · multipoint_evaluation]

#link("https://judge.yosupo.jp/problem/polynomial_interpolation")[Library Checker · polynomial_interpolation]
