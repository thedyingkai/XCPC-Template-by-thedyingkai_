`pointInPolygon(q,p)` 用射线奇偶性判断点是否位于简单多边形的闭区域，边界也返回真。射线跨过顶点时采用半开区间，只让该顶点被一条相邻边计数；这避免了重复翻转。多边形可以顺时针或逆时针，但必须简单且顶点按边界顺序给出。

`segmentIntersection` 区分 `0` 不交、`1` 严格相交、`2` 共线重叠、`3` 单点相交，并显式处理退化点段；类型二的另外两个返回值是重叠段端点。`distanceSS` 有交点时为零，否则取四个端点到另一线段距离的最小值。

`segmentInPolygon` 判断整条线段是否留在多边形闭区域。它收集线段与每条边的交点参数，再检查相邻参数区间的中点；边界交点把线段切成若干段，而每个开区间内部不会再次跨越边界，所以中点位置代表整段。一次调用会产生 $O(n)$ 个候选区间，每个中点又调用 $O(n)$ 的 `pointInPolygon`，总复杂度是 $O(n^2)$；顶点很多时不能把它当成线性判定。端点必须先位于闭区域，沿边界走也允许。若题意要求严格内部，需要同时排除端点在边界、重叠边和中途接触边界，不能只把 `pointInPolygon` 的返回值取反。
