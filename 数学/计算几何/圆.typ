圆板处理非负半径的圆。`circleRelation` 依次返回 `0` 相离、`1` 外切、`2` 两交点、`3` 内切、`4` 内含、`5` 重合；所有后续接口都应先按这一语义处理无解、唯一解和无限多解。

`lineCircleIntersection` 求无限直线与圆的交点，先投影到垂足，再沿直线方向移动剩余半弦长；若题目给的是线段，结果还要用 `pointOnSegment` 过滤。`circleIntersection` 用两圆心连线上的投影长度和垂直偏移求交点，重合圆返回空数组，调用者需通过关系值区分“无交点”和“无限多交点”。

`tangentPoints(p,c)` 返回过点向圆作切线的切点，圆内点为空；`commonTangents` 返回每条公切线在两个圆上的切点。`circleIntersectionArea` 用两段扇形减三角形计算交面积，并单独处理相离和包含。零半径圆在切线语义上会退化，题目需要切线时应保证半径严格为正。圆形物体绕圆障碍运动时，可把障碍半径增加物体半径后按点运动；多圆面积不能只做两两容斥，应改用圆弧扫描或 Green 公式。
