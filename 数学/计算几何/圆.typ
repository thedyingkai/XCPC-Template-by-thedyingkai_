圆板处理非负半径的圆。`circleRelation` 依次返回 `0` 相离、`1` 外切、`2` 两交点、`3` 内切、`4` 内含、`5` 重合；所有后续接口都应先按这一语义处理无解、唯一解和无限多解。

`lineCircleIntersection` 求无限直线与圆的交点，先投影到垂足，再沿直线方向移动剩余半弦长；若题目给的是线段，结果还要用 `pointOnSegment` 过滤。`circleIntersection` 用两圆心连线上的投影长度和垂直偏移求交点，重合圆返回空数组；重合的正半径圆有无限多交点，重合的零半径圆只代表同一个点，调用者需通过关系值和半径区分这些情况与无交点。

`tangentPoints(p,c)` 返回过点向圆作切线的切点，圆内点为空；`commonTangents` 返回每条公切线在两个圆上的切点。`circleIntersectionArea` 用两段扇形减三角形计算交面积，并单独处理相离和包含。零半径圆在切线语义上会退化，题目需要切线时应保证半径严格为正。圆形物体绕圆障碍运动时，可把障碍半径增加物体半径后按点运动；多圆面积不能只做两两容斥，应改用圆弧扫描或格林公式。

圆与直线相交先求圆心到直线的垂足 $H$，距离记为 $d$。$d>r$ 无交点，$d=r$ 相切，$d<r$ 时半弦长为 $sqrt(r^2-d^2)$，从 $H$ 沿单位方向向两侧各走这一长度便得到交点。实际代码先比较平方，再开方，并把误差造成的微小负数截到零。线段题还需判断交点是否位于端点之间；整条直线有两个交点，不代表有限线段一定穿圆。

两圆交点以圆心连线为对称轴。设圆心距为 $d$，从第一圆心沿连线走 $x=(d^2+r_1^2-r_2^2)/(2d)$ 到公共弦中点，再沿垂直方向走 $sqrt(r_1^2-x^2)$ 得到两个交点。这个公式由两圆方程相减得到，必须先排除同心等特殊情况，不能在 $d=0$ 时直接代入。关系接口返回的是整圆关系，与两个圆盘是否有公共面积不同：内含时没有圆周交点，但圆盘交面积为较小圆的面积。

过圆外点 $P$ 作切线，切点 $T$ 满足半径 $O T$ 与 $P T$ 垂直。因此三角形 $O T P$ 在 $T$ 处为直角，切线段长度为 $sqrt(|O P|^2-r^2)$。代码先求两个切点中点在 $O P$ 上的位置，再加减垂直偏移；圆上点返回自身，圆内点没有实切线。若题目求避开圆盘的最短路，候选通常由直线段、切线段和圆弧组成，还要检查直线段是否穿过其他障碍，不能仅连接所有切点就结束建图。

公切线可用单位法向量 $v$ 表示。取第二圆半径的符号 $s=1$ 或 $-1$，两个切点分别为 $O_1+r_1 v$、$O_2+s r_2 v$；切点连线与法向量垂直，得到 $v dot (O_2-O_1)=r_1-s r_2$。同号给外公切线，异号给内公切线，方程是否有单位向量解由圆心距与这个半径差的绝对值决定。两圆相切时，一条公切线的两个切点可能重合，不能直接用这两个相同点构造非退化直线；可以用该切点处的半径垂线表示切线。重合的正半径圆有无穷多条公切线，而当前 `commonTangents` 返回空数组，需先判关系。

两圆相交的透镜形面积由两个弓形组成。圆心角为 $theta$ 的弓形面积是 $r^2(theta-sin theta)/2$，来自扇形面积减等腰三角形面积。角度使用弧度，`acos` 的输入要截到 `[-1,1]`。例如两个半径为一、圆心距也为一的圆，交面积为 $2pi/3-sqrt(3)/2$，可作为小样例检查。这里各接口都是常数时间；坐标误差、相切和重合的返回含义往往比复杂度更需要留意。
