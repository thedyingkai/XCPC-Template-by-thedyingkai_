点、向量和有向直线是后续几何板的共同接口。整数坐标的 `dot`、`cross` 返回 `i128`，适合朝向、共线和距离平方的精确判定；交点、单位向量和距离返回 `d128`。`pointOnLineLeft(p,l)` 表示点严格位于有向直线 `a -> b` 左侧，这一方向约定会直接传给半平面交。

`lineIntersection` 要求两条直线不平行，`normalize` 和 `distancePL` 要求方向非零；`distancePS` 允许线段退化为点。`pointOnSegment` 对整数坐标使用 `i128` 精确判断，对浮点坐标使用误差。浮点符号统一经过绝对误差 `GEOM_EPS`，但排序仍按原坐标比较；题目坐标尺度相差极大时应重新评估误差。整数点虽然叉积用 `i128`，两坐标先在类型 `T` 中相减，因此还需保证坐标差能装入 `T`。

只比较远近时优先用 `square`，可避免开方和额外误差。线段交点是浮点结果，若题目只问整数线段是否相交，应停留在叉积判定，不要反过来比较计算出的交点。
