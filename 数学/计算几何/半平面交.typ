`hp(lines)` 把每条有向直线的左侧及边界视为合法区域，只返回面积非零的有界交集多边形；空集、点、线段和无界交集均返回空数组。直线先按方向极角排序，同向平行线只保留限制更紧的一条；随后用双端队列维护边界。为排除退化交集，新直线经过队首或队尾交点时也会弹出旧边界，最终相邻边界的交点按逆时针排列。时间复杂度为 $O(n log n)$，空间为 $O(n)$。

每条直线必须有非零方向，退化判定使用 `GEOM_EPS`。需要截取无界区域时可加入边界框，但结果是原交集与边界框的交，不能据此判断原区域是否有界。边界框应覆盖题目所需的范围，过大会放大浮点误差。从凸多边形生成约束时应统一为逆时针边，使内部位于左侧；方向不确定时拿一个已知严格位于内部的点验叉积，落在右侧就交换端点。若题目允许可行集退化为点或线段，不能仅用返回数组是否为空判断有无解，需要另行处理这些情况。

半平面交适合求多个凸多边形的公共部分、二维一次不等式共同限制的区域，以及简单多边形的核。核是能看到多边形内所有点的位置集合，可以把简单多边形按逆时针每条边的左侧相交；非凸多边形本身不等于这些半平面的交，不能借此求任意非凸多边形的交集。若最终得到有界多边形，可再求面积，或在顶点中比较线性目标函数的最值。

把 $a x+b y<=c$ 转成有向直线时，先取边界上一点 $P$，再取方向 $(-b,a)$。对任意点 $X$，方向与 $X-P$ 的叉积恰为 $c-a X_x-b X_y$，所以左侧对应原来的不等式。例如 $x>=0$ 用向下的竖线，$y>=0$ 用向右的横线；与 $x+y<=1$ 相交得到顶点为 `(0,0),(1,0),(0,1)` 的三角形。若 $a=b=0$，约束要么恒真，要么直接矛盾，应在建线前处理。

按极角排序后，合法区域的边界也按这个方向顺序出现。队列中的直线是尚可能成为交集边界的约束，相邻两条直线的交点是当前候选顶点。新半平面把队尾顶点切掉时，旧的最后一条边已经无法与新边按当前顺序围成合法凸边界，弹出后继续检查；队首同理。每条直线最多入队、出队一次，因此排序以后的维护为线性。全部插入后还要用首尾约束互相清理，才能把边界闭合。

同向平行约束中只需保留更紧的一条，例如 $x>=1$ 已经包含 $x>=0$ 的要求。反向平行约束可能形成带状区域，也可能矛盾，不能直接套同向去重。本实现的整个队列过程和最后面积检查以“正面积有界交集”为目标，边界接触时也会删除冗余或退化候选；空返回值保留了前述多种含义。

求凸多边形内最大内切圆半径时，可二分半径 $r$，把每条边沿指向内部的单位法向量平移 $r$，剩余半平面交就是圆心能放置的区域。平移的是距离，必须先把法向量单位化，不能直接乘未归一化方向。最优半径处的可行域可能退化，当前接口会把它判成空；二分通常从可行侧逼近极限，但若要精确判定边界可行或输出退化圆心，就需补相应处理。

交点计算需要除以两方向的叉积，近乎平行时误差会放大；统一坐标尺度、避免无意义的大边框，比只把 `GEOM_EPS` 调小更有用。`pointOnLineLeft` 在基础板中表示严格左侧，而本题建模使用的是含边界的闭半平面，调用其他几何接口做二次筛选时要分别核对判定语义。

洛谷 P4196「半平面交」。
