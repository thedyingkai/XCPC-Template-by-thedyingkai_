==== 用途

- 输入若干有向直线表示的“左侧合法”约束，求这些半平面的公共部分；本板输出有界交集的凸多边形。

==== 接口与边界

- 依赖 `点与直线基础.cpp`；`hp(lines)` 保留每条有向直线左侧。
- 只处理交集为有界多边形的情况；无界问题先加入边界框。
- 从凸多边形建半平面时先统一为逆时针。方向不确定时用一个合法点验叉积，合法点在右侧就交换端点。

==== 板子题

- 洛谷 P4196「半平面交 / 凸多边形」。
