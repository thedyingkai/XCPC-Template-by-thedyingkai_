`hp(lines)` 把每条有向直线左侧视为合法区域，返回这些半平面的有界交集多边形。直线先按方向极角排序，同向平行线只保留限制更紧的一条；随后用双端队列维护仍可能成为边界的直线，新直线若排除队首或队尾交点，就持续弹出。最终相邻边界线的交点按环序组成凸多边形。

每条直线必须有非零方向。输入交集无界时，本接口也返回空，若题目要保留无界区域，应先加入足够大的边界框；边界框必须覆盖所有可能答案且不能大到放大浮点误差。从凸多边形生成约束时应统一为逆时针边，使内部位于左侧；方向不确定时拿一个已知合法点验叉积，落在右侧就交换端点。空集、低于二维的退化交集和无界交集在本返回类型中都没有单独编码。

洛谷 P4196「半平面交」。
