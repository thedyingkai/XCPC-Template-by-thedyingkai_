`convexHull(points,false)` 对点排序去重后维护上下凸链，返回不重复首点的逆时针凸包，并删除边上的共线中间点。每个点至多进出栈一次，所以排序后扫描为线性时间，总复杂度 $O(n log n)$。传 `keepCollinear=true` 会保留边界共线点；所有点共线时直接返回排序后的全部点。

`diameter2(hull)` 在环序凸包上用旋转卡壳求最远点对距离平方，允许保留边界共线点。它先线性删除共线中间点；所有点共线时，直接取字典序两端点的距离。固定一条边后，对踵点到该边的三角形面积先增后减，指针只需单向移动一周；更新边两端到对踵点的距离即可覆盖最远点对。时间和额外空间均为 $O(n)$。

输入必须已经是凸包环序，不重复首点且相邻点互异；`convexHull` 的两种返回结果都可以直接传入。整数点返回 `i128`。若要输出点对，在更新最大值时同步记录坐标；若需要原输入下标，去重和删除共线点时还要保留下标对应关系。多次判断点在凸多边形内时，固定一个顶点后二分扇区可做到单次 $O(log n)$。

凸包是包含所有输入点的最小凸集。求点集周长、最远点对，或最大化某个方向上的投影时，内部点不可能优于所有边界点，因此可以先删去它们；最近点对却可能完全落在内部，不能先求凸包。最远点对也能只看顶点：把一个内部点写成若干顶点的凸组合，它到固定另一点的距离不超过这些顶点距离的最大值，再对另一端同样处理即可。

单调链先按横坐标、纵坐标排序。维护下凸链时，新点和栈顶两点必须形成左转；若出现右转，栈顶被两端点连线挡在凸包内部，可以删除。叉积为零时，删不删栈顶由是否需要保留边界共线点决定。逆序再求上凸链，拼接后去掉重复端点。浮点点的排序仍用原坐标严格比较，不能把带误差的“近似小于”放进排序比较器，否则可能不满足严格弱序。

旋转卡壳可理解为夹住凸包的两条平行支撑线。固定边 `i -> i+1`，到这条边距离最大的顶点就是另一侧支撑线接触的对踵点；边长固定时比较三角形面积与比较距离等价，所以用叉积就够了。凸包边依次转动时，对踵点沿边界单向移动，不必为每条边从头找。最远点对会出现在某对支撑线的接触点中，枚举边两端与当前对踵点便覆盖候选，不能只比较每个点的相邻点。

例如矩形四点 `(0,0),(3,0),(3,4),(0,4)` 的直径平方是 `25`。若输入还包含底边中点 `(1,0)`，保留共线点的凸包仍可直接交给 `diameter2`，内部会先清理共线中间点。空集和单点返回零，两点返回它们的距离平方；所有点共线时取两端。整数坐标的叉积和平方虽返回 `i128`，点差仍先在坐标类型中计算，坐标范围和平方和都要能装入相应类型。

最小宽度也可沿边找最远对踵点，再用面积除边长，但最后要对各边宽度取最小值。最小面积外接矩形还要同时维护边方向上的最大、最小投影及垂直方向极值，需要多组单向指针；它不是把当前直径的 `max` 换成 `min` 就能得到的。只比较直径大小时一直保留距离平方，最终确实要长度才开方。

#link("https://judge.yosupo.jp/problem/static_convex_hull")[Library Checker · static_convex_hull]
