==== 用途

- 输入平面点集，得到包住全部点的逆时针凸包；再输入该凸包，可求最远点对的距离平方。

==== 接口与边界

- 依赖 `点与直线基础.cpp`；`convexHull(p, false)` 返回逆时针凸包，不重复首点，并删除边上的共线中间点。
- `keepCollinear = true` 保留边界点；所有点共线时返回排序后的全部点。
- `diameter2` 的输入必须是环序凸包，返回距离平方；整数点的返回类型为 `i128`。

==== 改板

- 需要最远点对时，在更新答案处同步记录点。
- 点在凸多边形内的多次询问：固定一个顶点二分扇区，单次 $O(log n)$。

==== 板子题

- 洛谷 P2742「二维凸包 / 圈奶牛」、P1452「Beauty Contest G」。
