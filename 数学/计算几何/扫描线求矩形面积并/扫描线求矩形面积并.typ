`RectUnion` 求轴对齐矩形并集面积。每个矩形在下边产生覆盖加一事件、上边产生减一事件；扫描线按 $y$ 排序，线段树在离散化的 $x$ 区间上维护当前至少覆盖一次的总长度。相邻事件高度差乘当前覆盖长度，就是这一横条中新增加的面积，因此重叠部分只统计一次。

构造 `RectUnion(n)` 后需填满 `b[0..2n-1]` 的事件和 `a[0..2n-1]` 的横坐标，输入应满足 `x1<x2`、`y1<y2`。坐标、覆盖长度和面积均为 `i64`；面积可能越界时要把 `w`、`sum` 和乘法中间量一起改成 `i128`。求周长并需要额外维护覆盖段数及左右端覆盖状态，并按同一高度成组处理事件，不能只替换面积累加式。

#link("https://judge.yosupo.jp/problem/area_of_union_of_rectangles")[Library Checker · area_of_union_of_rectangles]
