==== 用途

- 输入若干轴对齐矩形，求至少被一个矩形覆盖的总面积；扫描线输出的是并集面积，重叠区域只计算一次。

==== 接口与改板

- `RectUnion(n)` 需要填满 `b[0..2n-1]` 和 `a[0..2n-1]`；输入应满足 `x1 < x2`、`y1 < y2`。
- 坐标、覆盖长度和面积均为 `i64`；面积可能溢出时把 `w`、`sum` 及乘法中间量改为 `i128`。
- 求矩形周长并时需增加覆盖段数、左右端覆盖标记，并合并处理同一高度的事件，不能只改面积累加式。

==== 板子题

- 洛谷 P5490「扫描线 & 矩形面积并」。
