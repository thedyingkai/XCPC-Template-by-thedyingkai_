`Prime::sieve(n)` 在线性时间内枚举不超过 $n$ 的质数。`Sieve::init(n)` 还同时得到每个数的欧拉函数 `phi`、莫比乌斯函数 `mu`、约数个数 `d` 和约数和 `sigma`。核心不变量是每个合数只在“当前数乘其最小质因子”处被生成一次；遇到 `i % p == 0` 后必须 `break`，这既保证线性复杂度，也决定积性函数在质因子次数增加与加入新质因子时分别使用哪条递推。

数组均包含下标 $n$。`sigma` 保存精确 `i64`，范围更大或需要取模时，必须连同当前质数幂 `p_power`、几何和 `p_sum` 一起修改。需要最小质因子时，在质数分支记 `minp[i]=i`，生成 `i*p` 时记 `minp[i*p]=p`。若后续既需单点函数又需前缀和，应另开前缀数组，不能原地覆盖。

本板需要 $O(n)$ 个整数的内存，不能直接拿来筛到 $5 times 10^8$。这种范围只用本板筛到 $sqrt(n)$ 得到基质数，再对原区间分段标记；不要把分段筛通过的结果写成 `Prime::sieve(n)` 本身能通过。
