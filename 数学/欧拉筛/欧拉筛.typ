==== 用途

- 输入上界 $n$，在线性时间内得到 `1..n` 的质数，以及每个数的 $phi$、$mu$、约数个数和约数和；取前缀后可供整除分块、莫反和筛法查询。

==== 接口与改板

- `Prime::sieve(n)` 只筛质数；`Sieve::init(n)` 同时得到 `phi`、约数个数 `d`、约数和 `sigma`、莫比乌斯函数 `mu`，均包含下标 $n$。
- `sigma` 没有取模；按题目模数修改时要同步处理 `p_power`、`p_sum`。
- 需要最小质因子时，质数分支令 `minp[i] = i`，生成 `i * p` 时令 `minp[i * p] = p`。
- 把 `phi` 或 `mu` 改成前缀和后，不能再把原数组当单点函数值使用。

==== 板子题

- 洛谷 P3383「线性筛素数」、P2158「仪仗队」。
