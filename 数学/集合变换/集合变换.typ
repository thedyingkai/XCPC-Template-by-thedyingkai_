把集合表示为二进制位掩码，OR、AND、XOR 卷积分别求 `c[S]=sum(a[A]*b[B])`，求和条件依次为 `A|B=S`、`A&B=S`、`A^B=S`。它们用于统计两个状态按位合并后得到每个结果的加权方案数。

建模时先弄清每一位代表什么。若一位表示某种能力是否具备，两个人合起来拥有的能力是 OR，共同具备的能力是 AND；若一位表示某个开关被操作次数的奇偶性，合并两段操作后的状态是 XOR。把同一掩码的对象数量放到 `a[mask]`、`b[mask]`，卷积便能一次得到所有结果掩码的配对数。这里第一份对象来自 `a`、第二份来自 `b`，计算的是有序的两侧选择；令 `a=b` 不会自动排除同一对象或把交换两侧视为一种方案。

`subsetZeta(a)` 把 `a[S]` 变为所有子集的和，第二参数为 `true` 时做子集莫比乌斯逆变换；`supersetZeta` 对所有超集做同样操作。变换逐位累加，长度为 $2^k$ 时复杂度为 $O(k 2^k)$，模数固定为 $998244353$。OR 卷积先对子集求和，AND 卷积先对超集求和，逐点相乘后再逆变换；XOR 卷积使用沃尔什—哈达玛变换，逆变换每层乘 $2^(-1)$。

子集求和需要保证每个子集只算一次。处理某一位 `b` 前，已经处理的位允许从 `S` 中删去，尚未处理的位仍必须与 `S` 相同。若 `S` 含有第 `b` 位，原来的 `a[S]` 表示保留这一位的情况，`a[S^(1<<b)]` 表示不保留这一位的情况，两类互不相交，相加就把这一位也放开了。本轮只有含这一位的状态被修改，来源状态没有这一位，不会在同一轮被反复累加。所有位处理完，恰好遍历了 `S` 的每一种子集选择。超集求和对称地允许补上原本没有的位。

把单个位上的“相加”改成“相减”，就能撤销这一位的影响；各位操作可以交换顺序，所以本实现的逆变换仍按同样位序处理。不能改成对每个 `S` 随意遍历它的直接子集并累加，那样一个更小的子集可能沿多条路径重复贡献。子集和本身也能直接用于询问，例如 `a[T]` 是恰好具有能力集合 `T` 的对象数，变换后 `a[S]` 就是能力不超出 `S` 的对象数；要求对象至少含有所需能力 `S`，则取超集和。

OR 卷积为何能化成逐点乘，可以对答案再求一次子集和。并集包含于 `S`，等价于左右两集合分别包含于 `S`，于是

$ sum_(T subset.eq S)c[T] = sum_(A subset.eq S,B subset.eq S)a[A]b[B] = (sum_(A subset.eq S)a[A])(sum_(B subset.eq S)b[B]). $

右侧就是两个子集和数组的同位置乘积，逆变换后得到并集恰好等于 `S` 的答案。AND 卷积改看超集和：交集包含 `S`，等价于 `A`、`B` 都包含 `S`，同样拆成两个独立求和的乘积，再逆变换取出交集恰好等于 `S` 的部分。这里允许 `A`、`B` 相交，OR 卷积不能替代要求不相交的子集卷积。

XOR 的每一位只有两个状态。对一对数 `(x,y)` 做 `(x+y,x-y)`，第一项不区分该位取值，第二项给该位为 `1` 的状态加负号；逐位做完后，第 `K` 项就是所有 `a[S]` 按符号 `(-1)^popcount(K&S)` 加权的和。这个符号满足 `sign(K,A^B)=sign(K,A)*sign(K,B)`，所以 XOR 卷积的变换也等于两份变换的逐点乘积。

再做一次同样的 XOR 变换时，两个掩码相同，对所有 `K` 的符号乘积都为 `1`，总和为 `2^k`；两个掩码不同时，任选它们不同的一位，把 `K` 与翻转该位后的掩码配对，两项正负相消，总和为零。这说明两次变换得到原数组的 `2^k` 倍。单个位的蝴蝶做两次也直接得到 `(2x,2y)`，因此逆变换每层乘 `inv2`，总共正好除以 `2^k`。

可以用一位的小例子核对三种运算。取 `a=[1,2]`、`b=[3,4]`，OR 结果为 `[3,18]`，AND 为 `[13,8]`，XOR 为 `[11,10]`。例如 OR 的结果 `1` 来自 `(0,1)`、`(1,0)`、`(1,1)`，权重为 `1*4+2*3+2*4=18`；XOR 的结果 `0` 来自 `(0,0)` 与 `(1,1)`，权重为 `1*3+2*4=11`。对应 XOR 变换分别为 `[3,-1]`、`[7,-1]`，相乘得 `[21,1]`，逆变换就是 `[11,10]`；代码中的负数均保存为模意义下的非负代表。

三个卷积入口都会复制参数，不改变调用方的数组；三个变换入口则原地修改。输入长度必须相同且为正的二次幂，覆盖全部可能掩码，不够时先补零。长度 `1` 合法，只有空集合状态；`mask=0` 表示集合为空，不代表它的权重必须为零。时间为 $O(k 2^k)$，卷积占 $O(2^k)$ 额外空间，原地变换只用常数辅助空间。

OR、AND 的逆变换只有加减法，不要求模数是素数；XOR 逆变换还要求 `2` 可逆，不能直接在偶数模数下使用当前写法。改模数时同时检查乘法是否需要 `i128`，只改常量可能使逐点乘积溢出。若结果只是方案数模某数，本板已经满足；需要精确整数答案时，不能把模意义下的结果直接当成原计数。

数论中的莫比乌斯反演按整除关系求和，这里则按子集包含关系求和。只枚举一个集合的全部非空子集可写 `for(T=S;T;T=(T-1)&S)`，所有 `S` 合计为 $O(3^k)$。用它计算子集卷积时，还要计入空子集 `T=0` 对应的 `a[0]*b[S]`，`S=0` 时也不能漏掉。

#link("https://judge.yosupo.jp/problem/bitwise_and_convolution")[Library Checker · bitwise_and_convolution]

#link("https://judge.yosupo.jp/problem/bitwise_xor_convolution")[Library Checker · bitwise_xor_convolution]
