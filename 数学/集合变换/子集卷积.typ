`SubsetConvolution::multiply(a,b)` 计算 $c[S]=sum_(A union B=S,A inter B=emptyset)a[A]b[B]$，即把每个集合拆成两个不交部分的加权和。算法按集合大小分层求子集和，逐层卷积后再做莫比乌斯反演；只有 $|A|+|B|=|S|$ 的项留在目标层，配合 $A union B=S$ 就能排除交叠。数组长度为 $2^k$ 时，时间复杂度为 $O(k^2 2^k)$，空间为 $O(k 2^k)$。

输入长度必须相同且为二的幂。若允许 $A,B$ 相交，问题变成 OR 卷积，不能继续使用这一定义；$k$ 较小时直接枚举 `A` 的 $O(3^k)$ 写法通常更省代码。

它适合把一批元素分给两个有区别的部分。若 `a[A]` 表示第一部分恰好使用集合 `A` 的方案数，`b[B]` 表示第二部分的方案数，而且每个元素必须恰好使用一次，那么 `c[S]` 就是把 `S` 分配完的方案数。对固定 `S`，等价写法是 `sum(a[A]*b[S^A])`，其中 `A` 枚举 `S` 的全部子集。允许空部分时必须保留 `a[0]`、`b[0]`；要求两部分都非空，则将它们设为零。两部分不区分时，还要按题目处理交换后的重复，当前接口不自动计算无序划分。

普通 OR 卷积只保证并集等于 `S`，会把一个元素同时分给两边。按集合大小分层，可以记住这次配对总共用了多少次元素。初始只把 `a[S]` 放在第 `popcount(S)` 层，其余层为零；对子集求和后，第 `i` 层在位置 `S` 的值为

$ A_i[S]=sum_(T subset.eq S,|T|=i)a[T], quad B_i[S]=sum_(T subset.eq S,|T|=i)b[T]. $

对每个固定的 `S`，把层数当成多项式次数，令 `C_t[S]=sum(A_i[S]*B_(t-i)[S])`。此时统计的是 `A,B` 都包含于 `S` 且 `|A|+|B|=t` 的所有配对，仍可能缺少某些元素，也可能两边重复使用某些元素。逐层做子集莫比乌斯逆变换，才把“并集包含于 `S`”还原成“并集恰好为 `S`”。之后 `C_t[S]` 只保留并集为 `S`、两边大小之和为 `t` 的贡献。

最后取 `t=popcount(S)`。因为

$ |A|+|B|=|A union B|+|A inter B|, $

已经保证 `A union B=S`，左边再等于 `|S|`，就只能有交集大小为零。分层负责区分重复使用次数，逆变换负责剔除没有覆盖全 `S` 的项，两步缺一不可。例如目标 `S={0,1}`，取 `A=B={0}` 时也有 `|A|+|B|=2=|S|`，但元素 `1` 没被使用、元素 `0` 用了两次；若只按大小筛选而不做逆变换，这一项就会被误计入答案。

以掩码 `0,1,2,3` 分别表示空集、`{0}`、`{1}`、`{0,1}`，取 `a=[1,2,3,4]`、`b=[5,6,7,8]`。`S=3` 的四种合法拆分是 `(0,3)`、`(1,2)`、`(2,1)`、`(3,0)`，所以 `c[3]=1*8+2*7+3*6+4*5=60`，完整答案为 `[5,16,22,60]`。重叠配对 `(1,1)` 的权重 `2*6=12` 在变换中出现后，逆变换会把它归到并集掩码 `1` 的第 `2` 层；这个掩码只取第 `1` 层，因此不会进入任何合法拆分答案。

实现中的二维数组第一维是集合大小，第二维才是集合掩码，不能把层数与某一位是否存在混淆。`A`、`B` 各有 `k+1` 层，每层一次 $O(k 2^k)$ 的子集和；每个掩码再做 $O(k^2)$ 的层间乘法，最后逆变换，所以总时间为 $O(k^2 2^k)$。只保留 `0..k` 层即可，次数更高的项不可能在最后被选中。

空间常数较大：当前同时保存 `A`、`B`、`C` 三个 `i64` 二维数组，主体占约 `24*(k+1)*2^k` 字节。`k=20` 时这部分已经是 `504 MiB`，还未计入输入、返回值及容器开销，不能只看渐进复杂度就按普通状压数组的内存估算。输入必须非空；长度 `1` 对应没有元素，返回值就是 `a[0]*b[0]` 取模。

模数沿用 `SetTransform::MOD=998244353`，允许负权输入，内部会先归一化。多个带标签部分可以逐次做子集卷积，使已经使用的元素集合与下一部分不交；但若转移是在所有拆分中取最小代价，不能把加法机械换成 `min`，因为莫比乌斯逆变换依赖加减抵消，最小值没有对应的减法逆操作。直接枚举子集虽然较慢，却能处理这种最小值转移，两份算法的适用运算不同。

#link("https://judge.yosupo.jp/problem/subset_convolution")[Library Checker · subset_convolution]
