`exEulerPow(a,b,m)` 在 `b` 是任意长非负十进制字符串时求 $a^b mod m$，底数与模数不要求互质，要求 `m>0`。`depow` 同时算出 $b mod phi(m)$ 并记录原指数是否已经不小于 $phi(m)$；小指数保留原值，大指数使用余数加一个 $phi(m)$，这正是非互质情形的扩展欧拉降幂条件。尤其是 $b=0$ 时不能无条件加周期。

已知 $gcd(a,m)=1$ 时只需把指数模 $phi(m)$；当前 `phi` 用试除分解，单次复杂度 $O(sqrt(m))$。多次询问同一模数应把 `phi(m)` 移到外部缓存，模数很大且需要分解时可接 Pollard--Rho。十进制指数必须非空且只含数字。

洛谷 P5091「扩展欧拉定理」。
