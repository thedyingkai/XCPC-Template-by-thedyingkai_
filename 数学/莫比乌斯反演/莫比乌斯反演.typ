狄利克雷卷积定义为 $(f ast g)(n)=sum_(d mid n)f(d)g(n/d)$。因为 $1 ast mu=epsilon$、$phi ast 1=id$，整除和 $F(n)=sum_(d mid n)f(d)$ 可以反演成 $f(n)=sum_(d mid n)mu(d)F(n/d)$；有限范围的“枚举倍数”形式也同理，只是偏序方向相反。

这里的 $1(n)=1$、$id(n)=n$，$epsilon(n)$ 仅在 $n=1$ 时为一。莫比乌斯函数满足：$mu(1)=1$；含平方质因子的数取零；其余数若有 $r$ 个不同质因子，则取 $(-1)^r$。因此 $sum_(d mid n)mu(d)=product_(p mid n)(1-1)$，在 $n>1$ 时为零。这条消去恒等式就是反演的依据：把所有“被某个 $d$ 整除”的较宽条件加权后，恰好只留下所需的精确条件。

例如 $F(n)$ 统计某个参数是 $n$ 的约数的对象数，而 $f(n)$ 统计参数恰为 $n$ 的对象数。直接统计前者容易时，就先求 $F$，再按反演恢复 $f$。另一种常见方向是 $F(d)=sum_(d mid v, v<=N)f(v)$，表示参数为 $d$ 的倍数，此时
$f(d)=sum_(k=1)^(floor(N/d)) mu(k)F(d k)$。
除数和倍数两种式子不能混用；先写清楚一个对象究竟计入了哪些 $F(d)$。

处理 $gcd$ 二维计数时，先把权值写成 $f=g ast mu$，则
$sum_(i<=n,j<=m)g(gcd(i,j))=sum_(d<=min(n,m))f(d)floor(n/d)floor(m/d)$。
互质计数对应 $g=[n=1]$，所以权值是 `mu`；$gcd$ 和对应权值 `phi`。右式再用整除分块和 `f` 的前缀和计算。统计 $gcd(i,j)=k$ 时先把两端同时除以 $k$，矩形区间则做四次二维前缀容斥。

上式中 $g(t)=sum_(d mid t)f(d)$，所以对每一对 $(i,j)$，把 $g(gcd(i,j))$ 换成枚举它们共同约数的和。固定 $d$ 后，$i$ 有 $floor(n/d)$ 种取法，$j$ 有 $floor(m/d)$ 种取法，这才得到两个整除商的乘积。互质计数可直接从
$[gcd(i,j)=1]=sum_(d mid i, d mid j)mu(d)$
开始推导，不必另猜反演后的系数。

计算时令 $q_n=floor(n/l)$、$q_m=floor(m/l)$，共同右端点为 $r=min(floor(n/q_n),floor(m/q_m))$，本段贡献是 $(F(r)-F(l-1))q_n q_m$，其中 $F$ 是反演后权值 $f$ 的前缀和。若求 $gcd$ 的平方和，应先算 $f(t)=sum_(d mid t)mu(d)(t/d)^2$，不能仍用 `phi`。更多维的互质元组将两个整除商换成多个整除商的乘积；若每个数带权，固定 $d$ 后改为相应的倍数权值和。

也可以直接做精确最大公约数分布。先求 $C(d)$，表示所有数都被 $d$ 整除的方案数；从大到小计算 $E(d)=C(d)-sum_(k>=2)E(k d)$，就得到最大公约数恰为 $d$ 的方案数。这个倒序减倍数过程是同一反演的另一种实现，适合值域可枚举、只需一次求出所有 $E(d)$ 的题目。数组中无序选两个不同位置时用 $C(d)=binom(c_d,2)$；有序且允许重复选位置时才是 $c_d^2$，其中 $c_d$ 为被 $d$ 整除的元素个数。

当前欧拉筛已经给出 `mu`、`phi`，整除分块文件给出通用主循环，不必再复制一份代码。一般权值卷积可在 $O(N log N)$ 内枚举除数和倍数；乘积与带权和可能越过 `i64`，应按范围换 `i128`。洛谷 P2522、P3455 都要求把互质条件落到这条公式。
