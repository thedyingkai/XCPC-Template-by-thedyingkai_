==== 正整数有序拆分

这份代码先令 $n=x^x mod 1000$，再求把 $n$ 写成 $k$ 个正整数之和的有序方案数。放置 $k-1$ 个隔板后，答案是 $C(n-1,k-1)$；因此 `n == 0`、`k <= 0` 或 `k > n` 时答案为零。若每一项允许为零，模型应改成 $C(n+k-1,k-1)$，不能沿用当前数组下标。

组合数可能远超内置整数范围，代码用 `Big` 保存杨辉三角递推表。这里 $n<1000$，时间和空间为 $O(n k)$；只需要一个组合数且范围更大时，可改用一维递推或大整数的乘除公式，省去二维表的空间。输入语义要求 `x >= 0`，因为普通快速幂接口只接受非负指数。
