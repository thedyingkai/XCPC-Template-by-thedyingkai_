==== 接口与边界

- `Big` 支持带符号比较、十进制输入输出及 `+ - * / %`；除法向零取整，余数与被除数同号。
- 同时需要商和余数时调用 `divmod(a, b)`。`Big * Big` 和 `Big / Big` 均为二次复杂度；超长乘法改用 FFT/NTT。
- `神秘题目.cpp` 求 $n=x^x mod 1000$ 后输出 $C_(n-1)^(k-1)$，只适用于 $k$ 个正整数的有序拆分；非负拆分改为 $C_(n+k-1)^(k-1)$。

==== 板子题

- 洛谷 P1601「高精度加法」、P1303「A*B Problem」、P1480「A/B Problem」。
