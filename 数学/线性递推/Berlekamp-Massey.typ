`berlekampMassey(sequence)` 在质数模数下，从一段序列前缀求与它相容的最短线性递推，返回的 `c` 满足 $a_n=sum_(i=1)^k c_(i-1)a_(n-i)$。处理到每个新位置时，若当前递推预测的差值非零，就用上一次使线性复杂度增长的递推乘一个适当系数和位移来消掉该差值；算法始终保持已经扫描的前缀全部满足当前关系。

适合“前若干项容易算，目标下标很大”的题目，前提是能证明答案序列满足固定系数线性递推。例如一个固定的有限状态线性转移，每一步均乘同一个 $D$ 阶矩阵，其任一固定线性组合形成的序列都满足阶数至多 $D$ 的递推。可以先按原 DP 算前缀，再恢复递推，避免直接做大矩阵快速幂；转移随时间任意变化、带 `min/max` 或其他非线性操作时，没有这个保证。

内部保存的关系写成 $a_n+C_1 a_(n-1)+...+C_L a_(n-L)=0$，所以输出要把 `C[1..L]` 取负。扫描到第 $n$ 项时，左式的值就是 `delta`；为零表示现有关系仍然成立，为非零表示需要修正。`B` 保存上一次阶数增长前的旧关系，`last` 是它当时留下的非零差值，`shift` 是从那个位置移到当前项所需的位移。作
$C(x)<-C(x)-(delta/"last")x^"shift" B(x)$
即可抵消当前差值；所保存的历史关系与位移保证修正项在此前需要满足的位置上为零。

若旧阶数 $L$ 满足 $2L<=n$，这次失败说明原阶数不够，新阶数变为 $n+1-L$，并保存修正前的关系供后续使用；否则阶数保持不变。这里的阶数描述从哪一项开始施加递推，不能只靠删掉系数末尾的零来缩短初值段。处理长度为 $N$ 的样本，求递推的时间为 $O(N^2)$、空间为 $O(N)$；这些量与后续求远项的复杂度分开计算。

`nth(initial,c,n)` 再用多项式取模于 $O(k^2 log n)$ 求第 $n$ 项，`n` 从零开始，初值至少提供前 $k$ 项。模数必须是质数，因为修正系数需要非零差值的逆元；全零序列返回空递推。有限前缀只能保证“与样本相容”，通常至少提供真实阶数两倍以上的连续项，并拿额外项复核。已知递推时直接调用 `nth`；阶数很大时改用 Bostan--Mori。

`nth` 使用特征多项式 $R(x)=x^k-c_0x^(k-1)-...-c_(k-1)$。递推允许把 $x^k$ 换成右侧低次项，因此二进制快速幂求 $x^n mod R(x)=sum_(i=0)^(k-1)b_i x^i$ 后，答案就是 $sum b_i a_i$。例如斐波那契数列的 `initial={0,1}`、`c={1,1}`，关系 $x^2=x+1$ 对应 $a_(t+2)=a_(t+1)+a_t$，`nth(...,5)` 得到 $5$。

若已证明真实阶数不超过 $D$，连续的前 $2D$ 项足以确定一个生成后续序列的最短关系；没有阶数上界时，多取样并验证只能增加可信度，不能证明未来项一定服从猜出的式子。前缀和序列可以另行恢复递推，或从原生成函数增加一个因子 $1/(1-x)$；更换模数后要重新求递推，不能把某个模数下的系数直接搬过去。向 `nth` 多传初值时，代码会对已提供下标直接返回该初值，这些额外项仍应与递推一致。

#link("https://judge.yosupo.jp/problem/find_linear_recurrence")[Library Checker · find_linear_recurrence]
