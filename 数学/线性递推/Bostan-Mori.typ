==== 从递推到目标项

`bostanMori(P,Q,n)` 求形式幂级数 $P(x)/Q(x)$ 的第 $n$ 项，只要求 `Q[0] != 0`，分子次数可以不小于分母次数。分子分母同时乘 $Q(-x)$ 后，分母只剩偶次项，分子按 $n$ 的奇偶保留对应次数；下标每轮减半，目标系数不变。令 $d=max(deg P,deg Q)+1$，复杂度为 $O(d log d log(n+1))$；分母为非零常数时，这个过程同样适用。

这份算法用于已知有理生成函数、只求极大下标的一项；它每轮保留完整的短分子和分母，并不生成前 $n$ 项。`P[i]`、`Q[i]` 都是 $x^i$ 的系数。$Q(0)!=0$ 保证分母可逆，因而 $P/Q$ 在零点附近对应唯一形式幂级数；这里是系数运算，不需要讨论实数代入时是否收敛。

令 $U(x)=P(x)Q(-x)$、$V(x)=Q(x)Q(-x)$。因为 $V(-x)=V(x)$，可写成 $V(x)=W(x^2)$；再把分子分为 $U(x)=U_0(x^2)+x U_1(x^2)$。于是
$P(x)/Q(x)=U_0(x^2)/W(x^2)+x U_1(x^2)/W(x^2)$。
若 $n=2r$，目标系数等于 $[x^r]U_0(x)/W(x)$；若 $n=2r+1$，则等于 $[x^r]U_1(x)/W(x)$。代码按 `n&1` 抽取分子、按偶数下标抽取分母，再令 `n>>=1`，正是这两个等式。到 $n=0$ 时只需返回常数项之比。分子次数较高时也适用，只是开始几轮要处理它的实际长度。

已知递推 $a_t=sum_(i=1)^k c_(i-1)a_(t-i)$ 时，`recurrenceNth(initial,c,n)` 构造 $Q=1-c_0x-...-c_(k-1)x^k$，并用前 $k$ 项确定低次分子。初值至少给 $k$ 项，系数模数固定为 `998244353`，下标从零开始；递推系数可以含末尾零，也可以全部为零。未知递推可先用 Berlekamp--Massey 从足够长的前缀恢复；含常数或低次多项式的非齐次递推，应扩状态或先推导相应生成函数。

具体地，令 $A(x)=sum_(t>=0)a_t x^t$，则 $[x^t]A Q=a_t-sum_(i=1)^k c_(i-1)a_(t-i)$，负下标项视为零。当 $t>=k$ 时它由递推变为零，因此只需计算
$P=(Q(x) sum_(t=0)^(k-1)a_t x^t) mod x^k$。
注意分子通常不是初值数组本身。斐波那契数列从 $0,1$ 开始时，$Q=1-x-x^2$、$P=x$；从 $1,1$ 开始时，同一递推的分子改为 $1$。

若题目求前缀和，第 $n$ 项就是 $[x^n]P/(Q(1-x))$；常数非齐次项会贡献形如 $c x^k/(1-x)$ 的尾部生成函数，与低次初值项合并后再通分。实际接板时可直接构造新 `P,Q`，不必先猜一个更高阶递推。`recurrenceNth` 要求非空系数数组，若 BM 返回空递推，应按已知初值与全零后续单独处理；两份板的下标类型也不同，此处支持 `u64`。

当前卷积依赖 `poly998`。修改模数需要连同卷积和求逆实现一起修改，不能只改最后的取模常量；每轮乘积的补齐长度仍受底层 NTT 上限约束。若只求不大的连续区间，顺推的 $O(k n)$ 可能更省事；对每个不同下标单独调用本板会重复完整的折半过程。

#link("https://judge.yosupo.jp/problem/kth_term_of_linearly_recurrent_sequence")[Library Checker · kth_term_of_linearly_recurrent_sequence]
