卢卡斯定理把质数模数 $p$ 下的大组合数按 $p$ 进制逐位拆开：$C(n,k) equiv product_i C(n_i,k_i) \(mod p\)$。它适合 $n$ 远大于预处理范围、但 $p$ 足够小或每位组合数仍可承受的问题；模数必须是质数。

先调用 `lucas.init(p)` 预处理 `0..p-1`，随后用同一个 `p` 调用 `lucas.C(n,k,p)`。完整预处理后每次查询为 $O(log_p n)$。若 $p$ 很大而询问实际数位很小，可调用 `init(p,L)` 只预处理到 $L$；遇到大于 $L$ 的数位时，当前实现会退化为 $O(min(k_i,n_i-k_i))$ 的乘积公式，答案仍正确但可能超时。多询问时应先统计数位上界，不能把退化路径当作大规模主算法。

`k<0`、`n<0` 或 `k>n` 返回零。合数模数不能套卢卡斯定理，应分解为质数幂后分别计算，再用中国剩余定理合并。

洛谷 P3807「卢卡斯定理」可直接检验跨数位情形。
