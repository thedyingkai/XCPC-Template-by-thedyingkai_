==== 用途

- 输入很大的 $n,m$ 和较小质数 $p$，把它们按 $p$ 进制拆位后求 $C(n,m) mod p$；输出不需要预处理到 $n$。

Lucas 定理要求 $p$ 为质数。先调用 `lucas.init(p)`，再用相同的 $p$ 调用 `lucas.C(n,m,p)`。

- 当前预处理为 $O(p log p)$、空间 $O(p)$；$p$ 很大时不能直接套用。
- 合数模数需改用扩展 Lucas 或按质因子分解处理。

==== 板子题

- 洛谷 P3807「Lucas 定理」。
