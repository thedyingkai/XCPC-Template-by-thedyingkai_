整除分块利用 $floor(n/i)$ 只取 $O(sqrt n)$ 个不同值。当前左端点为 `l` 时，相同商一直延续到 `r=n/(n/l)`；同时出现 $floor(n/i)$、$floor(m/i)$ 时，右端取各自右端点的最小值。配合权值前缀和，可把 $sum_d f(d)floor(n/d)floor(m/d)$ 的一整块在 $O(1)$ 合并。

代码中的 `PRE()` 故意留空，使用前必须令 `pre[i]=sum_(d=1)^i f(d)`。互质数对取 $f=mu$，$gcd$ 和取 $f=phi$；一般 $sum g(gcd(i,j))$ 先求 $f=g ast mu$。这些权值来源见莫比乌斯反演，不能见到两个下取整就随意套 `mu`。乘积可能越过 `i64` 时，把答案和每次乘法的中间量一起改成 `i128`。

常见变形有 $n mod i=n-floor(n/i)i$，可把余数和拆成等差和与整除项；二维区间计数用四次前缀容斥；统计 $gcd(i,j)=k$ 时先把两个上界都除以 $k$，再统计互质对。洛谷 P2261、P1403 可分别练余数和与约数统计。
