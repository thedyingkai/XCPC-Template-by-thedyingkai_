==== 使用前必改

- `PRE()` 是空函数；必须先令 `pre[i]=sum_(d=1)^i f(d)`。当前主循环计算 $sum f(d) floor(n/d) floor(m/d)$。
- 一个整除项的块右端点为 `n / (n / l)`；有多个整除项时取各自右端点的最小值。
- 乘积可能超过 `i64` 时把答案和乘法中间量改为 `i128`。

==== 常用变形

- $n mod i=n-floor(n/i)i$，余数和可拆为等差和减整除分块。

==== 板子题

- 洛谷 P2261「余数求和」、P1403「约数研究」。
