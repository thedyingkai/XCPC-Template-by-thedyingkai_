==== 用途

- 输入含 $floor(n/i)$、$floor(m/i)$ 的求和，把商相同的一整段下标合并处理；配合权值前缀和可求余数和、约数统计以及 $gcd$/互质二维计数。
- 当前板输出 $sum_d f(d)floor(n/d)floor(m/d)$，题目只含一个整除项时删去另一项即可。

==== 使用前必改

- `PRE()` 是空函数；必须先令 `pre[i]=sum_(d=1)^i f(d)`。
- 统计互质数对时取 $f=mu$；求 $sum gcd(i,j)$ 时取 $f=phi$；一般 $sum g(gcd(i,j))$ 先令 $f=g ast mu$。推式见“莫比乌斯反演”。
- 一个整除项的块右端点为 `n / (n / l)`；有多个整除项时取各自右端点的最小值。
- 乘积可能超过 `i64` 时把答案和乘法中间量改为 `i128`。

==== 常用变形

- $n mod i=n-floor(n/i)i$，余数和可拆为等差和减整除分块。
- $sum_(i=1)^n floor(n/i)=sum_(d=1)^n tau(d)$，也等于满足 $x y<=n$ 的正整数有序数对数。
- 区间 $[a,b] times [c,d]$ 的二元计数用 $F(b,d)-F(a-1,d)-F(b,c-1)+F(a-1,c-1)$。
- 统计 $gcd(i,j)=k$ 时先把两个上界分别除以 $k$，再套互质数对公式。

==== 板子题

- 洛谷 P2261「余数求和」、P1403「约数研究」。
