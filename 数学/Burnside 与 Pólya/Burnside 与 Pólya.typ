==== 用途

- 输入物体位置上的对称变换及每种变换允许的染色，求旋转、翻转等价意义下本质不同的方案数；输出是群作用的轨道数。

==== Burnside 引理

- 有限群 $G$ 作用在状态集合上时，记 $F_g$ 为被变换 $g$ 固定的状态数，本质不同状态数为 $1/|G| sum_(g in G) F_g$。必须枚举的是题目允许的全部对称变换，不能只凭图形直觉删去某些变换。
- 若位置置换 $g$ 有 $c(g)$ 个环、每个位置有 $m$ 种颜色，则被 $g$ 固定的染色数为 $m^(c(g))$。

==== Pólya 计数

- 记 $c_j(g)$ 为 $g$ 中长度为 $j$ 的置换环数，循环指标为 $1/|G| sum_(g in G) product_j a_j^(c_j(g))$。不限制各颜色数量时代入 $a_j=m$。
- 限制每种颜色使用次数时，代入 $a_j=sum_c x_c^j$ 后提取目标单项式系数；只代入颜色数会丢失数量限制。
- 长度 $n$、$m$ 种颜色的旋转项之和为 $sum_(d mid n) phi(d)m^(n/d)$，项链数再除以 $n$。
- 手链还需加入反射：$n$ 为奇数时反射项之和为 $n m^((n+1)/2)$；$n$ 为偶数时为 $n/2 (m^(n/2+1)+m^(n/2))$，最后与旋转项一起除以 $2n$。

==== 边界与 trick

- 模意义下除以 $|G|$ 需要它与模数互质；否则不能直接乘逆元，应先在整数中完成可整除的求和，或按题目范围使用多模数与 CRT。
- 有障碍、固定点或颜色限制时，逐个变换计算其环是否与限制兼容；不能继续直接使用 $m^(c(g))$。
- 只有旋转等价用循环群，旋转和翻转都等价才用二面体群。

==== 板子题

- 洛谷 P4980「Pólya 定理」。
