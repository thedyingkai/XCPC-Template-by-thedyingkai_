普通生成函数把序列编码为 $A(x)=sum_(n>=0) a_n x^n$，乘积系数就是把规模拆成两部分的卷积；前缀和对应 $A(x)/(1-x)$，乘 $x^k$ 表示下标平移，$n a_n$ 对应 $x A'(x)$。常用展开有 $1/(1-x)^k=sum_n C(n+k-1,k-1) x^n$ 和 $-ln(1-x)=sum_(n>=1) x^n/n$。

使用前先规定“指数表示什么，系数统计什么”。若指数表示总重量，那么一种重量为 $w$ 的物品，最多选一次对应 $1+x^w$，可选任意次对应 $1/(1-x^w)$，最多选 $c$ 次对应 $(1-x^((c+1)w))/(1-x^w)$。独立决定每种物品的数量时相乘，目标重量 $N$ 的方案数就是 $[x^N]$。例如重量为一、二的两种物品各可任意选，生成函数为 $1/((1-x)(1-x^2))$，$x^3$ 的系数是二，分别对应 $1+1+1$ 和 $1+2$。

乘法为什么出现卷积：从 $A$ 选出一项 $a_i x^i$，从 $B$ 选出一项 $b_j x^j$，组合后的规模为 $i+j$、方案数为 $a_i b_j$。若只是从两类互不重叠的方案中任选一类，则应相加；同一方案能由多种拆分产生时，必须确认这些拆分是否本来就应视为不同方案，避免在乘法中重复计数。

顺序限制会改变整个式子。设每个非空部件的生成函数为 $B(x)$ 且 $B(0)=0$，有序拼接任意多个部件得到 $1+B+B^2+...=1/(1-B)$；各类型无序地选数量则对应各自选择因子的乘积。例如重量一、二的有序序列用 $1/(1-x-x^2)$，重量三有三种，而前面的无序选物只有两种。若部件允许零规模，任意拼接可能让同一系数成为无穷，不能再直接使用这个形式幂级数。

线性递推 $a_n=c_1 a_(n-1)+...+c_k a_(n-k)$ 乘上 $1-c_1 x-...-c_k x^k$ 后只剩由初值决定的低次分子，因此极大下标的单项可直接交给 Bostan--Mori。指数生成函数则写成 $sum a_n x^n/n!$，两个指数生成函数相乘时系数带 $C(n,k)$，用于有标号对象的拆分；它与普通卷积的组合含义不同。

指数生成函数的乘法来自分配标号：总共 $n$ 个不同元素，先选其中 $k$ 个交给第一部分，有 $binom(n,k)$ 种选法，再分别形成两个结构。因此实际卷积的数组存 $a_n/n!$，卷积结果的第 $n$ 项乘回 $n!$，才是结构数。若把非空有标号连通部件组成一个无序集合，记部件的指数生成函数为 $B$，整体为 $exp(B)$；展开中的 $B^k/k!$ 消去部件排列的重复。这里要求部件之间没有额外顺序或相互限制，且分解确实唯一。

把递推翻成方程时，每一项的起点必须对齐。例如 $a_n=sum_(i=0)^(n-1)a_i a_(n-1-i)$、$a_0=1$ 给出 $A=1+x A^2$，其中 $x$ 对应根额外占一个单位；漏掉它会把规模整体错移。若只有 $n>=k$ 才满足递推，前 $k$ 项应单独补入分子，不能把关系强行延伸到初值段。式子确定后，再决定用卷积、求逆、对数、指数、牛顿迭代还是远项算法。

多项式目录使用零下标并提供求逆、对数、指数、幂和多点求值；外层 NTT 板是一次卷积的 `1` 下标接口，不要混接。模意义积分要求相应整数可逆，当前 $998244353$ 模数下仍需保证次数小于模数。洛谷 P4389 是把若干选择的生成函数相乘的典型题。

若只求 $0,...,N$ 次系数，加法、乘法以及满足条件的形式幂级数求逆、对数、指数可以在模 $x^(N+1)$ 下计算，不需要更高次输入。单独求导会降低次数，若要保留导数的 $0,...,N$ 次项，原式须多留到 $N+1$ 次。`inverse(A,N+1)` 要求常数项非零，当前 `logarithm` 要求常数项为一，`exponential` 要求常数项为零。多个因子连续朴素相乘仍可能退化；因子多且次数相近时可分治合并，形如大量 $(1-x^w)^(-c_w)$ 的乘积则可先取对数，把乘积变为加和，再作一次多项式指数。

后一种变形是 $log A=sum_w c_w sum_(j>=1)x^(w j)/j$，所以 $[x^t]log A=(1/t)sum_(w mid t)c_w w$。先枚举倍数填出对数系数，再调用指数，可以避免逐个乘所有因子；最终是否划算取决于重量值域与目标次数。出现指定选取件数时，可另设变量 $y$ 给每件物品乘一次 $y$，不要把件数约束直接丢进仅记录总重量的一维系数。
