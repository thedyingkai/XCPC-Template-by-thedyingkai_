==== 用途

- 把计数序列编码为多项式或形式幂级数后，乘法给出“把规模拆成两部分”的方案数，除法可表示前缀和或线性递推，最后读取目标次数的系数。
- 有标号元素的拆分改用指数生成函数，乘积系数对应带二项式系数的合并。

==== 普通生成函数

- 序列 $a_n$ 的普通生成函数为 $A(x)=sum_(n>=0) a_n x^n$。乘积系数满足 $[x^n]A(x)B(x)=sum_(i=0)^n a_i b_(n-i)$，即 FFT/NTT 计算的普通卷积。
- 前缀和序列的生成函数为 $A(x)/(1-x)$；序列 $n a_n$ 的生成函数为 $x A'(x)$；乘 $x^k$ 表示下标平移 $k$ 位。
- 常用展开：$1/(1-x)=sum_(n>=0)x^n$，$1/(1-x)^k=sum_(n>=0)C(n+k-1,k-1)x^n$，$sum_(n>=0)n x^n=x/(1-x)^2$，$-ln(1-x)=sum_(n>=1)x^n/n$。

==== 递推与指数生成函数

- 若 $a_n=c_1 a_(n-1)+...+c_k a_(n-k)$，则 $(1-c_1 x-...-c_k x^k) A(x)$ 只剩由前 $k$ 项决定的低次多项式；求单项时也可考虑矩阵快速幂或线性递推算法。
- 指数生成函数为 $hat(A)(x)=sum_(n>=0)a_n x^n/n!$。两个指数生成函数相乘对应 $c_n=sum_k C(n,k)a_k b_(n-k)$；不要和普通卷积混用。
- $e^x=sum_(n>=0)x^n/n!$。导数标记一个对象，积分取消标记；只有明确区分普通生成函数和指数生成函数后再套组合构造。

==== 与现有板子衔接

- 多项式章节使用 `0` 下标系数，已经提供求逆、对数、指数、幂和多点求值；原 NTT 板的外层接口为 `1` 下标，只在单次卷积时单独使用。
- 模意义下积分需要除以 $1,2,...$，这些数必须在模数下可逆；当前多项式模数为 `998244353`，次数不得达到模数。
- 递推对应的有理生成函数只求极高单项时，直接用 Bostan--Mori，不展开全部前缀。

==== 板子题

- 洛谷 P4389「付公主的背包」。
