普通生成函数把序列编码为 $A(x)=sum_(n>=0) a_n x^n$，乘积系数就是把规模拆成两部分的卷积；前缀和对应 $A(x)/(1-x)$，乘 $x^k$ 表示下标平移，$n a_n$ 对应 $x A'(x)$。常用展开有 $1/(1-x)^k=sum_n C(n+k-1,k-1) x^n$ 和 $-ln(1-x)=sum_(n>=1) x^n/n$。

线性递推 $a_n=c_1 a_(n-1)+...+c_k a_(n-k)$ 乘上 $1-c_1 x-...-c_k x^k$ 后只剩由初值决定的低次分子，因此极大下标的单项可直接交给 Bostan--Mori。指数生成函数则写成 $sum a_n x^n/n!$，两个指数生成函数相乘时系数带 $C(n,k)$，用于有标号对象的拆分；它与普通卷积的组合含义不同。

多项式目录使用零下标并提供求逆、对数、指数、幂和多点求值；外层 NTT 板是一次卷积的 `1` 下标接口，不要混接。模意义积分要求相应整数可逆，当前 $998244353$ 模数下仍需保证次数小于模数。洛谷 P4389 是把若干选择的生成函数相乘的典型题。
