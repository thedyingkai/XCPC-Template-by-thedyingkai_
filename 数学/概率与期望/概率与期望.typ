==== 用途

- 输入随机过程的状态、转移概率和代价，求到达终态前的期望代价、事件计数的期望或方差；DAG 可递推，有环时转成线性方程组。

==== 常用公式

- 期望具有线性性：$E[sum_i X_i]=sum_i E[X_i]$，不要求随机变量相互独立。
- 用指示变量 $I_A$ 表示事件 $A$，则 $E[I_A]=P(A)$。计数型随机变量先拆成若干对象是否出现，通常比直接枚举总数简单。
- 非负整数随机变量满足尾和公式 $E[X]=sum_(k>=1) P(X>=k)$。
- 全期望公式为 $E[X]=sum_y P(Y=y)E[X | Y=y]$；方差可由 $E[X^2]-E[X]^2$ 计算。

==== 期望 DP

- 从状态 $u$ 经过代价 $w(u,v)$ 转移到 $v$ 时，写成 $E_u=sum_v p_(u,v)(w(u,v)+E_v)$。DAG 逆拓扑计算；有环时可能需要高斯消元。
- 若方程含自环概率 $p_0$，先移项：$E=(p_0 E+B)$ 化为 $E=B/(1-p_0)$。模意义下要求分母可逆。
- 独立重复试验成功概率为 $p$ 时，首次成功所需次数期望为 $1/p$；“直到收齐全部类型”通常按当前已收集种类分阶段求等待时间。

==== 边界与 trick

- 期望可直接相加不代表概率可直接相乘；只有独立事件才能把联合概率写成乘积。
- 随机排列中两个不同元素的相对次序各以 $1/2$ 概率出现，逆序对等成对计数优先使用指示变量。
- 题目要求条件期望时先确认条件事件概率非零；浮点输出按误差要求选择 `double` 或 `long double`。

==== 板子题

- 洛谷 P4316「绿豆蛙的归宿」。
