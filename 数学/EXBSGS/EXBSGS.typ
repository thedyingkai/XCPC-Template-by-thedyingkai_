`exbsgs(a,b,p)` 求最小非负整数 $x$，使 $a^x equiv b \(mod p\)$；`a` 与 `p` 不要求互质，无解返回 `-1`，要求 `p > 0`。算法先逐次约去 $gcd(a,p)$，剩余模数与底数互质后再用 BSGS，时间和空间均为 $O(sqrt(p))$。

它适合模意义下反求指数，例如不断乘同一个数，问最早何时到达给定余数。底数和目标可以先归一化，板内已处理负输入。普通对数不能用于这个问题：取模会周期性折返，同一个余数可能对应多个指数，也可能永远到不了。互质时幂序列在模 $p$ 的可逆元素中循环，但合数模数下它未必遍历所有可逆余数。

约因子时，维护已经取出的指数 $k$ 和剩余方程 $A a^t equiv b \(mod p\)$，原答案为 $k+t$。初始 $A=1,k=0$。每轮令 $d=gcd(a,p)$，$d=1$ 时结束约因子，进入 BSGS；$d>1$ 时，若 $d$ 不整除 $b$，则无解，否则令 $b<-b/d$、$p<-p/d$、$A<-A(a/d) mod p$，并把 $k$ 加一。若此时 $A=b$，剩余指数为零，直接返回 $k$。

约去公因子的理由是：排除剩余指数为零后，$a^t$ 至少还含一个 $a$，于是把 $A a^t-b$ 与模数同时除以 $d$，得到模 $p/d$ 下的 $A(a/d)a^(t-1) equiv b/d$。指数少一，已经移到前面的系数积入 $A$。这不是在模意义下求 $d$ 的逆元，因此即使 $d$ 与原模数不互质也合法。每轮都先检查新方程的零次幂解，保证较小答案不会被后续 BSGS 跳过。

例如 $4^x equiv 2 \(mod 14\)$，初始先排除 $x=0$，约去 $d=2$ 后得到 $2 times 4^t equiv 1 \(mod 7\)$，原指数为 $x=t+1$。剩余方程的最小解为 $t=1$，故答案是二。若目标改成三，第一轮就因 $2$ 不整除三而判无解。

互质部分取 $m=ceil(sqrt(p))$，表中记录 $b a^j mod p$，其中 $0<=j<m$；巨步按 $i=1,2,...,m$ 枚举 $A(a^m)^i mod p$。二者相等时，原答案为 $i m-j+k$。相同表值保留最大的 $j$，能使当前巨步对应的指数最小；这些指数区间随 $i$ 递增，所以第一次找到的就是最小答案。

这个拆法把正指数 $t$ 唯一写成 $i m-j$，其中 $0<=j<m$，第 $i$ 轮覆盖 $t=(i-1)m+1,...,i m$。原式两边乘 $a^j$，便得到可直接比较的 $A(a^m)^i=b a^j$。底数已与剩余模数互质，其乘法阶不超过可逆余数的数量，若有解就有小于模数的代表；$m^2>=p$ 因而足够覆盖。每一步都用上一步乘固定数更新，不需要对所有 $i,j$ 各做一次快速幂。

“求最小正整数解”不能简单把返回的零改成无解；互质且 $b=1$ 时，应另求底数的乘法阶或调整搜索范围。若原题是 $c a^x equiv b \(mod p\)$，先令 $d=gcd(c,p)$，检查 $d$ 是否整除 $b$，再在模 $p/d$ 下消去可逆的 $c/d$，化为本接口。每组模数都重新建表；同底数、同模数的大量询问可以考虑另存巨步或小步表，但需要重新安排最小解的搜索顺序。

$b equiv 1$ 对应零次幂，必须在约因子前保留这个答案。哈希表被构造数据卡常时可换自定义哈希；模数大到 `sqrt(p)` 的表无法承受时，这份算法本身就不适用，不能只调大内存。

#link("https://judge.yosupo.jp/problem/discrete_logarithm_mod")[Library Checker · discrete_logarithm_mod]
