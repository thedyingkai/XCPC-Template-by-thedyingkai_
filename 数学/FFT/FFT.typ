`FFT::multiply(a,b)` 用复数 FFT 求整数系数卷积，输入和输出均为 `1` 下标，结果有效区间为 `1..l1+l2-1`。变换后实部经四舍五入存入 `int`，所以它适合系数和长度使真实卷积系数仍在 `int` 内、且浮点误差可控的场合；系数更大时应改返回类型并评估误差，必要时拆系数或改用 NTT/CRT。

十进制大整数乘法可把最低位放在下标一，卷积后调用 `get(ans,10)` 进位。`get` 只处理非负系数，并用单个字符输出一位，要求 `2<=base<=10`；带符号输入先拆符号。相关匹配则翻转一个序列后卷积，把下标差转成下标和。

FFT 同样把系数转换成点值，在对应位置相乘后再转回系数；它用复数单位根，NTT 用模域单位根。设变换长度为 $L$，本代码正向使用 $omega=cos(2pi/L)+i sin(2pi/L)$，逆向使用其共轭，并在末尾除以 $L$。其他资料可能把正向指数符号反过来，只要正反配套即可，不能把两种约定各抄一半。输入底层 `fft` 的数组必须非空且长度为二次幂，封装 `multiply` 会自行处理空输入并补齐长度。

快速变换依赖奇偶分解 $A(x)=A_0(x^2)+x A_1(x^2)$。一对相反的单位根平方相同，因此求出两个半长多项式的点值后，每一对用加减便能合并；逐层倍增长度得到 $O(L log L)$ 时间、$O(L)$ 空间。位逆序置换对应不断按下标奇偶划分后的顺序，例如八个位置的顺序是 `0,4,2,6,1,5,3,7`。普通卷积需补到 $L>=n+m-1$，否则结果会首尾叠加成循环卷积。

整数乘法中，把数写成按进制展开的多项式，在该进制处代入就是原整数。比如 $12=2+1 times 10$、$34=4+3 times 10$，数字数组为 `{0,2,1}`、`{0,4,3}`，卷积得到 `{0,8,10,3}`，进位后为 `408`。先卷积得到每位尚未进位的总贡献，再从低位向高位处理进位；负数的借位规则不在 `get` 的实现范围内。

相关运算常要求每个对齐位置的 $sum_j A_(s+j)B_j$，下标是差而不是和。将长度为 $m$ 的 $B$ 反转成 $B'_j=B_(m-1-j)$ 后，卷积第 $s+m-1$ 项恰好是这个对齐位置的内积；这里公式从零编号，当前返回数组要读取 `result[s+m]`。例如按某个字符分别建立文本和模式的 `0/1` 数组，反转模式后卷积，得到每次对齐时该字符匹配的位置数；对字符集累加便是总匹配数，越出文本的对齐位置不应计入答案。

浮点结果的四舍五入只有在误差绝对值小于 $1/2$ 时才保证恢复整数，真实系数装得进 `int` 并不单独保证这个条件。长度、系数大小和误差累积都要估计，不能给所有输入承诺精确；把输出改成 `i64` 也不会增加 `double` 的有效位数。拆系数时，例如写成 $a=a_0+B a_1$，分别计算低低、低高、高低、高高四类卷积再按 $B$ 合并，目的是让单次浮点乘积保持在较小范围；合并与进位仍需足够宽的整数类型。要求确定精确且模数适合时，优先使用 NTT。

#link("https://judge.yosupo.jp/problem/multiplication_of_big_integers")[Library Checker · multiplication_of_big_integers]
