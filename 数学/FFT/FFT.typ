==== 用途

- 输入两组整数系数，求它们的卷积；可用于多项式乘法、相关匹配和十进制大整数乘法，当前接口把浮点结果四舍五入为整数。

==== 接口与边界

- `FFT::multiply` 使用 `1` 下标系数，结果有效区间为 `1..l1+l2-1`；改为常见的 `0` 下标时需同步修改装载和返回位置。
- 结果经 `round` 转为 `int`。系数可能超出 `int` 时改返回类型；精度不足时拆系数或改用 NTT/CRT。
- `get(ans, base)` 只适用于非负系数，且每一位按单个字符输出；当前只能直接用于十进制高精度乘法。
- 相关匹配可翻转一个序列后做卷积，把下标差改成下标和。

==== 板子题

- 洛谷 P3803「多项式乘法」、P1919「A*B Problem 升级版」。
