NTT 在模 $998244353$ 下精确计算两组系数的卷积，适合普通多项式乘法。`NTT::multiply(A,B)` 使用 `1` 下标输入，返回的有效区间是 `1..n+m-1`；空系数数组用仅含占位零的向量表示。底层 `ntt` 要求长度为二的幂且不超过 $2^23$，会先把负系数规范化。

卷积成立是因为单位根上的点值乘法对应系数卷积，逆变换再乘长度的逆元。更换模数时必须同时确认原根，以及所需二次幂长度整除 `MOD-1`；目标模数不支持足够长度时应使用多模 NTT 后 CRT，或拆系数 FFT。若还要做求逆、对数等完整形式幂级数运算，使用零下标的多项式基础文件，避免两套下标混用。

卷积的数学下标从零开始：$c_k=sum_(i+j=k)a_i b_j$。它对应把两部分的规模相加，例如两批物品按重量计数，卷积能同时统计每种总重量有多少种配对。只求一个 $c_k$ 时直接枚举可能更短；需要整段所有系数，才有必要一起变换。当前一基接口把数学上的 $a_i$ 放在 `A[i+1]`，例如 `{0,1,2,3}` 与 `{0,4,5}` 相乘返回 `{0,4,13,22,15}`。

系数表示下直接相乘要枚举每对下标；若改为在若干点上记录多项式的值，同一点的乘积就是结果多项式在该点的值，只需逐项相乘。NTT 在模质数的域中选取单位根来做这组求值与插值。设变换长度为 $L$，取 $omega=3^((p-1)/L)$，则 $omega$ 的阶恰为 $L$，$1,omega,...,omega^(L-1)$ 互不相同，足以确定次数小于 $L$ 的多项式。这里 $p=998244353=119 times 2^23+1$，所以支持所需的二次幂长度。

快速求值把多项式分成偶次项和奇次项：$A(x)=A_0(x^2)+x A_1(x^2)$。在 $omega^j$ 和 $omega^(j+L/2)$ 处，平方后的点相同，而后者恰好多一个负号，所以两处值由同一对较小规模答案相加、相减得到。一次合并的两项就是代码中的蝶形 `u+v,u-v`。递归有 $log L$ 层，每层处理 $L$ 项，总时间 $O(L log L)$；位逆序置换只是把奇偶递归的叶子顺序提前排好，之后可以按长度从小到大原地合并。

逆变换把单位根换成其逆元，最后乘 $L^(-1)$。由单位根等比和，$sum_(j=0)^(L-1)omega^(j t)$ 在 $t$ 是 $L$ 的倍数时等于 $L$，否则为零；正反两次变换因此只留下原系数的 $L$ 倍。正向、逐点相乘、逆向这三步缺一不可，乘完点值就输出会得到另一种表示。

长度为 $n,m$ 的两组系数，普通乘积有 $n+m-1$ 项，变换长度要补到至少这个数的二次幂。若长度不足，得到的会是模 $x^L-1$ 的循环卷积，高次项绕回低次项；只想输出低位，也不能直接把变换长度减到输出长度。`multiply` 会自行补零，底层 `ntt` 则使用没有占位的零下标数组，并要求调用者已经给出合法长度。

单模结果只确定每项模 $p$ 的余数。若题目要求精确整数或另一个模数，不能把这些余数直接对目标模数再取模；多模 NTT 要先用中国剩余定理恢复到足以区分原系数的范围。非负输入满足 $a_i<=A,b_i<=B$ 时，每项不超过 $min(n,m)A B$，所用模数乘积应大于这个上界；有符号系数若按对称区间恢复，模数乘积应大于两倍绝对值上界。所选每个模数也都必须支持变换长度，中间乘法类型需要一起检查。

#link("https://judge.yosupo.jp/problem/convolution_mod")[Library Checker · convolution_mod]
