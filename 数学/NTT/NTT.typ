==== 接口与边界

- 模数为 `998244353`、原根为 `3`，最大变换长度为 $2^23$。
- `multiply` 使用 `1` 下标系数，结果有效区间为 `1..n1+n2-1`；改 `0` 下标时需同步修改装载和返回位置。
- 更换模数必须同时确认原根，并保证所需二次幂长度整除 `MOD-1`。
- 目标模数不适合 NTT 时，使用多模数 NTT 后 CRT，或改用拆系数 FFT；本板只包含乘法。

==== 板子题

- 洛谷 P3803「多项式乘法」。
