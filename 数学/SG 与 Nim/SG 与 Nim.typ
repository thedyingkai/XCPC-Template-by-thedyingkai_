Nim 和 SG 定理处理无偏、正常规则且若干部分彼此独立的组合游戏。`firstWinsNim` 对堆大小异或，异或和非零当且仅当先手必胜；若某堆满足 `a[i] xor sum < a[i]`，把它减到该值即可转移到必败态。反常 Nim 只有“所有非空堆均为 $1$”这一种例外：非空堆数为偶数时先手胜，空局面仍是先手败；只要存在大于 $1$ 的堆，就恢复普通 Nim 判定。

一般状态的 SG 值满足 $g(u)=op("mex"){g(v) mid u -> v}$，终止状态为零，独立子游戏的 SG 值按位异或。`subtractionGameSG(maxState,moves)` 预处理减法游戏，自动删去非正和重复步长；`DAGSpragueGrundy::get(u)` 对有向无环状态图记忆化求值，并在发现递归环时抛出异常。

这套结论不适用于有偏游戏、允许平局或一般反常规则。有环时也不能把访问标记删掉硬算，必须先证明游戏总会沿某个量严格下降，或使用题目的专门结构。多局共用转移时预处理一次；周期只能在实际算出足够长前缀并证明后使用。

洛谷 P2197「Nim 游戏」、HDU 1848「Fibonacci again and again」。
