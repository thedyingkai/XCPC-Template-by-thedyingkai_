==== 用途

- 输入若干独立的无偏正常规则游戏状态，求每个状态的 SG 值并异或，最终判断先手是否必胜；普通 Nim 可直接对各堆大小异或。

==== Nim

- 普通 Nim 的先手必胜条件是所有堆大小的异或和非零；`firstWinsNim` 直接返回这一条件。
- 设总异或为 $s$，若某堆 $a_i xor s<a_i$，把它减到 $a_i xor s$ 即可把局面变成异或和为零。
- 反常 Nim 中，若所有非空堆均为 `1`，先手在非空堆数为偶数时获胜；只要存在大于 `1` 的堆，就恢复普通 Nim 的判定。

==== Sprague--Grundy

- 仅适用于无偏正常规则组合游戏。记 $g(u)$ 为状态 $u$ 的 SG 值，终止状态取 `0`，其余状态满足 $g(u)=op("mex"){g(v) mid u -> v}$。
- 若干子游戏彼此独立且一步只能操作其中一个，总局面的 SG 值为各子游戏 SG 值的异或；非零当且仅当先手必胜。
- `subtractionGameSG(maxState,moves)` 处理每次从一堆中减去允许数值的游戏；`DAGSpragueGrundy::get(u)` 记忆化计算一般有向无环游戏图。

==== 边界与 trick

- 有环状态图不能直接递归求 SG；需要先证明游戏必然结束并重新选择单调状态，或使用题目的专门结论。
- 有偏游戏、允许平局或反常规则不能直接套 SG 定理；反常 Nim 只是单独结论。
- 多局相同转移先预处理 SG；周期只可在实际计算出并验证后使用，不要默认 SG 序列必有某个短周期。

==== 板子题

- 洛谷 P2197「Nim 游戏」、HDU 1848「Fibonacci again and again」。
