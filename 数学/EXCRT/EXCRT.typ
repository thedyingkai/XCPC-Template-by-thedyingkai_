==== 用途

- 输入若干个模数不必互质的同余条件 $x equiv r_i \(mod m_i\)$，判断它们能否同时满足；`excrt` 有解时返回最小非负解，无解返回 `-1`，解集周期需另取各模数的最小公倍数。

==== 接口与边界

- `m`、`r` 使用 `1..n` 下标，要求 `n >= 1`、模数为正；调用前把 `r[i]` 规范到 `[0,m[i])`，尤其是 `n=1` 时函数不会代为规范。
- `excrt` 返回最小非负解，无解返回 `-1`。
- 中间乘法虽用 `i128`，合并后的模数仍存入 `i64`，所有模数的最小公倍数必须能放入 `i64`。
- 求最小的 $x>=L$：先得到基础解 $r_0$ 和总模数 $M$，再上取整选择 $r_0+k M$。

==== 板子题

- 洛谷 P4777「扩展中国剩余定理」。
