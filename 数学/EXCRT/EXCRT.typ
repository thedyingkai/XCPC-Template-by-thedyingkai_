`excrt(m,r,n)` 合并 $x equiv r_i \(mod m_i\)$，模数不要求互质；有解时返回最小非负解，无解返回 `-1`。数组使用 `1..n` 下标且每个模数必须为正，函数会规范化首个余数，后续余数可为任意 `i64`。

合并当前条件 $x=r_1+m_1 t$ 与新条件时，转成 $m_1t equiv r_2-r_1 \(mod m_2\)$。右端必须被 $gcd(m_1,m_2)$ 整除；扩展欧几里得给出约去公因子后的一个 $t$，新周期是 $lcm(m_1,m_2)$。这个整除条件正是两条同余相容的充要条件，所以逐条合并后得到的仍是全部公共解。

返回值只给基础解。若还要最小的 $x>=L$，需同时维护最终周期 $M$，再取 $r_0+ceil((L-r_0)/M)M$。中间乘法用 `i128`，但最终周期仍须装入 `i64`；超出时函数抛出 `overflow_error`，题目确实需要更大范围时应把返回类型和周期一起改为 `i128`。

洛谷 P4777「扩展中国剩余定理」。
