==== 用途

- 输入 $a,b,p$，在没有更宽整数类型时求不溢出的 $a b mod p$；输出与直接乘法取模相同，但只使用加法和倍增。

`s_mul(a,b,p)` 在没有更宽整数时用加法计算 $a b mod p$，要求 $p>0$、$b>=0$，复杂度 $O(log b)$。

- 乘数可能为负时先处理符号；`abs(LLONG_MIN)` 会溢出，需单独处理。
- 不要把 `add_mod` 改回 `(x + y) % p`，否则加法仍可能溢出。
