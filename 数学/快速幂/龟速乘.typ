`s_mul(a,b,p)` 在没有更宽整数类型时，用二进制拆分和不溢出的模加法求 $a b mod p$。接口接受任意有符号 `i64` 的 `a,b`，先把二者规范到 `0..p-1`，要求 `p > 0`，复杂度 $O(log p)$。

`add_mod` 写成比较后相减，是为了避免 `(x+y)%p` 在加法处先溢出；也不要用 `abs(b)` 处理负数，因为 `LLONG_MIN` 没有对应的正 `i64`。有 `i128` 时优先使用另一份一次乘法的短板。
