==== 用途

- 输入标量或方阵底数与非负指数，求模幂或矩阵幂；矩阵版本可把固定线性转移推进到第 $k$ 步。

==== 接口与改板

- `qp(a,b,p)` 要求 $p>0$、$b>=0$，乘法使用 `i128`。
- `Matrix::qp(base,k)` 要求方阵且 $k>=0$；矩阵乘法不检查维数，也不取模，递推题需在累加处加入题目模数。
==== 板子题

- 洛谷 P1226「快速幂」、P3390「矩阵快速幂」。
