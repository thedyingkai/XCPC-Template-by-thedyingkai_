普通快速幂解决“把同一个乘法对象重复乘 $k$ 次”的问题。`qp(a,k,p)` 求 $a^k mod p$，要求 `p > 0`、`k >= 0`，内部用 `i128` 承接乘法；指数为零时返回模意义下的单位元。负指数不是把循环条件改掉，而是先确认底数可逆，再对逆元做非负次幂。

`Matrix::qp(A,k)` 把同一套二进制拆指数用于方阵，适合固定线性转移推进到第 $k$ 步。输入必须是方阵，乘法维数会检查，但这个短板既不自动取模也不防整数溢出；递推题应把矩阵元素换成自带模运算的类型，或在乘加处加入题目模数。初始列向量为 $v$、一步转移为 $A$ 时，第 $k$ 步是 $A^k v$；含常数项的仿射转移可多加一个恒为 $1$ 的坐标。

#link("https://judge.yosupo.jp/problem/pow_of_matrix")[Library Checker · pow_of_matrix]
