==== 用途

- 维护整数集合的最大异或查询；也用于最大异或子段和树上最大异或路径。

==== 接口与坑

- `query(val)` 返回最大异或值，不返回配对的原数；空 Trie 返回 `0`。
- 当前只处理第 `30..0` 位。更大权值需改为 `u64`，并调整最高位和位运算常量。
- 支持删除时给节点加经过计数，查询只走计数为正的分支。

==== Trick

- 最大异或时优先走当前位相反的儿子；改求最小异或时优先走相同儿子。
- 最大异或子段必须把空前缀 `0` 先插入 Trie，再按顺序查询并插入当前前缀异或。
- 树上令 $sum_x$ 为根到 $x$ 的路径异或，则路径 $(u,v)$ 的异或为 $sum_u xor sum_v$。

==== 板子题

- 洛谷 P4551「最长异或路径」。
