==== 01 Trie 求解树上最大异或路径

给定一棵 $n$ 个节点的带权树，边权为整数。定义路径的权值为路径上所有边权的异或和。  
该算法利用性质：任意两点 $u, v$ 之间的路径异或等于 $sum_u xor sum_v$，其中 $sum_x$ 表示节点 $x$ 到根节点路径上边权的异或和。  
因此问题转化为：求 $sum_1, sum_2, ..., sum_n$ 中两个数异或的最大值，可用 01-Trie 在 $O(n log W)$ 时间内求解，$W$ 为值域大小（本例 $W = 2^31$）。

- 时间复杂度：$O(n log W)$，实际常用 $log W = 31$，即 $O(31n)$。
- 空间复杂度: $4n log W$ 。

==== 适用场景

- 树上任意两点路径异或最大值。
- 给定一个数组，求最大异或子段。
- 任何需要快速查询与某个数异或最大的数的场景，且值域固定。
- 不支持删除操作，仅限离线静态查询。

当需要支持删除或动态插入时，需在 Trie 节点中增加计数域。
