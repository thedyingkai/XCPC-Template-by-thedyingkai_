==== 用途

- 维护小写字符串集合的前缀计数；重复插入会重复计数。

==== 接口与坑

- `prefix_count(s)` 返回以 `s` 为前缀的已插入字符串数。
- 当前没有终止标记，不能判断完整字符串是否出现；也没有维护空前缀计数。
- 只支持小写字母。

==== 改法

- 查完整字符串时在终点增加 `end` 计数；支持删除时同时维护路径计数和 `end`。
- 换字符集时同步修改 `SZ` 和映射；节点多时改用数组池。

==== 板子题

- 洛谷 P8306「字典树」。
