普通 Trie 沿字符逐层建点，共享相同前缀。每个节点的 `count` 记录有多少个已插入字符串经过它，因此 `prefix_count(s)` 就是以 `s` 为前缀的字符串数；重复插入会重复计数，空前缀返回已插入字符串总数。

- 当前没有终止标记，不能判断完整字符串是否出现。
- 只支持小写字母。

- 查完整字符串时在终点增加 `end` 计数；支持删除时同时维护路径计数和 `end`。
- 换字符集时同步修改 `SZ` 和映射；节点多时改用数组池。

- 洛谷 P8306「字典树」需要把字符映射扩成大小写字母和数字共 `62` 种，并按题面读取多组插入、前缀查询。当前 `main` 只输出节点数，提交时要改为输出 `prefix_count`；每组结束后释放全部节点，或改用可清空的数组池。只重新构造 `Trie` 不会释放上一组通过 `new` 分配的节点。
