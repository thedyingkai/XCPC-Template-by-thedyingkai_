后缀数组把静态字符串的全部后缀按字典序排列，`sa` 给出排名到起点，`rk` 给出起点到排名，`height[i]` 是相邻后缀 `sa[i-1]` 与 `sa[i]` 的 LCP。它提供确定性的子串顺序和公共前缀信息，可继续解决不同子串数、重复子串、模式匹配和第 $k$ 小子串。当前使用倍增和计数排序，构造时间为 $O(n log n)$，空间为 $O(n)$。

- `sa`、`rk`、`height` 全部为 `0` 下标。
- `sa[i]` 是字典序第 `i` 小后缀的起点；`height[i] = LCP(sa[i],sa[i-1])`，`height[0] = 0`。
- 当前只接收字节字符串。整数序列需改输入类型并离散化初始排名。

倍增法先按一个字符排好序。假设已经知道长度 $k$ 的前缀排名，长度 $2k$ 的比较就变成二元组 `(rk[i],rk[i+k])` 的比较，后半段越界时视为小于所有正常排名。代码先利用旧 `sa` 整理第二关键字的顺序，再对第一关键字做稳定计数排序，每轮 $O(n)$；重新编号后把 $k$ 翻倍，直到排名互异或长度覆盖全串。不需要真的把后缀复制出来。

求 `height` 时按原串起点从左往右处理。若当前后缀与字典序前驱有公共前缀长 $h$，两者删掉首字符后仍有长至少 $h-1$ 的公共前缀；新后缀的真正前驱不会使可复用的下界更差。因此下一位置从 `max(h-1,0)` 开始比较，成功比较使指针增加，而每换一个位置最多减少一，总比较次数为线性。`height` 描述的是字典序相邻关系，不是原串相邻位置的 LCP。

以 `banana` 为例，`sa={5,3,1,0,4,2}`，对应 `a,ana,anana,banana,na,nana`，`height={0,1,3,0,0,2}`。所有以 `ana` 为前缀的后缀在排名中连续，占据起点 `3,1` 的那一段。任意两端后缀共同拥有的前缀，中间每个后缀也拥有；反过来，相邻 LCP 的最小值会限制两端的 LCP，这就是后缀 LCP 转成区间最小值的原因。

做题时可以先判断问题能否转成“一组后缀有共同的长度 $L$ 前缀”。把所有 `height>=L` 的相邻排名连起来，每个连通段就是一个相同长度 $L$ 子串的全部出现位置。出现次数、最早最晚位置、属于几个原串，都可以在这个段内统计。大量 LCP 查询要另外建立 RMQ：稀疏表预处理 $O(n log n)$、单次 $O(1)$，线段树则预处理 $O(n)$、单次 $O(log n)$；当前 `SA` 类本身没有这一层查询结构。

- 两个不同后缀的 LCP：令 `a=min(rk[x],rk[y])`、`b=max(...)`，查询 `height[a+1..b]` 的最小值；`x==y` 时返回 `n-x`。
- 比较两个等长子串：先查 LCP；LCP 足够长则相等，否则比较第一个不同字符。
- 多字符串公共子串：用互不相同、且不出现在任何原串中的分隔符拼接，并记录每个后缀属于哪一个原串；字节字符不够分配时改用整数序列。
- 不同子串数量为 `n * (n + 1) / 2 - sum(height)`，答案使用 `i64`。
- 至少出现两次的最长子串为 `max(height)`。
- 至少出现 `k>=2` 次的最长子串：二分长度，把连续 `height >= mid` 的位置看成后缀组；`k=1` 时答案直接为 `n`。
- 已知子串 `s[pos..pos+len)`：从 `rk[pos]` 向左右二分最远排名，使中间 `height` 的最小值仍不小于 `len`；所得排名区间就是全部出现位置。
- 外部模式串匹配：在 `sa` 上分别二分第一个不小于模式串、以及第一个严格大于“以模式串为前缀”的后缀；两端之差就是出现次数，比较时后缀先耗尽视为更小。
- 第 $k$ 小不同子串：按 `sa` 顺序，第 `i` 个后缀新贡献长度 `height[i]+1..n-sa[i]`。依次扣除前面各后缀的新子串数量，落入第 `i` 段时保留从 `1` 开始的段内排名 `k`，答案长度为 `height[i]+k`。
- 最长不重叠重复子串：二分长度 `L`，在每个连续 `height>=L` 的后缀组中维护起点最小值与最大值，差至少为 `L` 即可行。

- 洛谷 P3809「后缀排序」；P2408「不同子串个数」；P2852「Milk Patterns G」。
- #link("https://judge.yosupo.jp/problem/suffixarray")[Library Checker · Suffix Array]
