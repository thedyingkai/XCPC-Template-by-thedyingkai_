==== 输入与产物

- 输入静态字符串，预处理所有前缀的双模哈希。
- `get(l,r)` 返回半开子串 `[l,r)` 的双哈希，可比较等长子串或配合二分求 LCP。
- 适合子串相等、回文和拼接哈希；不能接受碰撞时改用后缀数组等确定性做法。

==== 接口与坑

- 字符串下标从 `0` 开始。
- 比较两个子串是否相等时先确认长度相同，再比较返回的两个整数。
- 所有 `StringHash` 使用相同的底数和模数，因此不同对象之间可以比较同长度子串。

==== 改法与 trick

- 题目卡固定哈希时，进程启动后只随机一次合法 `BASE`，所有对象必须共用；双模也不是数学上的零碰撞。
- 字符集包含任意整数时，先离散化并给编码整体加一，避免把有效符号映射为零。
- 需要拼接哈希时使用 `H(A+B) = H(A) * BASE^len(B) + H(B)`，两个模数分别计算。
- 回文判断：同时建立原串和反串哈希；原串 `[l, r)` 对应反串 `[n-r, n-l)`。
- 两个后缀的 LCP：二分长度并比较哈希，再用下一字符决定字典序。
- 整串去重时把长度和双模哈希一起作为键。

==== 板子题

- 洛谷 P3370「字符串哈希」。
