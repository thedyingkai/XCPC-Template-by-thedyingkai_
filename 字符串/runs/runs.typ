Runs 枚举字符串中所有极大周期串。一个结果 `(l,r,p)` 表示 `[l,r)` 的最小周期为 `p`、长度至少为 `2p`，并且不能再向任一侧扩展而保持该周期。板子用两种字符序的 Lyndon 根产生候选，再用正反 LCE 扩到极大区间，最后排序去重。当前构造三份倍增后缀数组，并用线段树查询 LCE，整体时间为 $O(n log n)$，空间为 $O(n)$。

==== 定义与边界

- run 满足 `r-l>=2p`，且在不越界时向左或向右扩展一个字符都会破坏周期 `p`。
- 区间使用 `0` 下标半开形式。

- 板子用正、反两种字符序的 Lyndon 根产生候选，再用前后向 LCE 扩成极大区间；两种字符序可能发现相同三元组，因此末尾必须排序去重。
- LCE 由后缀数组和区间最小值线段树完成；依赖字符串章节中的后缀数组板。
- 字符按无符号字节排序；整数序列需把后缀数组输入改成离散化后的整数，并把反序改成排名取反。

- 指数为 `(r-l)/p`；统计所有极大连续重复只遍历返回结果，不要把任意周期区间误当作 run。

周期 $p$ 的含义是区间内相距 $p$ 的字符都相等，不要求区间长度能被 $p$ 整除。例如 `ababa` 的最小周期是 `2`，整个串给出 `(0,5,2)`，指数为 $5/2$；求实数指数时不要做整数除法。`ababab` 的两个 `abab` 子区间都还能扩展，因此只返回覆盖全串的 `(0,6,2)`。题目要求枚举全部串联重复、统计周期段或求最大重复次数时，可以先枚举 run，再在每段中统计所需对象；不能把 run 数量当作重复子串数量。

若已知两个相邻重复块的候选起点 `i,j`，令 `p=j-i`。比较 `i,j` 向右的最长公共前缀得到 `b`，比较它们左边字符向左的最长公共后缀得到 `a`，周期就能扩成 `[i-a,j+b)`；其中每对相距 `p` 的字符都相等。条件 `a+b>=p` 等价于扩展后至少包含两个周期。代码在反串中查询 `lcp(n-i,n-j)` 得到左扩展量，`i==0` 时直接为零，以免访问越界位置。

难点在于不能枚举所有 `i,j`。板子对每个 `i`，找右侧第一个后缀排名比它小的位置 `j`，此时 `[i,j)` 是以 `i` 开始的最长 Lyndon 串；这个“右侧第一个更小值”由单调栈在线性时间求出。Lyndon 串没有更小周期，包含它且通过上述延伸得到的重复段，其最小周期就是 `j-i`，不必再对周期试除。

候选覆盖依赖 Lyndon 根的性质：每个 run 在正反两种字符序中，至少有一种序能使其中一个长度为最小周期的 Lyndon 块成为该起点的最长 Lyndon 串。只跑通常的字符序会漏掉另一种边界方向，所以要再对字符大小取反后的串求后缀排名；字符位置并没有倒过来。反串用于向左匹配，字符序取反用于补全候选，这两份辅助串各有用途。

若左扩展 `a>=p`，当前 Lyndon 块前面还有完整的一份相同块，代码跳过这个重复候选，保留更靠左的代表；两种字符序之间仍可能重合，因此最后按完整三元组排序去重。每种序只产生 $O(n)$ 个候选，每次 LCE 花 $O(log n)$，加上三份后缀数组和排序，得到当前 $O(n log n)$ 的复杂度。论文中的线性算法还需要线性后缀结构与常数时间 LCE，不能直接套在这份实现上。

#link("https://arxiv.org/abs/1406.0263")[Lyndon 根覆盖性质：The Runs Theorem]

- #link("https://judge.yosupo.jp/problem/runenumerate")[Library Checker · Run Enumerate]
