==== 输入与产物

- 输入多个小写模式串和待扫描文本，建立 Trie 与 fail 跳转。
- 产出每个模式串在文本中的出现次数，重叠匹配也计入。
- 当前板直接统计多模式串出现次数；模式动态启停和 fail 树后缀关系询问还需显式建立 fail 树，并加入 DFS 序及 Fenwick/线段树维护。

==== 接口与坑

- 只支持小写字母。先插入全部模式串，再依次调用 `build()`、`query(text)`、`topu()`。
- 相同模式串共用一个 `idx`。`topu()` 会修改 `du` 和节点计数，不能重复调用。
- 多篇文本只求总出现次数时，可连续 `query`，最后统一 `topu`。

==== 改法与 trick

- 换字符集时同步修改 `son` 大小和字符映射。
- 模式串动态启停：在其终止节点的 fail 树子树区间加减；扫描到状态 `u` 时查询 `dfn[u]` 的单点值。
- fail 树把 `fail[v]` 连向 `v`；文本扫到状态 `u` 时，所有匹配模式正是 `u` 到根路径上的终止节点。
- 模式串 `a` 是模式串 `b` 的后缀，当且仅当 `a` 的终止节点是 `b` 的终止节点在 fail 树上的祖先；批量询问可转 DFS 序祖先判定。
- 只求文本每个位置匹配了多少模式时，预处理 `out[u]=terminal[u]+out[fail[u]]`，扫描到 `u` 直接累加 `out[u]`；重复模式是否重复计数由 `terminal` 的定义决定。

==== 板子题

- 洛谷 P5357「AC 自动机（二次加强版）」。
