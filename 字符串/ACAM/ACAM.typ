AC 自动机把多个小写模式串放进同一棵 Trie，并用 `fail` 指针表示当前前缀失配后应转到的最长可用后缀。文本只扫描一次，每到一个状态先记访问次数，最后沿 fail 树从深到浅累加，就得到每个模式串的出现次数，重叠匹配也会计入。

==== fail 构造与计数

建自动机时按 BFS 处理节点。若 Trie 中存在边 `u -c-> v`，则 `fail[v]=go(fail[u],c)`；若不存在这条 Trie 边，就把 `go(u,c)` 补成 `go(fail[u],c)`，于是查询文本时每个字符只做一次数组转移。根的儿子 `fail` 为根，根缺失的转移也指回根。

文本扫到状态 `u`，说明 `u` 以及沿 `fail` 一直向上的所有终止节点都在当前位置匹配。代码先只给访问到的 `u` 加一，再把每条 fail 边看成 `fail[v] -> v`：`du[fail[v]]++`，从 `du==0` 的叶子开始把 `ans[v]` 加到 `ans[fail[v]]`。这正是按 fail 树从深到浅汇总，避免每个文本位置逐个跳祖先。

- 只支持小写字母。先插入全部模式串，再依次调用 `build()`、`query(text)`、`topu()`。
- 相同模式串共用一个 `idx`。`topu()` 会修改 `du` 和节点计数，不能重复调用。
- 多篇文本只求总出现次数时，可连续 `query`，最后统一 `topu`。

- 换字符集时同步修改 `son` 大小和字符映射。
- 模式串动态启停：在其终止节点的 fail 树子树区间加减；扫描到状态 `u` 时查询 `dfn[u]` 的单点值。
- fail 树把 `fail[v]` 连向 `v`；文本扫到状态 `u` 时，所有匹配模式正是 `u` 到根路径上的终止节点。
- 模式串 `a` 是模式串 `b` 的后缀，当且仅当 `a` 的终止节点是 `b` 的终止节点在 fail 树上的祖先；批量询问可转 DFS 序祖先判定。
- 只求文本每个位置匹配了多少模式时，预处理 `out[u]=terminal[u]+out[fail[u]]`，扫描到 `u` 直接累加 `out[u]`；重复模式是否重复计数由 `terminal` 的定义决定。

- 洛谷 P5357「AC 自动机（二次加强版）」。
- Library Checker 的 Aho–Corasick 题输出 Trie 节点数、每个节点的 Trie 父亲与 `fail`，以及每个模式串终点；它严格检查当前 `insert/build` 核心，但需在插入新节点时另存父亲，并记录每个模式串遍历后的终点，不使用 `query/topu`。
- #link("https://judge.yosupo.jp/problem/aho_corasick")[Library Checker · Aho–Corasick]
