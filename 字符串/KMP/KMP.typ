==== 输入与产物

- 输入模式串 `p` 和文本串 `t`，预处理模式的失配数组 `nxt`。
- `KMP(p).match(t)` 返回模式在文本中全部匹配的 `1` 下标起点。
- `nxt` 还可用于 border、周期、循环同构和前缀出现次数。

==== 接口与坑

- `nxt` 使用 `1` 下标，`nxt[i]` 是长度为 `i` 的前缀的最长真 border 长度；不要与 `0` 下标的 `pi` 定义混用。
- 空模式串返回空结果。

==== Trick

- 枚举长度 `x` 前缀的所有真 border：先令 `x = nxt[x]`，再反复跳 `nxt`。
- 最小周期候选为 `p = n - nxt[n]`；要求整串由周期块重复时还要检查 `n % p == 0`。
- 统计每个前缀的出现次数：先令 `cnt[1..n] = 1`，再按 `i=n..1` 执行 `cnt[nxt[i]] += cnt[i]`。
- 把每个前缀长度 `i` 连向 `nxt[i]` 得到失配树。对 `x<y`，`x` 是 `y` 的真 border 当且仅当 `x` 是 `y` 的严格祖先；两个前缀的 LCA 若等于其中任一节点，求共同真 border 时还要再跳一次 `nxt`。长度 `x` 的子树包含所有把它作为 border（允许等于自身）的前缀长度。
- 等长串 `s,t` 循环同构当且仅当 `t` 在 `s+s` 中有起点不超过 `n` 的匹配；若不允许原位置，再排除起点 `1`。
- 两次出现允许重叠时，间距 `d<m` 等价于模式串存在长度 `m-d` 的 border，也等价于 `d` 是其周期；沿 `nxt[m]` 链可枚举全部合法间距。
- 长度为 `L` 的串同时有周期 `p,q`，且 `L >= p+q-gcd(p,q)` 时，`gcd(p,q)` 也是周期。

==== 板子题

- 洛谷 P3375「KMP 字符串匹配」。
