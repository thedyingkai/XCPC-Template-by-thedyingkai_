==== 用途

- 求最长回文子串，或批量判断固定字符串的子串是否回文。

==== 接口与坑

- `is_pal(l, r)` 判断 `0` 下标半开区间 `[l, r)`；空区间返回 `false`。
- `p[i]` 等于该中心对应回文在原串中的长度，最长回文长度即 `max(p)`。
- `longest()` 在长度相同时取起点最小者。

==== Trick

- 回文子串总数为 $sum_i floor((p_i + 1) / 2)$，答案通常使用 `i64`。
- 枚举中心后，原串回文左端点为 `(i - p[i]) / 2`，长度为 `p[i]`。

==== 板子题

- 洛谷 P3805「Manacher」。
