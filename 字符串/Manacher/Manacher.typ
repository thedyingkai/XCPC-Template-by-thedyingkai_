==== 输入与产物

- 输入一个静态字符串，产出每个奇偶中心对应的回文长度数组 `p`。
- `longest()` 返回最长回文子串，`is_pal(l,r)` 判断半开子串 `[l,r)` 是否回文。
- 适合最长回文、回文子串计数和固定字符串上的批量回文判定。

==== 接口与坑

- 下标从 `0` 开始；空区间的 `is_pal` 返回 `false`。
- `p[i]` 等于该中心对应回文在原串中的长度，最长回文长度即 `max(p)`。
- `longest()` 在长度相同时取起点最小者。

==== Trick

- 回文子串总数为 $sum_i floor((p_i + 1) / 2)$，答案通常使用 `i64`。
- 枚举中心后，原串回文左端点为 `(i - p[i]) / 2`，长度为 `p[i]`。

==== 板子题

- 洛谷 P3805「Manacher」。
