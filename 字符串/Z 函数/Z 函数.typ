Z 函数记录每个后缀与整串的 LCP：`z[i]=LCP(s,s[i..])`，并约定 `z[0]=n`。维护当前最靠右的匹配盒后，盒内位置可以先复制对应前缀的已知长度，再继续暴力扩展，因此总复杂度为线性；模式匹配、border、周期和前缀出现次数都能直接读取这些值。

- 使用 `0` 下标；空串返回空数组。
- 模式串 `p` 匹配文本 `t` 时计算 `p + 分隔符 + t`；分隔符必须不在字符集中。无法保证时改用整数序列并加入独立编号。

- 所有真 border：枚举 `1 <= i < n`，若 `i + z[i] == n`，则 `z[i]` 是一个 border 长度。
- 判断长度 `p` 是否为周期：检查 `z[p] >= n-p`；若题目要求整串由周期块重复，还要检查 `n` 能否被 `p` 整除。
- 求每个前缀出现次数：把每个 `z[i]` 对长度区间 `[1, z[i]]` 贡献一次，可用差分统计。

- 洛谷 P5410「扩展 KMP / exKMP（Z 函数）」。
- #link("https://judge.yosupo.jp/problem/zalgorithm")[Library Checker · Z Algorithm]
