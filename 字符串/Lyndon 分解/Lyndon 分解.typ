Duval 算法在线性时间内把字符串唯一分解为字典序不增的 Lyndon 串序列。`factorize` 返回各因子的半开区间；在双倍串上运行同一过程，`min_rotation` 得到字典序最小循环表示的最小起点。算法只依赖字符间的全序，因此也能改成整数序列。

- Lyndon 串严格小于它的每个非空真后缀。任意字符串唯一分解为字典序不增的 Lyndon 串序列。
- 半开区间使用 `0` 下标，相邻相同因子仍分别返回。
- `min_rotation` 在有多个最小表示时取最小起点；答案串为 `s.substr(pos)+s.substr(0,pos)`，空串返回 `0`。

Lyndon 条件中的“严格”很重要。`ab` 小于后缀 `b`，所以是 Lyndon 串；`abab` 的后缀 `ab` 更短且是它的前缀，字典序反而更小，所以不是。Lyndon 串不能由更短串重复得到，也没有非空真前缀等于真后缀。分解结果必须字典序不增，例如 `ababbab` 分成 `ababb | ab`；它不是按最长重复块随意切割。

Duval 扫描时，`i` 是尚未输出部分的起点，`j` 是正在尝试加入的字符，`k` 在当前已知重复结构中与 `j` 对齐比较。相等时一起前进，表示周期还能延续；若 `s[k]<s[j]`，新字符使当前整段成为更长的 Lyndon 候选，把 `k` 放回 `i`，从头比较这个新候选。若 `s[k]>s[j]` 或 `j` 到达串尾，当前能输出的因子长度就是 `j-k`，把已经确认的完整重复块逐个输出，再处理余下部分。

例如 `abab` 从前两个字符确定候选 `ab`，后面两个字符逐一相等；扫描结束时 `j-k=2`，于是输出两个相同的因子 `ab | ab`。虽然内部有嵌套循环，每轮留下待重扫的尾部比已经输出的部分短，重扫成本可以摊到被输出的字符上，总时间为 $O(n)$。输出区间占 $O(n)$ 空间，扫描本身只需常数个指针。

最小循环表示把首尾连接改成在 `s+s` 中取长为 `n` 的子串。Duval 每轮确认一批起点不可能优于当前候选，直接跳过这些重复位置，只在原串范围内记录尚未被排除的候选起点。例如 `baba` 的最小表示为 `abab`，起点 `1` 和 `3` 等价，当前实现返回较小的 `1`。这里“双倍串”用于允许比较跨过原串末尾，输出仍只取原长度，不能把整个因子直接当作答案串。

- 判断循环同构先确认 `s`、`t` 等长，再在 `t+t` 中匹配 `s`；需要最小循环表示时再用 `min_rotation`。
- Lyndon 分解只依赖元素全序。改整数序列时还要去掉代码中的 `unsigned char` 强转，并用插入两遍元素代替字符串的 `source+source`；无需离散化。

- 洛谷 P1368「工艺」。
- #link("https://judge.yosupo.jp/problem/lyndon_factorization")[Library Checker · Lyndon Factorization]
