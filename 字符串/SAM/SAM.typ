后缀自动机适合把一个文本中数量可能达到平方级的不同子串压缩起来，统计不同子串数、出现次数、最长重复子串，或用它匹配另一个串。它把子串按相同的结束位置集合 `endpos` 分成若干状态。长度 `n>=2` 时至多有 `2n-1` 个状态，空串和单字符分别有 `1`、`2` 个状态。非初始状态 `v` 代表长度区间 `(len[link[v]],len[v]]` 内的一批子串，转移表示在右端追加字符。当前类只负责构造；随附主程序按 `len` 降序汇总出现次数，只在出现至少两次的子串中求 `occ*len` 最大值，没有重复子串时输出 `0`。

例如文本 `ababa` 中，`ba` 和 `aba` 都结束在下标 `2,4`，因此共用一个状态；`a` 还出现在下标 `0`，属于另一个状态。同一状态内，较短串都是最长串的后缀，而且中间长度不会缺失，所以能用一个长度区间表示。`link[v]` 指向不再属于当前等价类的最长后缀，其 `endpos` 严格包含当前集合。沿转移是在串尾加字符，沿 `link` 是丢掉串首的一部分，两种边不能混着理解。

从初始状态出发的每条路径都对应一个子串，同一个子串的转移路径唯一；不同路径可以汇合到同一状态。转移总是让 `len` 增大，形成有向无环图，适合按长度做 DP；后缀链接总是让 `len` 减小，把所有 `link` 反向连边则得到一棵树。子串种数通常在转移图或长度区间上统计，出现位置通常在后缀链接树上统计。

==== extend 与 clone

加入字符 `c` 时新建 `cur`，令 `len[cur]=len[last]+1`，再从 `last` 沿后缀链接向上，把所有尚无字符 `c` 转移的状态连到 `cur`。若一直走到 `-1`，`link[cur]=0`；否则遇到已有转移 `p -c-> q`。当 `len[p]+1==len[q]` 时可直接令 `link[cur]=q`。

若 `len[p]+1<len[q]`，原来由 `q` 表示的一部分较短子串会在新位置再次出现，而较长子串不会，它们的 `endpos` 从此不同，需要拆成两个状态。`clone` 复制 `q` 的全部转移和旧 `link`，并令 `len[clone]=len[p]+1`，表示拆出的较短部分；沿 `p` 的后缀链，把仍指向 `q` 的字符 `c` 转移改到 `clone`，最后令 `link[q]=link[cur]=clone`。克隆不代表新的前缀终点，所以统计出现次数时新状态 `cur` 初值为 `1`，`clone` 初值为 `0`，再沿后缀链接累加。

- 依次调用 `extend(c)` 构造；每个非初始状态 `v` 对应长度在 `(len[link[v]], len[v]]` 内的同一 `endpos` 类。
- 当前转移使用 `unordered_map`；小写字母大数据时改成定长数组。
- 多字符串不要直接无分隔拼接；按题意使用广义后缀自动机或逐串匹配。

构造时保存每个文本前缀对应的 `extend` 返回值，就给每个结束位置找到了一个状态。某个模式出现于位置 $r$，等价于该模式状态是这个前缀状态在后缀链接树上的祖先；所以对子树中的前缀终点计数，恰好得到 `endpos` 大小。按 `len` 降序累加时，儿子先处理完，父亲才能收到完整贡献。当前主程序的桶排序和汇总都是线性的；`unordered_map` 转移查找按平均常数时间估计，固定小字符集改数组后更稳定。

例如在 `ababa` 的自动机中匹配 `ba`，从状态零沿 `b`、`a` 走到同一个结束位置类，汇总后的次数是 `2`。若查询限制出现必须完整落在文本闭区间 `[L,R]`，模式长为 `m`，应统计结束位置在 `[L+m-1,R]` 的前缀终点，不能直接统计 `[L,R]`。可按结束位置逐个加入其状态的 DFS 序，再用主席树或离线树状数组查询模式状态的子树。

求另一个串与文本的最长公共子串时，同时维护状态 `v` 和实际匹配长度 `l`，初始都为零。读到字符 `c` 而当前无转移，就沿 `link` 回退，并把 `l` 缩到新状态的 `len`；找到转移后沿边走并令 `l++`，退到根仍无转移则令 `l=0`。每个位置的 `l` 是以该位置结束的最长公共子串长度，取最大值即可。不能每次把 `l` 直接赋成转移后状态的 `len`，因为一个状态代表多个长度，当前匹配可能只是其中较短的那个。

- 不同子串数量为所有非初始状态的 `len[v] - len[link[v]]` 之和。
- 出现至少 $k$ 次的最长子串：汇总 `endpos` 大小后，取满足计数的最大 `len`。
- 与另一字符串的最长公共子串：在自动机上扫描，失配时沿 `link` 回退并同步缩短当前长度。
- 单个模式串的出现次数：沿转移走到状态 `v` 后取 `endpos(v)` 大小；构造时新状态计数为 `1`、克隆为 `0`，再按 `len` 降序累加到 `link`。
- 不同子串按字典序第 $k$ 小：`ways[u]=sum(1+ways[v])`；按字符枚举转移，先计入到达 `v` 后立即结束的一个串，再沿从 `v` 出发的后续转移继续枚举。
- 按出现次数重复计入时改为 `ways[u]=sum(occ[v]+ways[v])`；总数可能超过 `i64`，计算时按 $k$ 截断即可。
- 当前 `unordered_map` 的遍历顺序不是字典序；求第 $k$ 小时必须排序每个状态的出边，或按字符集改成定长数组后顺序枚举。
- 后缀链接树中状态 `v` 的子树对应所有 `endpos` 被其包含的状态；配合 DFS 序可做某模式在文本前缀区间内的出现统计。

- 洛谷 P3804「后缀自动机」。
- #link("https://judge.yosupo.jp/problem/number_of_substrings")[Library Checker · Number of Substrings]
