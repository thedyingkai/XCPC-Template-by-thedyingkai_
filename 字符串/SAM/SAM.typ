==== 输入与产物

- 输入一个文本串，当前 `SAM` 类只负责逐字符建立状态、转移和后缀链接。
- 随附主程序沿后缀链接树汇总 `endpos` 大小，并求重复子串的 `occ * len` 最大值。
- 不同子串数可由状态长度差直接累加；最长公共子串和第 $k$ 小子串还需按后文另写扫描或 DAG DP，当前没有现成查询接口。

==== 接口与坑

- 依次调用 `extend(c)` 构造；每个非初始状态 `v` 对应长度在 `(len[link[v]], len[v]]` 内的同一 `endpos` 类。
- 当前转移使用 `unordered_map`；小写字母大数据时改成定长数组。
- 多字符串不要直接无分隔拼接；按题意使用广义后缀自动机或逐串匹配。

==== 改法与 trick

- 不同子串数量为所有非初始状态的 `len[v] - len[link[v]]` 之和。
- 出现至少 $k$ 次的最长子串：汇总 `endpos` 大小后，取满足计数的最大 `len`。
- 与另一字符串的最长公共子串：在自动机上扫描，失配时沿 `link` 回退并同步缩短当前长度。
- 单个模式串的出现次数：沿转移走到状态 `v` 后取 `endpos(v)` 大小；构造时新状态计数为 `1`、克隆为 `0`，再按 `len` 降序累加到 `link`。
- 不同子串按字典序第 $k$ 小：`ways[u]=sum(1+ways[v])`；按字符枚举转移，先计入到达 `v` 后立即结束的一个串，再进入 `v` 的子树。
- 按出现次数重复计入时改为 `ways[u]=sum(occ[v]+ways[v])`；总数可能超过 `i64`，计算时按 $k$ 截断即可。
- 当前 `unordered_map` 的遍历顺序不是字典序；求第 $k$ 小时必须排序每个状态的出边，或按字符集改成定长数组后顺序枚举。
- 后缀链接树中状态 `v` 的子树对应所有 `endpos` 被其包含的状态；配合 DFS 序可做某模式在文本前缀区间内的出现统计。

==== 板子题

- 洛谷 P3804「后缀自动机」。
