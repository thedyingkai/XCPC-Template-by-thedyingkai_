==== 用途

- 输入不保证为二分图的无向图，求不重复使用端点的最大匹配，返回匹配边数和每个点的匹配对象。
- 当合法配对关系中存在奇环时使用本板；若已知图是二分图，改用 Hopcroft--Karp。

==== 接口

- `GeneralMatching(n)` 的点为 `1..n`；`add(u,v)` 加无向边，自环忽略，平行边不影响答案。
- `maxMatching()` 用 Edmonds Blossom 算法求无权一般图最大匹配，返回匹配边数；`mate[u] == 0` 表示未匹配。
- 每次 `maxMatching()` 都从空匹配重新计算，复杂度 $O(n^3)$。

==== 选用与坑点

- 当前只求最大匹配边数，不求最大权匹配；带权一般图不能通过排序边权后反复调用本板得到正确答案。
- 恢复方案时枚举满足 `u < mate[u]` 的点即可，每条匹配边只输出一次。

- 洛谷 P6113「一般图最大匹配」。
