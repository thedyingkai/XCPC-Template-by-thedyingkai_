给定不保证二分的无向图，一般图最大匹配要选尽量多条两两不共享端点的边。合法配对关系中可能出现奇环时使用 Edmonds Blossom；若已经知道图是二分图，Hopcroft--Karp 更快也更短。

把物品、人员或状态作为点，两者允许成一对就连边；每个点最多使用一次，目标是配对数最多，便是这个模型。完美匹配要求所有点都配对，先检查点数为偶数，再判断返回值是否为 `n/2`。匹配的大小是边数，覆盖点数是其两倍。若每个点可使用多次，或选择一组对象的合法性不能拆成两两配对，需重新建模。

相对当前匹配，一条交替路上的边轮流是未匹配边、匹配边。两端都未匹配的交替路叫增广路，它首尾都是未匹配边；把路上选中和未选中的边互换，内部点仍各匹配一次，两端各新增一次匹配，边数净增一。反过来，当前匹配与一个更大匹配的对称差由交替环、交替路组成，其中必有一条增广路。这就是“找不到增广路便已最大”的依据。

普通交替路搜索在奇环上会错误地区分左右层。Blossom 发现同一交替树中两个外点相连时，把这段奇环收缩成一个花；任意穿过花的增广路都能在展开后恢复成原图增广路，因此收缩不改变“是否存在增广路”。找到未匹配端点后沿 `p` 翻转匹配，匹配数增加一；所有未匹配点都无法找到增广路时，由 Berge 定理得到最大匹配。

搜索从一个未匹配根出发，偶数层称外点，奇数层称内点。队列只扩展外点：沿一条未匹配边到达新点，若它未匹配就找到增广路；若已匹配，则连同它的匹配对象一起加入交替树，后者成为新的外点。遇到已经在树内的内点时不用扩展；遇到另一个外点时，两点到交替树公共祖先的路径加上这条边形成奇环。这里的公共祖先由 `p` 和 `mate` 交替向上跳得到，和原图预处理的 LCA 无关。

花的基点是这个公共祖先。沿奇环从基点到任一点有两种走法，长度奇偶不同，可以选出与外部路径接续的交替走法；这使花在搜索中能暂时充当一个外点。比如三角形 `1-2-3-1` 中已匹配 `2-3`，从未匹配的 `1` 出发，`2` 和 `3` 都有机会成为走出三角形的出口，固定一次普通 BFS 层数会漏掉其中一侧。缩花后保留所有出口，恢复增广路时再决定使用哪一侧。

当前实现不真的删除顶点或重建邻接表，而用 `base[x]` 表示缩点后的花。`markPath` 标记环上的两个分支并修正 `p`，将内部各点重新放入可扩展集合；因此最终沿 `p` 回溯时已能穿过花，调用者不必再手写一次展开。花内还可能含已收缩的花，判断同花和回溯时都必须经过 `base`，不能把它替换成搜索开始时的固定点号。

点编号为 `1..n`。`add(u,v)` 加无向边，自环忽略，平行边不改变答案；`maxMatching()` 返回匹配边数并填写 `mate`，其中 `mate[u]==0` 表示未匹配。恢复方案时只输出满足 `u < mate[u]` 的边。每次调用都会从空匹配重算；简单图或平行边已去重时复杂度为 $O(n^3)$，保留大量重复边会增加邻接表扫描，不能继续仅按点数估算。稠密大图和带权题要另选实现。

典型调用为 `GeneralMatching gm(n)`，加入所有合法配对后令 `int ans=gm.maxMatching()`。若输入点号从零开始，建图时统一加一，输出匹配端点时再减一。存图空间为 $O(n+m)$；同一对端点出现很多重复边时宜先去重，否则重复扫描会放大实际耗时。删除部分点重算时，可不加入与这些点相连的边，但完美匹配的目标大小应按剩余点数计算。

当前板只解决无权最大匹配。最大权一般图匹配不能按权排序后反复调用无权板；稳定婚姻、一般图完美匹配计数也都是不同问题。

#link("https://judge.yosupo.jp/problem/general_matching")[Library Checker · General Matching]
