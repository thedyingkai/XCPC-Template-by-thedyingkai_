==== 先分清要删什么、缩什么

有向图中的强连通分量把“互相可达”的点缩成一个点，结果一定是 DAG，适合消掉环后做拓扑 DP。无向图则按删边或删点区分：桥是删去后增加连通块数的边，割点是删去后增加连通块数的点；边双连通分量之间由桥连接成森林，点双连通分量通过割点相交，并可进一步建成圆方森林。

Tarjan DFS 中，`dfn[u]` 是首次访问次序，`low[u]` 是不经过父边、从 `u` 的 DFS 子树能回到的最早祖先。对树边 `u-v`，若 `low[v] > dfn[u]`，子树没有别的边绕回去，这条边就是桥；若 `low[v] >= dfn[u]`，从 `v` 子树离开必经 `u`，于是 `u` 是割点或产生一个点双。根没有父边，只有至少两个 DFS 儿子时才是割点。

SCC 的 `low` 只允许通过仍在 Tarjan 栈中的点更新；桥和边双必须按边编号跳过父边的反向边，不能只写 `v != parent`，否则平行边会被误判。点双中的割点可以属于多个分量，所以不能给每个点强行存唯一点双编号。

这些板子都按 `1..n` 编号，并需要对每个尚未访问的点启动一次 `tarjan`。原图不保证连通时，缩点结果是 DAG，桥树和圆方树则都是森林；接 LCA 前必须先判两点是否在同一棵树中。
