- 输入可离线的加边、删边和连通性询问时间序列，线段树分治配合可撤销并查集，按询问出现顺序返回两点是否连通。
- 在回滚并查集中补充块大小、块数等信息后，可在叶子回答相应统计；动态二分图还需改成维护奇偶关系和矛盾数的可撤销并查集，不能只改叶子输出。
- 只有加边直接用并查集；只有删边通常倒序处理。

一条边从加入到删除之前一直有效，可以先把操作序列转换为若干个“这条边在时间区间内存在”的记录。删除困难的原因是并查集已经把连通块合在一起，无法知道删掉这一条边后该如何拆开。线段树分治不在原时间顺序中直接删除：把有效区间分解到时间线段树的节点上，递归进入节点时加边，退出时撤销本次递归造成的变化。

若某条边的有效区间包含时间 `t`，区间分解后恰有一个保存这条边的节点位于根到叶子 `t` 的路径上；不包含 `t` 的边则不会出现在这条路径上。因此走到叶子时，并查集恰好加入了该时刻全部有效边。每条边只存到 $O(log q)$ 个节点，避免了对每个时刻都从头建立整个图。子递归开始前保存 `snapshot()`，返回时 `rollback(snap)`，兄弟区间就不会受到彼此边集合的影响。

可撤销并查集只按大小合并，不使用路径压缩。小根挂到大根后，一个点每增加一层深度，所属集合大小至少翻倍，所以 `find` 最坏为 $O(log n)$。一次成功合并只改小根父亲、大根大小与块数，记录旧值就能常数时间撤销；路径压缩会额外修改沿途许多父亲，当前历史记录无法恢复这些变化。合并已经连通的两点不写历史，所以回滚到保存的历史长度，不能靠“执行了几次 unite”决定撤销几次。

==== 接口与坑点

- 按时间调用 `add`、`erase`、`query`，最后调用一次 `solve()`；返回值只对应 `query`，顺序不变。
- 同一无向边可重复加入；`erase` 与最近一次未删除的 `add` 配对。删除不存在的边会触发断言。
- 加入时间为 `l`、删除操作时间为 `r` 时，有效区间是 `[l, r-1]`。

构造 `DynamicConnectivity(n)` 后顶点用 `1..n`，操作下标由加入序列的顺序从 `0` 自动编号。依次 `add(1,2)`、`query(1,3)`、`add(2,3)`、`query(1,3)`、`erase(1,2)`、`query(1,3)`，`solve()` 返回 `[0,1,0]`。直到操作结束仍未删去的边，其有效区间延伸到最后一个时刻。每次 `add` 和 `erase` 也占一个时间位置，返回答案却只包含询问，不要把答案下标直接当成操作下标。

同一条边的多次加入在代码里视为多份副本，删除最近加入且尚未删除的一份，只要还剩一份就仍然连通。端点顺序会统一成较小编号在前，因此 `add(1,2)` 与 `erase(2,1)` 可以配对；若题面是按边编号删除，或者两条平行边还带有不同属性，应改为按边编号配对，不能继续只用端点作键。

设总操作数为 $q$、有效区间数为 $A$，线段树保存边的总量为 $O(A log q)$；合并与连通性查询各用 $O(log n)$，总时间为 $O((A log q+q)log n+q log q)$，最后一项包含用有序映射配对边的开销。空间为 $O(n+q+A log q)$，当前并查集历史在一条递归路径上至多保留 $n-1$ 次成功合并。离线要求预先知道加删时刻，强制在线的解密操作不能直接交给这份板子。

==== 常改

- 查询连通块大小、数量或权值：把信息和修改记录加入可撤销并查集，在叶子读取答案。
- 动态二分图：维护到根的奇偶关系和矛盾数。
- 有效区间由题面直接给出时，跳过 `add/erase` 配对，直接加入对应时间区间。

改成动态二分图时，让每条边要求 `color[u] xor color[v]=1`。两点不在同一块时，按这个等式计算挂接根之间的奇偶关系；两点已经同根时，仍要检查原有关系是否满足等式，不满足就增加矛盾数。此时“合并失败不写历史”不能照搬：没有发生根合并也可能改变了矛盾数，这类修改同样必须记录并撤销。在时间叶子看全局矛盾数是否为零，便可判断整个图是否二分图；只检查某个连通块则需把矛盾信息放到相应根上。

维护分量贡献时，也把总答案纳入回滚。例如每块贡献是某个函数 `f(size)`，合并前先从全局答案扣去两块的旧贡献，合并后加入新贡献，历史中保存恢复所需的旧值。换成其他结构做线段树分治，仍需满足加入顺序不影响目标状态，且每次修改可以可靠撤销；本板中边的加入顺序不影响连通性，才可以沿时间线段树的遍历顺序重新组织它们。

- 洛谷 P5787「二分图」需按题面把边直接加入 `[l,r-1]`，在回滚并查集中维护奇偶权和矛盾数，并在每个时间叶子输出整图是否二分图。
- Codeforces 1140F 需在根上维护左右点数、分量贡献及其全局和，并在每个时间叶子直接保存全局和。
