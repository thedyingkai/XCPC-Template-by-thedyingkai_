给定左右两部点和允许配对的边，最大匹配要求每个点至多使用一次，并让配对数尽量大。分配、DAG 最小路径覆盖、偏序链覆盖等题，若“一个对象只能占一个左端和一个右端”，通常都能先拆成二分图再套本板。

匹配还能增大，当且仅当存在一条从未匹配左点出发、在未匹配边和匹配边之间交替、最终到达未匹配右点的增广路。沿路翻转边的匹配状态会恰好多出一条匹配边；不存在增广路时由 Berge 定理可知已经最大。Hopcroft--Karp 每轮 BFS 只保留最短增广路层次，再沿这些层次同时增广一批互不冲突的最短路，复杂度为 $O(E sqrt(V))$。代码用显式路径栈完成分层搜索，增广路很长时不会占用递归调用栈。

==== 接口、方案和转化

- 左部点为 `1..n`，右部点为 `1..m`；`add(u,v)` 加一条允许匹配的边。`maxMatching()` 返回匹配边数，`ml[u]`、`mr[v]` 为匹配对象，`0` 表示未匹配。
- `minVertexCover()` 会先补跑最大匹配，再从所有未匹配左点沿“左到右非匹配边、右到左匹配边”搜索。返回的“左部未访问点 + 右部访问点”覆盖每条边，大小等于最大匹配，正是 Kőnig 定理给出的最小点覆盖。
- 恢复匹配边时枚举 `ml[u] != 0`；图修改后可以再次调用 `maxMatching()` 从现有匹配继续增广。

DAG 最小路径覆盖把每个原点拆成左右副本，原有向边从左副本连到右副本，答案为 `n - 最大匹配数`。这只允许路径使用原边；若题意允许链中相邻元素只需可达，必须先补传递闭包。二分图最大独立集是全部点减去最小点覆盖；点有容量时改最大流，边带权时改 KM 或最小费用流。一般图存在奇环时不能使用本板。

#link("https://judge.yosupo.jp/problem/bipartitematching")[Library Checker · Bipartite Matching]
