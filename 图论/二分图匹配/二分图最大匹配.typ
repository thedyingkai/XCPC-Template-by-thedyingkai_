==== 用途

- 输入二分图两侧的点和允许配对的边，求不重复使用端点的最大配对数，并返回每个点的匹配对象。
- 同一最大匹配还能恢复一组最小点覆盖；常用于分配、DAG 路径覆盖和偏序链覆盖。

==== 接口

- 左部点为 `1..n`，右部点为 `1..m`；`add(u, v)` 从左部向右部加边。
- `maxMatching()` 返回最大匹配数；`ml[u]`、`mr[v]` 为匹配点，`0` 表示未匹配。
- `minVertexCover()` 返回一组最小点覆盖的左右部点集，并会先补跑最大匹配。
- 当前实现为 Hopcroft--Karp，复杂度 $O(E sqrt(V))$，不适用于一般图匹配。

==== 常见转化

- DAG 最小路径覆盖：每点拆成左右两点，原边从左连右，答案为 `n - 最大匹配数`；按 `ml/mr` 恢复路径。
- 上式求的是使用原 DAG 边的点不相交路径覆盖；若题目允许链中相邻元素只需可达，建二分图前必须先求传递闭包。
- 偏序集的链使用可比关系连边。Dilworth 定理给出“最小链覆盖数 = 最大反链大小”，不能把它与只使用原边的 DAG 路径覆盖混为一谈。
- 二分图最小点覆盖大小等于最大匹配数；最大独立集为全部点减去一组最小点覆盖。
- 点有容量时改最大流；带权匹配改 KM 或最小费用最大流。
- 洛谷 P3386「二分图最大匹配」；P2764「最小路径覆盖问题」。
