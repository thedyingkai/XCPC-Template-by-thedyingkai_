- 输入无向带权图，按边权依次合并连通块，得到一棵记录各次合并阈值的重构森林。
- 原点的 LCA 可回答最小化最大边或最大化最小边的瓶颈值；沿祖先跳转可得到给定阈值下的连通块及其大小。

==== 接口

- 原点为 `1..n`。构造参数 `ascending=true` 时按边权升序合并；每次两个连通块合并，新建父节点，`val` 记录本次边权，整棵重构森林至多有 `2*n-1` 个点。
- `lca(u,v)` 返回重构树 LCA，不连通返回 `0`；`mergeValue(u,v)` 返回其权值，不连通返回 `nullopt`。
- 升序时，`mergeValue(u,v)` 是所有原图路径中“最大边权”的最小值；降序时是所有路径中“最小边权”的最大值。查询同一点会得到叶子的正负无穷哨兵，通常应单独处理。
- `componentNode(u,limit)` 返回只保留边权 `<= limit`（升序）或 `>= limit`（降序）时，包含 `u` 的重构树节点；`leaves[node]` 是该连通块原图点数。

==== 选用与坑点

- 所有阈值询问已知时，按阈值排序后直接用并查集更短；在线反复查询、还需连通块子树统计时再建重构树。
- 相同权值的合并顺序可以任意，但阈值查询必须向上跳到满足条件的最高祖先，不能停在第一个同权父亲。
- 升序构造与最小生成树瓶颈性质衔接；降序构造可处理最大瓶颈路。
