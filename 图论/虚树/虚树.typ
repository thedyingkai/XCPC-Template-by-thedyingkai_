原树很大、每次询问只涉及 $k$ 个关键点时，虚树保留这些关键点及维持祖先关系所必需的 LCA，把一次树上 DP 的规模从 $n$ 压到 $O(k)$。常见目标包括连接关键点的代价、关键点对距离和，以及只由少量特殊点触发的路径贡献。

能否压缩取决于被删掉的长链是否能用少量信息代替。在关键点和分叉点之间，中间点既不是本次询问的对象，也不产生新的关键分支；若整段链对 DP 的作用可合成长度、最小边权、最大值或一个可复合的转移，就能只保留两端。若每个普通点还有本次必须逐个处理的独立贡献，需先预处理成路径查询或合并转移，不能因为它不在关键点集中就直接删掉。

关键点按 DFS 序排序后，只需补相邻关键点的 LCA；这些点已经包含所有会改变压缩树分叉的位置。再次排序去重，用栈维护当前祖先链，栈顶就是新点在虚树中的父亲。虚树边代表原树上的一整段祖先路径，因此默认权值是深度差。HLD 预处理完成后，一次构造的时间为 $O(k log k+k log n)$，临时空间为 $O(k)$；对象的邻接表仍开到 $O(n)$，构造时另需清理上一次虚树涉及的点和边。

相邻 LCA 足够的原因是 DFS 序中每棵子树连续。若某个祖先有至少两个儿子子树含关键点，从一个非空儿子子树走到下一个非空儿子子树时，交界处的两个相邻关键点的 LCA 正是这个祖先。因而全部必要分叉都能补齐，其他非关键祖先只位于可压缩长链上。去重后若原有 $k>0$ 个不同关键点，最多补 $k-1$ 个点，总点数不超过 $2k-1$。

例如根为 `1`，原树有 `1-2,2-4,2-5,1-3,3-6`，关键点为 `4,5,6`。需要补入 `2` 和 `1`，虚树边为 `1->2,2->4,2->5,1->6`；最后一条代表原路径 `1-3-6`，长度为 `2`。点 `3` 在本次查询中没有新的分支，所以可以省掉；点 `2` 即使不在关键点集中，也必须留下。

==== 接口和状态

先在同一根下建立 `HLD`，再调用 `build(key)`；返回值是虚树根，`nodes` 包含关键点和补入的 LCA，`g` 只存父到子的边。构造会清空上一次 `nodes` 涉及的邻接表，但关键点标记、出现次数和 DP 数组仍需调用者按 `nodes` 清理。空关键点集返回 `0`。

具体顺序是原树建好后建立 `HLD hld(n,root,g)`，再建立 `VirtualTree vt(n,hld)`；每次输入关键点，记录其标记或出现次数，再令 `r=vt.build(key)`。`vt.nodes` 按原树 DFS 序排列，父亲总在儿子前面，所以逆序枚举它并汇总 `vt.g[u]` 中各儿子的答案，就能完成自底向上的 DP，不一定要再递归。处理结束只清理本次 `vt.nodes`，不要每次把长度为 `n` 的 DP 数组全部清零，否则多次小询问仍可能退化成 $O(q n)$。`HLD` 引用原邻接表，虚树又引用 `HLD`，两者在全部查询期间都必须存活。

若原树有边权，把虚树边权改成 `rootDist[child]-rootDist[parent]`；若 DP 强制包含固定根，把根也加入关键点。重复关键点在结构上会去重，若题意把它们当作多个对象，另存出现次数作为点权。

当前边权类型为 `int` 且保存无权深度差，改成加权距离时邻接表的第二项也要按范围改为 `i64`。若虚边表示链上最小边权，则用树链剖分或倍增查询这条原树路径的最小值，不能用深度差代替。根加入结构不代表它自动成为一个关键对象；对距离和、数量统计等问题，结构点和关键点标记应分开维护。

设虚树子树含 `cnt[v]` 个关键对象、总数为 `k`，父边长度为 `w`，所有无序关键点对距离和中该边贡献 `w*cnt[v]*(k-cnt[v])`。这类“每条边被多少对象对跨过”的计数，是把原题改写成虚树 DP 的常用入口。

这个式子来自删边后的两侧划分：一侧有 `cnt[v]` 个对象，另一侧有 `k-cnt[v]` 个，每对跨侧对象的路径都经过这条边。相同点出现多次时用出现次数初始化 `cnt`，同点对象间距离自然为零。若统计有序点对，把最终答案乘二；乘法先提升到足够宽的整数类型。原边权非负时，连接所有关键点的最小子树长度等于满足 `0<cnt[v]<k` 的虚边长度之和；若额外加入了关键点共同祖先上方的固定根，这个条件会排除不需要的上方链。

另一类题要求以最小代价切断固定根到所有关键点的连接，设原边权是非负删除代价且根不是关键点。把根加入虚树，并把虚边权改为对应原链的最小边权。令 `dp[u]` 表示不切断 `u` 上方连边时，在它下方完成隔离的最小代价。对于儿子 `v`，若 `v` 是关键点，就必须切断这条虚边代表的链，贡献其最小边权；否则贡献 `min(链上最小边权,dp[v])`，分别对应在上方切一次或继续到分支内部切。各儿子贡献相加即得 `dp[u]`，答案为 `dp[root]`。路径长度在这个模型中没有用，决定能否正确改板的正是虚边保存的信息。
