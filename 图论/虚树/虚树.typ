原树很大、每次询问只涉及 $k$ 个关键点时，虚树保留这些关键点及维持祖先关系所必需的 LCA，把一次树上 DP 的规模从 $n$ 压到 $O(k)$。常见目标包括连接关键点的代价、关键点对距离和，以及只由少量特殊点触发的路径贡献。

关键点按 DFS 序排序后，只需补相邻关键点的 LCA；这些点已经包含所有会改变压缩树分叉的位置。再次排序去重，用栈维护当前祖先链，栈顶就是新点在虚树中的父亲。虚树边代表原树上的一整段祖先路径，因此默认权值是深度差。

==== 接口和状态

先在同一根下建立 `HLD`，再调用 `build(key)`；返回值是虚树根，`nodes` 包含关键点和补入的 LCA，`g` 只存父到子的边。构造会清空上一次 `nodes` 涉及的邻接表，但关键点标记、出现次数和 DP 数组仍需调用者按 `nodes` 清理。空关键点集返回 `0`。

若原树有边权，把虚树边权改成 `rootDist[child]-rootDist[parent]`；若 DP 强制包含固定根，把根也加入关键点。重复关键点在结构上会去重，若题意把它们当作多个对象，另存出现次数作为点权。

设虚树子树含 `cnt[v]` 个关键对象、总数为 `k`，父边长度为 `w`，所有无序关键点对距离和中该边贡献 `w*cnt[v]*(k-cnt[v])`。这类“每条边被多少对象对跨过”的计数，是把原题改写成虚树 DP 的常用入口。
