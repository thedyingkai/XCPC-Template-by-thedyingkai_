==== 用途

- 输入一棵固定的有根树和本次的 $k$ 个关键点，构造只含关键点及必要 LCA 的虚树，并保留祖先关系和路径长度。
- 在这棵压缩后的树上做 DP，可处理关键点间的连接代价、距离统计和路径贡献；当前实现单次 $O(k log n)$。

==== 接口与坑点

- 先在同一棵有根树上建立 `HLD`；`build(key)` 返回虚树根。
- `nodes` 含补入的 LCA；`g[u]` 只存父亲到儿子的边，边权是深度差。
- `build` 只清空上次虚树的邻接表；关键点标记和 DP 状态由调用者按 `nodes` 清理。

==== 常改与 trick

- DP 需要原树路径权和时，虚树边权取 `dist[child] - dist[parent]`。
- DP 必须包含固定根时，把固定根加入关键点。
- 关键点按 `dfn` 排序，只需补相邻点的 LCA，再排序去重。
- 设关键点总数为 `k`，虚树子树中的关键点数为 `cnt[v]`，父边长度为 `w`。所有无序关键点对距离和中，该边贡献 `w * cnt[v] * (k-cnt[v])`。
- 连接全部关键点的最小原树子树长度，只累加满足 `0 < cnt[v] < k` 的虚树父边；额外加入固定根时，该条件会排除根外多出的路径。
- 输入关键点可能重复时，虚树节点需去重，但题目把重复出现视为不同对象时，应另存出现次数作为点权。

- 洛谷 P2495「消耗战」：加入根 `1`，并把虚树边权改为原树两端路径上的最小边权；当前深度差不能直接使用。
