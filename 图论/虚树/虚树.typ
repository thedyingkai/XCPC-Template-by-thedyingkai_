==== 选用

- 原树固定，每次只处理 $k$ 个关键点；在关键点及其 LCA 构成的虚树上 DP，当前实现单次 $O(k log n)$。

==== 接口与坑点

- 先在同一棵有根树上建立 `HLD`；`build(key)` 返回虚树根。
- `nodes` 含补入的 LCA；`g[u]` 只存父亲到儿子的边，边权是深度差。
- `build` 只清空上次虚树的邻接表；关键点标记和 DP 状态由调用者按 `nodes` 清理。

==== 常改与 trick

- DP 需要原树路径权和时，虚树边权取 `dist[child] - dist[parent]`。
- DP 必须包含固定根时，把固定根加入关键点。
- 关键点按 `dfn` 排序，只需补相邻点的 LCA，再排序去重。

- 洛谷 P2495「消耗战」：加入根 `1`，并把虚树边权改为原树两端路径上的最小边权；当前深度差不能直接使用。
