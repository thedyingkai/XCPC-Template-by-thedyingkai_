在有向图中固定源点 `s`。若每一条从 `s` 到 `v` 的路径都经过 `u`，就称 `u` 支配 `v`。立即支配点 `idom[v]` 是除 `v` 外最靠近它的支配点；所有立即支配关系组成支配树。因此“从起点到某状态无论怎样走都绕不开哪些点”，可以直接变成支配树上的祖先链。

例如有边 `s->a,s->b,a->c,b->c,c->d`，到 `c` 可以绕过 `a`，也可以绕过 `b`，故 `idom[c]=s`；到 `d` 必须经过 `c`，故 `idom[d]=c`。支配树中 `a,b,c` 都是 `s` 的儿子，`d` 是 `c` 的儿子。原图可以有环，支配树仍是一棵树；将原图缩成 SCC 会丢掉分量内部的必经点，不能代替求支配树。

一个点的全部严格支配点必在 DFS 树上从根到它的祖先链中，因为 DFS 树上的这条路径也是原图路径。对这些支配点按经过顺序排列，最后一个就是立即支配点。因此支配树上 `u` 的子树恰好是从源出发必须经过 `u` 的可达点：删除 `u` 后，这些点失去可达性，其他原本可达的点仍至少有一条绕开 `u` 的路径。若不统计被删点自身，子树大小需减一；原本不可达的点不在树中。

Lengauer--Tarjan 只处理从 `s` 可达的点。对于非根点 `v`，考虑所有从 `u` 到 `v`、且内部点 DFS 序都大于 `dfn[v]` 的路径；这些 `u` 中 DFS 序最小的点就是 `v` 的半支配点。算法按 DFS 序倒序处理，用并查集的 `eval` 找出祖先链上半支配点 DFS 序最小的候选，再计算并修正立即支配点 `idom`。DFS 树用于组织计算，最终的支配关系仍取决于原图中的所有有向路径。

半支配点只要求存在一条满足 DFS 序条件的路径，它未必支配 `v`。倒序处理 `w` 时，DFS 序更大的点已经有 `semi`，算法遍历原图中所有指向 `w` 的边；若前驱已被接入并查集，`eval` 就沿已处理的 DFS 祖先段取半支配序最小的代表，将一整段路径的候选压成一次查询。这里 `ancestor` 连接的是 DFS 父子关系，`label` 保存路径上的最优候选，不能换成普通按大小合并的并查集。

算出 `semi[w]` 后，将 `w` 放进 `bucket[semi[w]]`。等 DFS 祖先链上的相关区段都已接好，再处理这个桶，才能用 `eval` 判断立即支配点是半支配点本身，还是另一个候选点的立即支配点。第二种情形先在 `dom` 中留下候选，最后按 DFS 序正序执行 `dom[w]=dom[dom[w]]` 消去间接引用。省略最后一遍修正，会把半支配关系或中间候选误当成最终支配关系。这份路径压缩的简化实现时间为 $O((n+m) log n)$，空间为 $O(n+m)$，两遍 DFS 均使用显式栈。

`build(s)` 返回 `idom`：根的立即支配点为 `0`，不可达点为 `-1`；`tree` 是支配树。`dominates(u,v)` 用支配树 DFS 序判断祖先关系，`u==v` 也视为支配。换源后支配关系整体变化，必须重新 `build`。

调用时用 `DominatorTree dt(n)` 建立点号 `1..n` 的图，逐条 `dt.add(u,v)` 加有向边，然后 `dt.build(s)`。例如多次问删除 `u` 后原本可达的 `v` 是否还能到达，答案就是 `!dt.dominates(u,v)`，端点删除按题意另判；若问有多少点被某个点阻断，在 `dt.tree` 上统计子树大小即可。`dominates` 单次 $O(1)$，但不能据此直接回答“同时删掉两个点”后是否还能到达。

所有从 `s` 到 `v` 的路径必经点恰为支配树上 `s` 到 `v` 的链，可继续接 LCA、树链剖分或树上差分。若题目问必经边，可把每条原边拆成一个边点再求点支配，但图规模会变成 $O(n+m)$；单纯问“删点后是否还可达”时，也要留意询问点本身是否按题意计入。

若问从每个点走向固定终点 `t` 的所有路径必经哪些点，把原图全部边反向，以 `t` 为源求支配树。多个起点允许任选一个出发时，加超级源连向所有允许的起点，再求一次；这描述的是这些起点路径的并集，不能拿一棵树分别回答每个起点的支配关系。

Library Checker 要求输出 `p[s]=s`，而当前 `build(s)` 返回的 `idom[s]=0`；提交时只需把源点这一项改成 `s`，其余可达点直接输出 `idom`。

#link("https://judge.yosupo.jp/problem/dominatortree")[Library Checker · Dominator Tree]
