==== 用途

- 输入有向图和固定源点 `s`，建立支配树，回答哪些点是从 `s` 到某点的所有路径都绕不开的必经点。
- 运行后得到每点的立即支配点和支配树，可继续做必经点计数、祖先判断和树上路径操作。

==== 定义与接口

- 在指定源点 `s` 的有向图中，若每条 `s -> v` 路径都经过 `u`，则 `u` 支配 `v`。支配关系依赖源点，换源必须重新 `build`。
- `build(s)` 使用 Lengauer--Tarjan 算法建立支配树；`idom[v]` 是立即支配点，根为 `0`，从 `s` 不可达的点为 `-1`。
- `tree` 是支配树，`dominates(u,v)` 用其 DFS 序判断支配关系；`u == v` 时也返回真。

==== 使用场景与坑点

- 所有从 `s` 到 `v` 的路径必经点，恰为支配树上 `s` 到 `v` 的祖先链；批量计数、路径加可继续接 LCA 或树链剖分。
- 支配不等于原图中的祖先关系，也不能用普通割点替代；割点忽略路径方向和固定源点。
- 当前求的是点支配。边的必经性可先把原边拆成边点，再在拆点图上求支配关系。

- 洛谷 P5180「支配树」。
