差分约束处理一组形如 $x_v-x_u<=c$ 的不等式，判断它们能否同时成立，并在可行时给出一组变量取值。排期先后、位置差、区间上下界等条件只要能化成两个变量之差的界，就可把变量视为点，把不等式视为一条最短路边。

$x_v-x_u<=c$ 等价于 $x_v<=x_u+c$，正好是边 `u -> v`、边权 `c` 的松弛条件。所有点同时以距离 `0` 开始，相当于超级源向每点连零边；若还能不断降低某个距离并形成至少 `n` 条边的改进链，就必然经过负环，而负环表示把一圈不等式相加后得到矛盾。

==== 建模和接口

- `addLeq(u,v,c)` 加 $x_v-x_u<=c$；`addGeq(u,v,c)` 加 $x_v-x_u>=c$；`addEqual` 同时加入两个方向。
- 整数变量上的严格不等式 $x_v-x_u<c$ 改成 $x_v-x_u<=c-1$。常数界可增加一个基准变量后照常建边。
- `solve()` 返回“是否可行”和一组 `i128` 解。约束通常允许整体平移；若基准变量必须为 `0`，把所有返回值减去该基准变量的值。

当前实现是队列优化 Bellman--Ford，常见数据较快，但最坏复杂度仍为 $O(n m)$；不能因为某道普通最短路边权非负，就把 Dijkstra 塞进这份板子并把它当作差分约束。只有差值等式时，带权并查集通常更短；出现不等式或需要判断负环时再用本板。
