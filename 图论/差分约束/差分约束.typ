==== 用途

- 输入若干形如 $x_v-x_u<=c$ 的差分不等式，判断这些约束能否同时满足，并在可行时给出一组变量取值。
- 排期先后、位置差、上下界等关系可转成最短路边；约束图存在负环当且仅当无解。

==== 建边

- `addLeq(u,v,c)` 加入 $x_v-x_u<=c$，对应最短路边 `u -> v`、边权 `c`。
- $x_v-x_u>=c$ 等价于 $x_u-x_v<=-c$，调用 `addGeq(u,v,c)`；等式调用 `addEqual` 加两个方向。
- 整数变量上的 $x_v-x_u<c$ 改成 $x_v-x_u<=c-1$；常数上下界可增加一个基准变量再按同样规则建边。

==== 接口与坑点

- `solve()` 返回是否可行以及一组 `i128` 解；所有点初始距离为 `0`，等价于超级源向每点连零边。
- 当前用队列优化 Bellman--Ford 判负环；约束含负边，不能改用 Dijkstra。
- 约束通常允许整体平移；若基准变量必须为 `0`，将返回的所有值减去该变量的返回值。
- 只有差值等式时用带权并查集更直接；出现不等式后再用差分约束。

- 洛谷 P5960「差分约束算法」。
