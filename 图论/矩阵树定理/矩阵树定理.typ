矩阵树定理把“图中有多少棵生成树”转成一个拉普拉斯矩阵余子式的行列式。边权不是距离，而是选择这条边的方案权；因此结果是每棵树边权乘积之和，普通计数把全部权设为 `1`。平行边会自然累加，自环不会进入生成树。

生成树必须包含全部顶点，且连通、无环，恰有 $n-1$ 条边。如果题目只要求给定关键点连通，或要求生成树总边权最小，这个行列式不能直接回答；前者允许省略普通点，后者还带优化条件。题目中每对端点有若干条可区分连接方式时，可把方式数作为边权；选中的每条边各独立选择一种方式，总方案数正是树上边权乘积。

无向边 `(u,v,w)` 给两个对角元各加 $w$，给两个非对角元各减 $w$。这样得到的拉普拉斯矩阵 $L=D-A$，每行之和为零，直接求整张矩阵的行列式必为零。删除任意同编号的一行一列后，行列式展开中只有选出一棵生成树的项不会相互抵消，所以余子式就是加权生成树数。图不连通时余子式必为零。

例如无权三角形的对角元均为 `2`，非对角元均为 `-1`；删掉第一个点对应的行列，得到二阶矩阵 `[[2,-1],[-1,2]]`，行列式是 `4-1=3`，恰好对应删除三条边中的任意一条。若三条边权分别为 $a,b,c$，结果变成 $a b+a c+b c$，而不是三棵树的边权和。一点图有一棵空边集生成树，代码返回 `1`。

有向图必须先看清方向：`countInArborescence(root)` 统计每个非根点选一条出边、最终都指向根的内向树，使用出度拉普拉斯矩阵；`countOutArborescence(root)` 统计从根能到所有点的外向树，使用入度拉普拉斯矩阵。两者颠倒是最常见的 WA，建议用两三个点的单向链手算核对。

对原有向边 `u->v`，内向版本修改 `L[u][u]+=w,L[u][v]-=w`，外向版本修改 `L[v][v]+=w,L[v][u]-=w`，再删除根的行列。内向树中根出度为零、其他点出度为一，外向树中根入度为零、其他点入度为一；还必须满足沿树方向的全体可达性，单独满足度数可能留下与根分离的有向环，行列式会消去这些选择。例如只有边 `1->2->3` 时，以 `1` 为根的外向树数为一、内向树数为零；以 `3` 为根则相反。与无向情况不同，有向答案通常随根而变。

点编号为 `1..n`。`countUndirected(root)` 中删除哪一个点不影响无向答案；三类接口都默认模 `1000000007`，可写成 `countUndirected<Mod>` 等改模数。当前行列式通过费马逆元消元，`Mod` 必须是质数；若题目要求合数模或精确整数，必须换成无除法消元或分解模数。复杂度为 $O(n^3)$，大规模稀疏图不能靠稀疏存边绕过行列式瓶颈。

消元时每列找非零主元，交换行会使行列式变号；用主元逆元消去下方元素后，答案为各主元乘积再乘行交换符号。某列找不到非零主元时，当前模数下行列式为零。这只说明方案数模 `Mod` 为零，不能反推出原图没有生成树；检查是否存在应另做连通性或对应方向的可达性判断。矩阵空间为 $O(n^2)$，每次计数都会重新建立矩阵并消元。

建立 `MatrixTree mt(n)` 后，无向图逐条 `mt.addUndirected(u,v,w)`，有向图逐条 `mt.addDirected(u,v,w)`，最后选择对应的计数入口。两类边分开保存，无向计数不会读取已加的有向边，反之亦然。若规定一些无向边必须出现在树中，先检查这些边是否成环；成环则无解，否则把它们缩成连通块，对剩余边建立缩点后的图再计数，已指定边的方案权乘积另乘到答案上。被缩成自环的其他边不能再选。

Library Checker 的两题模数都是 `998244353`：无向题调用 `countUndirected<998244353>`；有向题要求根能到达所有点，必须调用 `countOutArborescence<998244353>(root)`。不能沿用默认的 `1000000007`，也不能把有向题写成统计指向根的 `countInArborescence`。

#link("https://judge.yosupo.jp/problem/counting_spanning_tree_undirected")[Library Checker · Counting Spanning Tree Undirected]

#link("https://judge.yosupo.jp/problem/counting_spanning_tree_directed")[Library Checker · Counting Spanning Tree Directed]
