==== 用途

- 输入无向图或有向图及每条边的方案权，通过 Laplacian 余子式计算生成树或指定根有向生成树的加权计数。
- 输出是模质数意义下的方案数，不会构造某一棵生成树；普通计数把所有边权设为 `1`。

==== 接口

- 点编号为 `1..n`；边权是该边的方案权，普通计数取 `1`。平行边会自然累加，自环不计入生成树。
- `countUndirected(root)` 求无向图生成树权值乘积之和；无向图删除任意同一行列所得余子式相同，`root` 只决定删除哪一行列。
- `countInArborescence(root)` 统计所有边指向根的内向树；`countOutArborescence(root)` 统计从根出发的外向树。两者不能混用 Laplacian 的入度、出度约定。
- 代码包含 `数学/线性代数/线性代数.cpp`，结果按其中的 `Matrix<i64>::MOD` 取模；当前高斯消元使用费马逆元，因此模数必须为质数。

==== 构造核对

- 无向边 `(u,v,w)`：`L[u][u] += w`、`L[v][v] += w`、`L[u][v] -= w`、`L[v][u] -= w`。
- 内向树使用出度 Laplacian；外向树使用入度 Laplacian。删除根对应的行和列后求行列式。
- 图不连通或指定根不可满足方向要求时，余子式行列式为 `0`。

- 洛谷 P6178「Matrix-Tree 定理」。
