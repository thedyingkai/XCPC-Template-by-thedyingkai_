给定点数较小的带权图，Floyd 一次求出任意两点间的最短距离，并可恢复具体路径。它允许负边，适合点对询问很多、状态图稠密，或转移天然写成“允许哪些中间点”的题；时间 $O(n^3)$，空间 $O(n^2)$。

令 `dist[i][j]` 表示只允许已经处理过的点作中间点时的最短路。加入中间点 `k` 后，一条最优路要么不用 `k`，要么由 `i -> k` 和 `k -> j` 两段组成，所以转移只有 `min(dist[i][j], dist[i][k]+dist[k][j])`。这也是 `k` 必须放在最外层的原因。`next[i][j]` 同步保存从 `i` 出发的下一点，因而能逐步恢复路径。

==== 接口和边界

- `add_edge(u,v,w)` 只加有向边，并保留平行边中的最小权；无向边需加两个方向。全部建边后调用 `run()`。
- `query(u,v)==INF` 表示不可达。`path(u,v)` 返回顶点序列；若不可达，或相关负环使最短路没有有限答案，则返回空数组。
- `has_negative_cycle()` 通过某个 `dist[i][i]<0` 判断图中存在负环。只有当 `s` 能到该负环且负环还能到 `t` 时，`s -> t` 才是负无穷。
- 为了把不可达与有限距离分开，代码取 `INF=LLONG_MAX/4`。每条边及所有有限最短距离都必须严格落在 `(-INF,INF)`；负环造成的更小值会饱和到 `-INF`，只用于继续判负环，不能当成精确距离。点数稍大时 $O(n^2)$ 内存和 $O(n^3)$ 时间会先成为瓶颈。

把加法改为逻辑与、最小值改为逻辑或可求传递闭包；把合并代价改为 `max` 可求“最小化路径最大边”。点按时间开放的题，按开放顺序加入中转点，并在回答前确认询问两端也已开放。
