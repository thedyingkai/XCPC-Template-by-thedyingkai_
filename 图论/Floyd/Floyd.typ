==== 选用

- 多源最短路，$O(n^3)$ 时间、$O(n^2)$ 空间；允许负边。

==== 接口与坑点

- `add_edge(u, v, w)` 只加有向边，并保留重边最小值；加完边后调用 `run()`。
- `query(u, v) == INF` 表示不可达。
- `has_negative_cycle()` 只判断图中是否存在负环；判断 `s` 到 `t` 的答案是否为负无穷，还需检查负环是否从 `s` 可达且能到达 `t`。
- 转移时 `k` 必须在最外层。

==== 常改

- 传递闭包：`can[i][j] |= can[i][k] && can[k][j]`。
- 最小化路径最大边：`d[i][j] = min(d[i][j], max(d[i][k], d[k][j]))`。
- 点按时间开放：按开放顺序加入中转点；回答前还要检查询问两端均已开放。

- 洛谷 B3647「Floyd」；洛谷 P1119「灾后重建」。
