给定一张非负边权有向图和源点，Dijkstra 求源点到每个点的最短距离，并用 `parent` 留下一棵最短路树。题目出现“总代价是沿路边权之和”“所有边权非负”“需要大量单源到点询问”时，可直接把状态视为点、合法转移视为边；无向边调用两次 `add`。

算法始终从堆中取当前距离最小的未确定点。由于边权非负，任何绕经尚未确定点的路线都不会把它再变小，因此该点出堆后距离已是最终值。松弛成功时记录前驱，沿 `parent` 反向走即可恢复一条最短路。

==== 接口和边界

- 点编号为 `1..n`。`add(u,v,w)` 要求 `0 <= w < INF`；`dij(src)` 会重置距离、访问标记和前驱。
- 不可达点的距离为 `INF`，`get_path(t)` 返回空数组。构造后尚未运行 `dij` 时，所有点同样视为不可达。
- 复杂度为 $O((n+m) log n)$，空间为 $O(n+m)$。`INF` 是不可达哨兵，所有需要保留的真实最短路都必须严格小于 `INF`。

负边会破坏“出堆即确定”的不变量；有负边且无负环用 Bellman--Ford，DAG 用拓扑序松弛。多源最短路把所有源以距离 `0` 同时入堆。若只求到一个终点，可在该终点第一次有效出堆时停止。

统计最短路条数时，距离变小时覆盖计数、相等时累加；但零权边会让“按距离递增做 DP”失去拓扑顺序，需要另行处理最短路子图中的零权结构。

#link("https://judge.yosupo.jp/problem/shortest_path")[Library Checker · Shortest Path]
