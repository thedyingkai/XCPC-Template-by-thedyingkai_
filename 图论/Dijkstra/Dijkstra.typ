==== 选用

- 单源、非负边权；稀疏图用堆优化 Dijkstra。
- 有负边不能用；DAG 直接按拓扑序松弛。

==== 接口

- `add(u, v, w)` 只加有向边；无向边加两次。
- `dij(src)` 会重置状态；不可达距离为 `INF`。
- `get_path(t)` 依赖最近一次 `dij`，不可达返回空数组。

==== 常改与 trick

- 多源：所有源点以距离 `0` 一起入堆。
- 正边权图统计最短路条数：变短时覆盖，相等时累加；含零权边时不能直接这样传递计数。
- 最短路上的边满足 `dist[u] + w == dist[v]`；有零权边时不能直接按距离做 DAG DP。
- 往返最短路：原图、反图各跑一次。

- 洛谷 P4779「单源最短路径（标准版）」。
