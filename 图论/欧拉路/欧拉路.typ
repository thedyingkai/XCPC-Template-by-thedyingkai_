==== 用途

- 输入有向图或无向图，寻找一条恰好使用每条边一次的欧拉路径或欧拉回路，并输出经过的顶点序列。
- 适合一次走完全部道路、还原边的排列，以及把字符串前后缀拼接转成边遍历的问题。

==== 判定

- 有向欧拉回路：所有点入度等于出度；欧拉路径：起点出度多 `1`，终点入度多 `1`，其余相等。
- 无向欧拉回路没有奇度点；欧拉路径恰有两个奇度点。
- 度数满足后仍需保证所有非孤立点处于同一对应连通块；当前板子通过检查是否用完全部边完成此项判定。

==== 接口与常改

- `Euler(n, true)` 为有向图，`Euler(n, false)` 为无向图；无向边只调用一次 `add`。
- `trail()` 自动选起点，`trail(start)` 指定起点；成功返回 `m+1` 个点，否则返回空数组。
- 字典序最小：按另一端点排序邻接表；有两个合法起点时显式调用 `trail(较小起点)`，不要用自动选点。
- 输出边编号或标签：栈中同时保存进入当前点的边编号，回溯时记录边。
- 字符串首尾拼接：前后缀作点，字符串作边。

- 洛谷 P7771「欧拉路径」；P2731「骑马修栅栏」。
