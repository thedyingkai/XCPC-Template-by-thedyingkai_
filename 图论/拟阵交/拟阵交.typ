==== 用途

- 输入同一地面集上的两个独立性判定，求同时在两个拟阵中独立的最大基数元素集合，并返回所选元素编号。
- 典型模型是“边集既无环又不重复颜色”的最大彩虹森林；结果达到 `点数-1` 时就是彩虹生成树。

==== Oracle 接口

- 地面集元素编号为 `0..n-1`。`matroidIntersection(n,first,second)` 返回同时属于两个拟阵的最大基数独立集。
- 每轮增广前会调用 `reset(chosen)`；`canAdd(in)` 判断 `I + in` 是否独立，`canExchange(out,in)` 判断 `I - out + in` 是否独立。Oracle 必须始终相对本轮传入的同一个 `I` 回答，不能在询问时永久修改状态。
- `常用拟阵.cpp` 提供划分拟阵和图拟阵：划分拟阵限制每组最多选择 `capacity[group]` 个元素；图拟阵把无向边集独立定义为森林。

==== 使用与复杂度

- 最大彩虹森林中，第一拟阵取图拟阵，第二拟阵按边颜色分组且每组容量为 `1`。
- 交换图中，第一拟阵的边从已选元素指向未选元素，第二拟阵的边方向相反；找到从“可直接加入第一拟阵”到“可直接加入第二拟阵”的路后，对整条路取对称差。
- 当前求无权最大基数拟阵交；最大权拟阵交需要最短增广路和权值势能，不能先按权排序再调用本板。
- 算法每轮最多检查 $O(n^2)$ 次交换。图拟阵 Oracle 在 `reset` 时建当前森林的 DFS 序，随后用“新边是否跨过被删树边的割”在 $O(1)$ 判断交换。
- `GraphicMatroidOracle` 的点编号为 `1..vertices`，边按传入数组的 `0` 下标编号；传给两个 Oracle 的地面集大小必须相同。
