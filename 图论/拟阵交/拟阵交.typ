拟阵交在同一地面集上给出两套“独立”约束，求同时满足两套约束的最大基数子集。最常见的竞赛模型是彩虹森林：所选边既不能成环，又不能重复颜色；若最后选到 `点数-1` 条，就是一棵彩虹生成树。它不是把两个普通贪心叠在一起，先按一种约束贪心往往会堵死另一种约束。

先确认每套约束确实是拟阵。空集必须合法，合法集合的子集也必须合法；对于两个合法集合 $A,B$，若 $|A|<|B|$，必须能从 $B$ 中找出一个不在 $A$ 中的元素加入 $A$，且仍合法。这条增广性质是算法保证，不是只要能写出合法性检查就能用。森林满足它，同组数量不超过容量也满足它；一般图的匹配、任意背包容量约束则不满足，不能直接充当一个拟阵。三套约束也不能机械增加第三个判定器。

设当前公共独立集为 $I$。本板先按元素顺序贪心取一个极大公共独立集，再从它开始增广；拟阵交允许从任意公共独立集出发，这一步只减少后续轮数，不会把局部极大误当成答案。未选元素若能直接加入第一拟阵，就可作为增广起点；若也能加入第二拟阵，直接选入即可。否则在交换图中交替考虑“按第二拟阵删一个已选元素后加入它”和“按第一拟阵删一个已选元素后加入另一个未选元素”。找到终点后对整条路取对称差，两套独立性都保持，集合大小增加一。若不存在增广路，拟阵交换性质保证当前集合已经最大。

交换图的点是地面元素，不是原图顶点。设 `x` 已选、`y` 未选：若 `I-x+y` 满足第一拟阵，就连 `x->y`；若满足第二拟阵，就连 `y->x`。起点集合是第一拟阵允许直接加入的未选元素，终点集合是第二拟阵允许直接加入的未选元素。这样一条路必定按“未选、已选、未选……”交替，起点和终点都是未选元素，翻转后净增加一个。代码用多源 BFS 找边数最少的增广路；最短路没有向前跳过中间点的交换边，拟阵交换定理据此保证整条路同时翻转仍然合法。不能随便用 DFS 找一条有绕路的路径后照样翻转。

例如第一套限制是“每种颜色最多一条”，第二套限制是“边不能成环”。某条蓝边可以作为起点，但加入后会在森林中形成环，于是可沿第二拟阵的交换边走到环内某条已选红边。删掉这条红边腾出了红色名额，再沿第一拟阵的交换边走到一条未选红边；若新红边连接当前森林的两个连通块，它便是终点。整次增广换掉一条边、加入两条边，解释了直接贪心不再能加边后，为什么仍可能扩大答案。

==== 独立性判定接口

全部候选元素构成地面集，编号为 `0..n-1`。两套约束各提供一个独立性判定器，每轮开始用同一个 `chosen` 调用它们的 `reset`；`canAdd(in)` 判断把 `in` 加入当前集合后是否独立，`canExchange(out,in)` 判断删去 `out`、加入 `in` 后是否独立。询问必须只相对这轮固定的 $I$ 回答，不能在一次判断中永久修改判定器的状态。

`常用拟阵.cpp` 提供划分拟阵和图拟阵。划分拟阵限制每组最多选 `capacity[group]` 个元素；图拟阵要求所选无向边构成森林，并用当前森林的 DFS 序在 $O(1)$ 判断“新边是否跨过删边形成的割”。图拟阵顶点编号为 `1..vertices`，两个判定器的候选元素顺序和数量必须完全一致。若原图点标号很大而实际只出现少量点，先把边端点离散化再传入；`reset` 会按 `vertices` 重建数组，直接拿标号上界当点数会浪费大量时间。

图拟阵的交换可由唯一环理解。新边两端原来不连通时，直接加入就合法，删去任意已选边后仍合法；两端已连通时，新边加上两端间的唯一路径恰成一环，必须删掉这条路径上的边。把待删边朝向 DFS 子节点 `child`，该边在两端路径上等价于两个端点恰有一个位于 `child` 子树中，因此一次子树区间判断就能完成交换检查。自环不能选，平行边作为不同候选保留，但不能同时选入同一森林。

彩虹森林的调用顺序是把每条候选边及颜色按相同顺序存入 `edges`、`group`，建立 `PartitionMatroidOracle first(group,vector<int>(颜色数,1))` 与 `GraphicMatroidOracle second(点数,edges)`，然后调用 `matroidIntersection(边数,first,second)`。返回值是候选边的编号数组；数组大小用于判定能否生成树，编号用于恢复方案。每组允许多条时只需改划分容量，无需改通用增广过程。

两边都是容量为 `0/1` 的划分拟阵时，调用 `unitPartitionMatroidIntersection(first,second)`。它仍返回所选地面元素编号，但把交换图压缩到划分组，在一张层次图中批量找最短增广路；设地面集大小为 $n$、两边组数之和为 $g$，复杂度为 $O((n+g)sqrt(g))$，空间为 $O(n+g)$。普通二分图的每条边可作为一个地面元素，左右端点分别作为两边的 `group`；容量为零的组不会选入元素。一般容量不能调用这个入口。

通用 `matroidIntersection` 每次增广最多枚举 $O(n^2)$ 个交换判断，最多增广到答案大小 $r$；独立性判断过慢或候选元素过多时，可能无法满足时限。当前板只求无权最大基数拟阵交；最大权拟阵交需要带权最短增广路与势能，不能先按权排序后调用本板。若只需要普通二分图匹配，Hopcroft--Karp 的接口更直接；需要保留两份划分拟阵及地面元素编号时，再用上面的专用入口。

若一次 `canAdd/canExchange` 为 $O(C)$、两份 `reset` 合计为 $O(R)$，可按 $O((r+1)(n^2 C+R))$ 估计通用版本总时间；开始的贪心最多再做 $O(n)$ 次加入判断及 $O(r)$ 次重建，已被这个上界覆盖。交换图按需枚举，不保存全部 $O(n^2)$ 条边，算法本身额外空间为 $O(n)$，再加两份判定器的状态。

#link("https://www.spoj.com/problems/COIN/")[SPOJ · Coin Collecting]：每轮的两只信封属于同一个容量为一的划分组，每只信封的两枚硬币作为图拟阵中的一条边；国家编号先离散化，调用通用 `matroidIntersection`，答案是所选边数的两倍。该题用于检查划分拟阵与图拟阵的交换增广，不能换用“两边都是划分拟阵”的专用入口。
