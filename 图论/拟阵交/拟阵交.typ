拟阵交在同一地面集上给出两套“独立”约束，求同时满足两套约束的最大基数子集。最常见的竞赛模型是彩虹森林：所选边既不能成环，又不能重复颜色；若最后选到 `点数-1` 条，就是一棵彩虹生成树。它不是把两个普通贪心叠在一起，先按一种约束贪心往往会堵死另一种约束。

设当前公共独立集为 $I$。未选元素若能直接加入第一拟阵，就可作为增广起点；若也能加入第二拟阵，直接选入即可。否则在交换图中交替考虑“按第二拟阵删一个已选元素后加入它”和“按第一拟阵删一个已选元素后加入另一个未选元素”。找到终点后对整条路取对称差，两套独立性都保持，集合大小增加一。若不存在增广路，拟阵交换性质保证当前集合已经最大。

==== Oracle 接口

地面集编号为 `0..n-1`。每轮开始会用同一个 `chosen` 调用两个 Oracle 的 `reset`；`canAdd(in)` 判断把 `in` 加入当前集合后是否独立，`canExchange(out,in)` 判断删去 `out`、加入 `in` 后是否独立。询问必须只相对这轮固定的 $I$ 回答，不能在一次判断中永久修改 Oracle 状态。

`常用拟阵.cpp` 提供划分拟阵和图拟阵。划分拟阵限制每组最多选 `capacity[group]` 个元素；图拟阵把无向边集独立定义为森林，并用当前森林的 DFS 序在 $O(1)$ 判断“新边是否跨过删边形成的割”。图拟阵顶点编号为 `1..vertices`，两只 Oracle 的地面集顺序和大小必须完全一致。

两边都是容量为 `0/1` 的划分拟阵时，调用 `unitPartitionMatroidIntersection(first,second)`。它仍返回所选地面元素编号，但把交换图压缩到划分组，在一张层次图中批量找最短增广路；设地面集大小为 $n$、两边组数之和为 $g$，复杂度为 $O((n+g)sqrt(g))$，空间为 $O(n+g)$。普通二分图的每条边可作为一个地面元素，左右端点分别作为两边的 `group`；容量为零的组不会选入元素。一般容量不能调用这个入口。

通用 `matroidIntersection` 每次增广最多枚举 $O(n^2)$ 个交换判断，最多增广到答案大小 $r$，因此慢 Oracle 或过大的地面集会明显 TLE。当前板只求无权最大基数拟阵交；最大权拟阵交需要带权最短增广路与势能，不能先按权排序后调用本板。若只需要普通二分图匹配，Hopcroft--Karp 的接口更直接；需要保留两份划分拟阵及地面元素编号时，再用上面的专用入口。
