==== 用途

- 输入一棵有根树和若干点对，求每对点的最近公共祖先；配合深度、根距离和向上跳可回答距离、路径第 $k$ 个点等询问。
- 下表四种实现得到相同的 LCA，按询问是否离线以及是否还要处理路径操作选择。

==== 选型

#table(
  columns: (auto, auto, auto),
  align: center,
  table.header([算法], [复杂度], [选用]),
  [倍增], [$O((n + m) log n)$], [普通在线询问；容易补向上跳 $k$ 级祖先],
  [Tarjan], [$O(n + m)$], [询问全部给出，只离线回答一次],
  [树链剖分], [$O(n + m log n)$], [还要做路径、子树操作],
  [RMQ + 欧拉序], [预处理 $O(n log n)$，查询 $O(1)$], [静态树、询问多],
)

==== 接口与坑点

- 四份实现都只预处理指定根所在的连通块，均使用递归 DFS。
- 倍增、树链剖分在构造时预处理，传入双向邻接表。
- Tarjan 版把 `(v, id)` 同时放入 `q[u]`、`q[v]`，`id` 为 `1..m`，答案在 `ans[id]`。
- RMQ 版用 `addedge` 加无向边，随后 `build(root)`。

==== 常用公式

- 距离：`dep[u] + dep[v] - 2 * dep[lca(u, v)]`；边带权时把 `dep` 换成根距离。
- 换根为 `r`：`lca(u,v)`、`lca(u,r)`、`lca(v,r)` 中深度最大的点为答案。
- 路径第 `k` 个点（从 `u` 起、`0` 下标）：令 `w=lca(u,v)`、`a=dep[u]-dep[w]`、`b=dep[v]-dep[w]`；若 `k <= a` 则 `jump(u,k)`，否则 `jump(v,a+b-k)`。当前倍增版需补 `jump`。
- 三点汇合：三对 LCA 中深度最大的点为汇合点。

- 洛谷 P3379「最近公共祖先」；洛谷 P4281「紧急集合」。
