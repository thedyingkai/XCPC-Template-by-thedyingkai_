- 输入一棵固定根树和路径、子树或换根询问，把树上问题转成差分累加、DFS 序区间或换根 DP。
- 这些公式分别得到批量路径贡献、连续的子树区间，以及换根后的子树或全点距离和。

==== 树上差分

- 多次路径修改、最后统一统计时使用；若要在线修改查询，仍用树链剖分。
- 路径点权加 `x`：令 `l = lca(u,v)`，执行 `d[u] += x`、`d[v] += x`、`d[l] -= x`、`d[parent[l]] -= x`。后序累加 `d[parent] += d[child]` 后，`d[u]` 是点 `u` 的累计值；根的父亲编号 `0` 时忽略 `d[0]`。
- 路径边权加 `x`：执行 `d[u] += x`、`d[v] += x`、`d[l] -= 2*x`。后序累加后，`d[u]` 是边 `(parent[u],u)` 的累计值。
- 洛谷 P3128「Max Flow」；P2680「运输计划」可二分答案后用边差分统计不合格路径。

==== 欧拉序转换

- 只记录进入时间时，`u` 的子树是 `[dfn[u], dfn[u]+sz[u]-1]`；`u` 是 `v` 的祖先当且仅当 `dfn[u] <= dfn[v] < dfn[u]+sz[u]`。
- 点加、子树和：在 `dfn[u]` 单点加，查询子树区间；子树加、点查：对子树区间做差分加，在 `dfn[u]` 查前缀。
- 树上莫队记录进入、退出各一次。先交换端点使 `tin[u] <= tin[v]`，令 `l = lca(u,v)`：若 `l == u`，使用 `[tin[u],tin[v]]`；否则使用 `[tout[u],tin[v]]`，回答前临时切换一次 `l`，回答后撤销。

==== 换根

- 固定预处理根，当前根为 `r`。查询 `u` 的子树：`u == r` 时为整棵树；`u` 不是 `r` 的祖先时仍为原子树；否则令 `c = jump(r,dep[r]-dep[u]-1)`，答案为整棵树减去 `c` 的原子树。
- 设点权总和为 $W$，固定根下 `v` 的子树点权和为 $S_v$，`v` 是 `u` 的儿子，边长为 $w$。若 $F_u=sum_x a_x dot d(u,x)$，则 $F_v=F_u+(W-2S_v)w$。无点权时取 $W=n$，并令 `S[v] = sz[v]`。
- 一般换根 DP 在每个点对儿子贡献做前后缀合并，向儿子传递“除去该儿子后的贡献”；合并必须满足题目所需的结合顺序。

- 洛谷 P3478「STA-Station」用于检查距离和换根公式。
