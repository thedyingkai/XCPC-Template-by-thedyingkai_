功能图中每个点恰有一条出边。于是从任何点不断沿出边走，都会先经过一条不重复的链，再进入唯一的有向环；每个弱连通块也恰好由一个环和若干棵指向环的树组成。本板预处理这套结构，用来回答第 $k$ 个后继、首次到达另一点的步数、轨道大小以及点所属的环。

先反复删除入度为零的点，最后留下的恰是环点；从所有环点同时沿反图 BFS，就能给每个树上点确定入环点和到环深度。`up[j][u]` 保存走 $2^j$ 步后的点，64 层覆盖整个 `u64` 范围。这个分解也说明了到达判定：目标在树上时只能沿同一条祖先链到达；目标在环上时，先走到 `entry[x]`，再沿环的唯一方向前进。

构造参数 `nxt[1..n]` 是每点后继。`jump(u,k)` 求第 `k` 个后继；`distance(x,y)` 求第一次从 `x` 到 `y` 的步数，不可达返回 `-1`；`orbitSize(u)` 是第一次出现重复点以前见到的不同点数。`onCycle/dep/entry/cid/pos/cycleLen` 分别给出环标记、入环深度、入环点、环编号、环上位置和环长。

若题目要统计挂树答案，可记录剥离顺序并逆序 DP；若有在线修改后继，静态分解会全部失效，不能局部改一个 `to` 后继续查询。`build()` 可以对同一份 `to` 重新预处理，但调用前若修改后继，仍需保证每个值都在 `1..n`。
