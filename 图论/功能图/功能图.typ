==== 用途

- 输入每个点恰有一个后继的有向图，处理从任意点沿唯一出边反复行走的问题。
- 预处理后可回答第 $k$ 个后继、首次到达另一点的步数、轨道大小，并得到每个点的所属环和入环位置。

==== 结构与接口

- 构造参数 `nxt[1..n]` 表示每点唯一的出边。每个弱连通块由一个有向环和若干棵指向环的树组成。
- `onCycle[u]`、`dep[u]`、`entry[u]` 分别表示是否在环上、到环距离、首次进入的环点；`cid/pos/cycleLen` 记录环编号、环上位置和环长。
- `jump(u,k)` 求第 `k` 个后继，`k` 为 `u64`；`orbitSize(u)` 是从 `u` 出发能遇到的不同点数。
- `distance(x,y)` 求沿出边从 `x` 第一次到 `y` 的步数，不可达返回 `-1`。

==== 改法与坑点

- 入度为零的点不断出队后，未删除点恰为所有环点；再从环点沿反图处理挂树。
- 若 `y` 不在环上，必须满足 `dep[x] >= dep[y]` 且向上跳深度差后恰为 `y`；若 `y` 在环上，答案为 `dep[x]` 加 `entry[x]` 到 `y` 的有向环距离。
- 求每点第一次重复前的步数可直接用 `orbitSize`；统计进入环前的信息时按剥离顺序逆序 DP。

- CSES「Planets Queries I / II」「Planets Cycles」。
