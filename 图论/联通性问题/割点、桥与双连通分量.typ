==== 用途

- 输入无向图，标出割点和桥，并给每个点分配边双编号、枚举每个点双的点集。
- 桥森林和圆方森林不是当前代码的现成返回值；需按边双编号或点双点集另行建图，再在新森林上处理删点删边后的连通性及必经桥、必经割点询问。

==== 接口与坑点

- 四份板子都需对每个未访问点调用 `tarjan`。
- `CutDot`、`VDCC` 使用双向邻接表，入口为 `tarjan(root, root)`。
- `CutEdge::add`、`EDCC::add` 已加入正反边，每条无向边只调用一次；`EDCC` 构造参数 `m` 是无向边数。
- `CutEdge::bri` 按 DFS 方向保存桥；P1656 需先把端点规范为 `min/max`，再按点对排序。`EDCC::dcc[u]` 是边双编号，`VDCC::dcc` 保存各点双点集。

==== 常改与 trick

- 桥树：把跨越不同 `dcc` 的原边连到缩点图。
- 圆方树：每个点双新建一个方点，与分量内所有原点连边。
- 连通图加最少边变成边双连通图：桥树叶子数为 `leaf`，答案为 `(leaf + 1) / 2`。
- 原图连通时，删除非根点后的连通块数为满足 `low[v] >= dfn[u]` 的 DFS 儿子数加 `1`；删除根后为其 DFS 儿子数。
- 两点之间必经桥的数量，等于其边双编号在桥树上的距离；删除某座桥是否分开两点，可转为判断对应桥树边是否位于两分量的树路径上。
- 对不等于询问端点的原图点 `x`，删除 `x` 是否使 `u,v` 断开，可转为判断圆方树上的原点 `x` 是否位于原点 `u,v` 的树路径上。计数时只统计割点对应的圆点，并按题意排除端点。
- 桥树和圆方树都是森林；跨原连通块询问应先判无路径，再使用 LCA、树链剖分或树上前缀和。

- 洛谷 P3388「割点」；P1656「炸铁路」；P8436「边双连通分量」；P8435「点双连通分量」。
