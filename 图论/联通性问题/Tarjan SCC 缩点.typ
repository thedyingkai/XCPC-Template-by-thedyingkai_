==== 用途

- 输入有向图，Tarjan 求出每个点的强连通分量编号和分量大小，再把分量间边缩成一张 DAG。
- 用于压缩环依赖和可达关系、在缩点 DAG 上做 DP，也是 2-SAT 判定与赋值的前置分解。

==== 接口与坑点

- `work()` 后，`scc[u]` 为分量编号，`cnt` 为分量数，`sz[id]` 为分量大小。
- 当前编号为逆拓扑序：缩点边从较大编号指向较小编号。更换 SCC 实现后要重新确认编号方向。
- `shrink()` 会先求 SCC，再返回去重后的缩点 DAG。

==== 常用 trick

- 点权按 `scc[u]` 合并，再在缩点 DAG 上 DP。
- 入度、出度必须在缩点边上统计；是否保留分量间重边按题意决定。
- 所有点都能到达的点只可能在唯一的零出度 SCC 中。
- SCC 数大于一时，把 DAG 补成强连通图至少需 `max(零入度数, 零出度数)` 条边。

- 洛谷 P3387「缩点」；洛谷 P2341「受欢迎的牛」。
