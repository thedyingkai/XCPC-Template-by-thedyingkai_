==== 用途

- 对有向图求强连通分量并缩成 DAG；对无向图求割点、桥、点双和边双，并进一步建立圆方树或桥树。
- 这些分解用于压缩环依赖、判断删点删边后的连通性，以及把原图询问转成树或 DAG 上的询问。

==== Tarjan 核对

- SCC：树边回溯用 `low[v]`；只有目标仍在栈中时，才用 `dfn[v]` 更新返祖边。
- 桥：`low[v] > dfn[u]`；无向图必须按边编号跳过父边的反向边，否则重边会误判。
- 割点：非根节点满足 `low[v] >= dfn[u]`；根节点至少有两个 DFS 儿子。
- 边双缩点得到桥树或桥森林；点双中的割点可属于多个分量，建圆方树时不能把它强行归入唯一分量。
