==== 通用概念

- 时间戳 (dfn)：DFS 访问次序编号。
- 追溯值 (low)：不经过父边能回溯到的最早祖先的 dfn。
- DFS 树：图中通过 DFS 生成的树结构，包含树边、反向边、反向边、横叉边。

所有算法时间复杂度和空间复杂度均为 $O(N+M)$。

#table(
  columns: (auto, auto, auto, auto),
  align: center,
  table.header(
    [], [SCC 缩点], [vDCC 缩点（基于根）], [vDCC 缩点（基于割点）],
  ),
  [记录分量的时机], [离 x 时], [离 x 时], [回 x 时],
  [记录分量的条件], [x 是分量的根], [x 是分量的根], [x 是割点],
  [原图], [有向有环图], [无向有环图], [无向有环图],
  [缩点后的新图], [拓扑图], [树（或森林）], [树（或森林）],
  [新图的应用], [入度出度找答案，递推最长路], [割边找答案，叶节点找答案], [割点找答案，叶节点找答案],
)
