- Prüfer 序列把一棵 $n$ 个有标号顶点的树与长度 $n-2$ 的序列相互转换。
- 编码、解码可以恢复序列或边集；序列中的出现次数还能直接处理树计数和指定度数计数。

==== 接口与性质

- `pruferEncode(n,edges)` 与 `pruferDecode(code)` 使用边集和最小堆，复杂度 $O(n log n)$，适合普通规模。
- `pruferEncodeLinear(parent)` 与 `pruferDecodeLinear(code)` 使用以 `n` 为根的父亲数组，复杂度 $O(n)$；洛谷 P6086 的大范围应使用这两个接口。
- 只支持 $n>=2$。输入边必须构成树；解码序列中的编号必须在 `1..n`。
- 顶点 $v$ 的度数等于它在 Prüfer 序列中的出现次数加一。

==== 计数与改板

- Cayley 公式：$n$ 个有标号顶点的树共有 $n^(n-2)$ 棵。
- 给定正度数 $d_1,...,d_n$ 且 $sum d_i=2n-2$，对应树数为 $(n-2)! / product_i (d_i-1)!$。
- 只需要度数或计数时直接使用出现次数性质，不要真正构造树。
- 线性版本用单调指针维护当前最小叶子；父亲数组必须表示一棵以编号 `n` 为根的树，`parent[n]=0`。

==== 板子题

- 洛谷 P6086「Prüfer 序列」。
