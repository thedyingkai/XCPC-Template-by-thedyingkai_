2-SAT 接受若干个“两个条件至少满足一个”的布尔限制，判断是否存在一组取值，并在有解时给出方案。每个条件是某个变量取真或取假。互斥、相等、二选一、选了某项就必须选另一项，都可先化成两个条件的析取或蕴含；一般的三元子句不能直接塞进 2-SAT。

子句 $a or b$ 等价于两条蕴含 $not a -> b$、$not b -> a$。若同一变量取真和取假的两个状态能互相推出，它们就在同一个强连通分量中，两种取值都会矛盾。否则，缩点后按逆拓扑顺序确定尚未赋值的状态，并把相反状态设为假，就能得到一组解。因此，有解当且仅当每个变量的真假状态不在同一 SCC。

==== 接口和建模

- 变量编号为 `1..n`。`id(x,true)` 表示 $x$ 为真，`either(x,xv,y,yv)` 加入 `(x=xv) or (y=yv)`。
- `imply(x,xv,y,yv)` 加入蕴含并自动补上逆否边；`force(x,xv)` 固定一个变量。`solve()` 成功后，答案在 `ans[1..n]`。
- “不能同时选”写成 `either(x,false,y,false)`；相等要加入两个方向的同值蕴含；不等可分别加入“至少一个真”和“至少一个假”。

当前 SCC 编号按缩点 DAG 的逆拓扑序生成，因此赋值比较方向写在 `solve()` 中。换成 Kosaraju 或别的 SCC 板子时，必须用一条强制为真的小样例核对比较方向。若要表达一组变量至多一个为真，两两连边需要平方条限制；规模大时应加前缀辅助变量做线性编码。

建图、求 SCC 的时间和空间都是 $O(n+m)$，其中 $m$ 是蕴含边数。

#link("https://judge.yosupo.jp/problem/two_sat")[Library Checker · Two SAT]
