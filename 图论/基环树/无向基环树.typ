==== 用途

- 输入一棵连通无向基环树，拆出唯一的环和挂在环上的各棵树。
- 分解后可回答两点应走树内还是经过环，并为最短路、直径及环形 DP 提供环顺序和挂树信息。

==== 接口

- `UnicyclicGraph(n)` 只接受连通无向基环树，即点数、边数同为 `n`；`addEdge` 返回从 `0` 开始的边编号，`build()` 判定并分解。
- `cycle` 按环上顺序保存点，`cycleEdge[i]` 是 `cycle[i]` 到下一环点的边；支持自环和两条平行边构成的环。
- 非环点的 `root` 是所属挂树的环根，`parent/parentEdge/dep/dist` 是从环向外建立的树信息。
- `clockwiseDistance` 按 `cycle` 顺序求环距离；边权非负时 `shorterCycleDistance` 才表示两环点最短距离。

==== 常用处理

- 度数不超过 `1` 的点不断出队，剩余点恰为环；处理挂树后，再把环复制一遍做环形 DP、单调队列或双指针。
- 两点位于同一挂树时走树内路径；挂树不同则分别走到 `root`，再选择环的两个方向。需要树内距离时，对 `parent` 森林另建 LCA。
- “功能图”是每点出度恰为一的有向图；“基环树”是无向连通图且恰有一个环，不要混用判定和距离方向。
