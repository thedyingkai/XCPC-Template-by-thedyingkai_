无向连通图有 $n$ 个点、$n$ 条边时恰有一个环，称为基环树。把唯一环拆出来以后，其余边构成若干棵挂在环点上的树；两点路径、直径、环形 DP 等问题便可拆成“各自走到环根”和“在环上选方向”两部分。

不断删除当前度数不超过 $1$ 的点，树枝会被逐层剥掉，未删除点恰为环。沿环记录 `cycle` 和 `cycleEdge` 后，再从全部环点向外 BFS，便得到每个非环点所属的挂树、父边、深度和到环距离。`build()` 同时检查边数与连通性；返回 `false` 表示输入不是一棵连通基环树。

`addEdge(u,v,w)` 返回从 `0` 开始的边编号。`cycleEdge[i]` 连接 `cycle[i]` 与下一个环点，代码支持自环以及两条平行边构成的二元环。`root[u]` 是挂树的环根，`parent/parentEdge/dep/dist` 是从环向外建立的树信息；`clockwiseDistance` 按记录顺序求环上距离。

两点挂在同一环根下时，最短路还需要挂树内的 LCA；环根不同则把两段到根距离加上环的两个方向中较小者。`shorterCycleDistance` 只在非负边权的通常最短路语义下使用，所有前缀和与答案必须能放进 `i64`。环形最大值或直径常把环复制一遍，再接单调队列或双指针。
