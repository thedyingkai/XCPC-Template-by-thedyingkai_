==== Edmonds--Karp 最大流

这份板子是标准 Edmonds--Karp：每轮 BFS 在残量网络中找一条边数最少的增广路，用该路的瓶颈容量增广一次，直到汇点不可达。BFS 选择最短增广路后，任意残量边成为关键瓶颈的次数受点数限制，因此复杂度为 $O(V E^2)$，与容量大小无关；大图通常改用 Dinic。

`EK(n,S,T)` 建图，`add(u,v,c)` 加有向原边并补零容量反边，`ek()` 返回从当前残量网络新增的流量。`pre[v]` 保存本轮到达 `v` 的残量边，`mf[T]` 是整条路瓶颈。保存原正向边编号后，初始反向容量为零时，最终反向容量就是该边流量。

只判断流量是否达到 `k` 时，可把瓶颈截到剩余需求并提前结束。不要把这份 EK 的复杂度写成 Dinic，也不要为了通过大规模匹配题在文件内部换成分层阻塞流；需要性能时直接选用对应的 Dinic 或 Hopcroft--Karp 板子。
