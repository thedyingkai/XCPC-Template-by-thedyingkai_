==== 接口与坑点

- `add(u, v, c)` 加有向边，并自动加入容量为 `0` 的反向边。
- `ek()` 返回本次从当前残量网络继续增广得到的流量；通常只调用一次。
- 复杂度 $O(V E^2)$，规模大时用 Dinic。
- 恢复原边流量：建边时保存正向边编号，初始反向容量为 `0` 时，最终反向容量即流量。

==== 常改

- 只判断流量是否至少为 `k`：累计到 `k` 后停止。
- 洛谷 P3376「网络最大流」。
