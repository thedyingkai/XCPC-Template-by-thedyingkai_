==== 用途

- 输入每条有向边的流量下界和上界，判断能否同时满足容量范围与各点流量守恒，并在可行时恢复每条原边的实际流量。
- 可处理带强制最低流量的循环流和有源汇可行流；当前板子只判可行性，不直接求最大流、最小流或最小费用。

==== 接口与坑点

- `add(u, v, lower, upper)` 要求 `0 <= lower <= upper`，返回原边编号。
- 无源汇可行流调用 `feasible()`；有源汇可行流调用 `feasible(s, t)`。
- 可行时，`edgeFlow(id)` 返回原边实际流量。
- 所有边必须在 `feasible` 前加入，且 `feasible` 只能调用一次。

==== 原理与常改

- 原边剩余容量为 `upper-lower`；下界造成的点流量差用超级源汇补齐。可行当且仅当超级源发出的边全部满流。
- 固定流量为 `x`：令下界、上界都为 `x`。
- 有源汇时增加 `t -> s` 的无穷容量边转成循环流。
- 求最大流或最小流前，修改 `feasible(s,t)` 以保存附加边 `t -> s` 的编号；可行后取其反向残量为 `base`，并把附加边正反残量及超级源汇相关边全部清零。
- 再把 Dinic 的源汇改成 `s,t`，最大流为 `base + dinic()`；改成 `t,s`，最小流为 `base - dinic()`。
- 边带费用时改用最小费用流维护剩余容量；当前 Dinic 不保存费用。

- LibreOJ 115「无源汇有上下界可行流」；洛谷 P5192「Shoot the Bullet」需按上面的有源汇最大流改法扩展。
