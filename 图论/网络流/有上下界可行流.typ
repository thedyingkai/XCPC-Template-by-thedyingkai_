每条有向边除了容量上界，还有必须流过的下界。本板判断这些范围能否与点流量守恒同时满足，恢复一组原边流量，并可继续求有源汇最大流。固定必须经过的流量、每项至少分配多少、带最低需求的循环运输，都可先写成上下界边。

先让每条边流满下界，只把 `upper-lower` 留作残量容量。代码记 `balance[u]=下界流入-下界流出`：若 `balance[u]>0`，下界造成的净流入过多，原残量网络必须再从 `u` 流出这些量，因此加 `SS -> u`；若 `balance[u]<0`，原残量网络必须向 `u` 补流，因此加 `u -> TT`。超级源发出的边能全部满流，当且仅当这些差额能在原网络中补齐，于是得到一组可行循环流。

`add(u,v,lower,upper)` 返回原边编号。无源汇循环流调用 `feasible()`；有源汇可行流调用 `feasible(s,t)`，做法是再加一条 `t -> s` 的充分大容量边，把源汇不守恒转成循环。`edgeFlow(id)` 在可行后返回下界加残量反边容量，也就是原边实际流量。所有原边必须先加完，`feasible` 只能调用一次。

`maxFlow(s,t)` 先读取附加边 `t -> s` 上的基础流，删去超级源汇边和附加边，再从 `s` 到 `t` 继续增广；无解返回 `nullopt`。附加边容量目前取 `LLONG_MAX/4`，真实答案和所有容量和必须落在安全范围内。若要求最小流，可在可行后删附加边，改从 `t` 向 `s` 尽量退流；若边还有费用，则超级源汇阶段和后续目标都必须换成相应的最小费用流。
