每条有向边除了容量上界，还有必须流过的下界。本板判断这些范围能否与点流量守恒同时满足，恢复一组原边流量，并可继续求有源汇最大流。固定必须经过的流量、每项至少分配多少、带最低需求的循环运输，都可先写成上下界边。

设原边的流量为 $f$、下界为 $l$、上界为 $r$，改写成 $f=l+x$ 后，只需给新变量容量约束 $0<=x<=r-l$。困难在于各点预先送出的下界流量通常不守恒：每个点还需用这些新变量补足净流入或净流出。下界只是预先占用流量，不是已经找到一组可行流；不能简单把边容量减去下界后直接跑原源汇最大流。

先让每条边流满下界，只把 `upper-lower` 留作残量容量。代码记 `balance[u]=下界流入-下界流出`：若 `balance[u]>0`，下界造成的净流入过多，原残量网络必须再从 `u` 流出这些量，因此加 `SS -> u`；若 `balance[u]<0`，原残量网络必须向 `u` 补流，因此加 `u -> TT`。超级源发出的边能全部满流，当且仅当这些差额能在原网络中补齐，于是得到一组可行循环流。

符号可以用一条下界为 `2` 的边 `a->b` 检查。预送后 `a` 少了两单位、`b` 多了两单位，因此需要沿原残量网络从 `b` 再流回 `a` 两单位；辅助路径应是 `SS->b->...->a->TT`。全部 `balance` 之和为零，所以正差额总量等于负差额绝对值之和。若超级源最大流达到 `need`，所有辅助边都恰好填满；删去这些边后，下界加新流量便在每个原点守恒。反过来，任意可行原流量减去下界都能补齐这些辅助边，这也说明判定没有漏解。

`LowerBoundFlow f(n)` 的原点编号为 `1..n`，内部占用 `n+1,n+2` 作为超级源汇。`add(u,v,lower,upper)` 返回原边编号。无源汇循环流调用 `feasible()`；有源汇可行流调用 `feasible(s,t)`，做法是再加一条 `t -> s` 的充分大容量边，把源汇不守恒转成循环。`edgeFlow(id)` 在可行后返回下界加残量反边容量，也就是原边实际流量。所有原边必须先加完，`feasible` 只能调用一次。

`maxFlow(s,t)` 内部先调用 `feasible(s,t)`，读取附加边 `t -> s` 上的基础流，删去超级源汇边和附加边，再从 `s` 到 `t` 继续增广；无解返回 `nullopt`。因此直接调用 `auto ans=f.maxFlow(s,t)`，不能先调用 `feasible` 成功后再调用它，否则会触发重复建图的断言。只需一组可行方案时调用 `feasible`，需要最大流时调用 `maxFlow`，成功后都可用此前保存的编号逐条 `edgeFlow(id)` 恢复方案。

附加边上的流量等于原图从 `s` 到 `t` 的净流量。删辅助边时正反两个方向的残量都必须清零，否则后续增广可能绕辅助边改变问题。原边的反向残量不能清掉：它表示在不低于下界的前提下撤回此前多送的流量，后续最大流可能需要先撤回再改路。第二阶段在可行流的残量图上继续增广，因而始终保留下界和中间点守恒；无增广路时就是可行流中的最大值。

这份有源汇入口通过一条容量非负的 `t->s` 边闭合，只寻找净流量非负的可行流，不能处理“允许答案为负，而且所有可行源汇流量都为负”的定义。若题目要求恰好流 `k>=0`，可自己加入下上界都为 `k` 的边 `t->s`，随后调用无参数 `feasible()`；不要再调用 `feasible(s,t)` 额外加入另一条回边。该固定边满足守恒，便强制原网络的净流量等于 `k`。

求最小非负流时，先调用 `feasible(s,t)`，保存 `returnFlow` 并清空全部辅助边的正反残量，再把源汇交换，从 `t` 向 `s` 退流，答案为基础流减退流量。退流上限须限制为基础流，否则可能得到题目不允许的负流量；当前类没有最小流入口，需要同时调整 Dinic 的总推流上限，不能只改源汇后无限制调用。若允许带符号流量，则还要相应改初始可行性建模。

扩建只增加两个点和 $O(n)$ 条边，最多调用两次 Dinic，可沿用一般网络的 $O(V^2 E)$ 时间、$O(V+E)$ 空间上界。附加边容量目前取 `LLONG_MAX/4`，真实需求、基础流、容量和及 `balance` 累加必须落在安全范围内。若边还有费用，仅找到任意可行流后接普通最短增广路，可能留下可改善费用的残量负环；需要保证整个可行流阶段及后续优化都满足所选费用流算法的条件，或使用能处理负环的最小费用循环流，不能只给这份 Dinic 的边增加费用字段。
