==== Dinic 最大流

输入一张非负容量网络和源汇，`Dinic(n,S,T)` 建立残量图，`add(u,v,c)` 加一条有向原边并自动补零容量反边，`dinic()` 返回从当前残量网络还能增加的流量。点数组开到 `0..n`；通常只调用一次，若重复调用，返回值不是历史累计流量。

BFS 只保留残量网络中从源出发的最短边数层次，DFS 随后只沿层次增加一的边推阻塞流。一轮结束后所有当前最短增广路都被堵住，下一轮源汇距离严格增加，所以算法最终停在不存在增广路的最大流。一般图复杂度为 $O(V^2 E)$；二分图匹配、单位网络有更紧的界，但已知是普通二分图时仍优先用 Hopcroft--Karp。

正反边编号满足 `id ^ 1`。保存每条原边的正向编号后，初始反向容量为零时，`e[id ^ 1].c` 就是流量。若只判断流量是否至少为 `k`，可把每次允许推送的上限截到剩余需求并在达到后停止。容量、单次增广和答案都用 `i64`，总流量必须不溢出；DFS 仍是递归实现，层数极深时需要改显式栈或提高栈空间。
