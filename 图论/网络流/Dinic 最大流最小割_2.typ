==== 接口与坑点

- `Dinic(n, S, T)` 的点范围为 `0..n`；`add(u, v, c)` 加有向边并自动加入反向边。
- 正反边编号满足 `id ^ 1`。恢复原边流量时保存正向边编号；初始反向容量为 `0` 时，最终 `e[id ^ 1].c` 即流量。
- `dinic()` 返回本次从当前残量网络新增的流量；重复调用不会返回累计值。
- 容量与答案均为 `i64`；`dfs` 递归，分层图很深时注意栈空间。

==== 常改

- 只需判断是否达到 `k`：每轮最多推 `k-flow`，达到后停止。
- 洛谷 P3376「网络最大流」；P1343「地震逃生」。
