==== 先把题目改成流量守恒

网络流中的每条有向边有容量，除源、汇外每个点都满足流入等于流出。最大流适合“资源能通过哪些通道送多少”，最小割适合“删掉哪些容量代价才能阻断源汇”；二者数值相等，最大流结束后的残量可达集还能直接给出一组最小割划分。若每单位流量另有费用，就在可行流中继续最小化总费用。

常见建模先看限制落在点还是边。边不相交路径把相关边容量设为 `1`；点不相交路径把点拆成入点、出点，中间容量设为 `1`。二分图多重匹配按“源到左部、左到右、右部到汇”连边，源汇侧容量是点的使用次数。多源多汇加超级源汇，容量只需取真实供需上限。

最大权闭合子图把正权点由源连入、负权点连向汇，依赖 `u` 选中必选 `v` 时连 `u -> v` 的充分大容量边；答案是正权和减最小割。这里的“无穷”只需严格大于所有有限割的总容量，不能随手用 `LLONG_MAX` 后又参与加法或费用乘法。

==== 最小割的二元状态写法

约定源侧为状态 `1`、汇侧为状态 `0`。状态 `0` 付 `c0` 时连 `S -> x`，状态 `1` 付 `c1` 时连 `x -> T`；仅当 `x=1,y=0` 付 `c` 时连原边 `x -> y`。强制为 `1` 就用充分大容量从源连入，强制为 `0` 就向汇连出。两点状态不同时付 `c` 需要两条方向相反、容量均为 `c` 的原边；`add` 自动生成的零容量残量反边不能代替第二条原边。

恢复原边流量时，在 `add` 前保存正向边编号；若初始反向容量为零，最终反向容量就是已发送流量。恢复最小割必须先把最大流跑完，再从源沿正残量边搜索。若目标允许少选或空选，最小费用流不能盲目推到最大流，必须限制目标流量或只接受改善目标的增广。
