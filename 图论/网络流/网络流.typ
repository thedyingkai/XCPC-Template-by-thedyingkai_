==== 先把题目改成流量守恒

网络流中的每条有向边有容量，除源、汇外每个点都满足流入等于流出。最大流适合“资源能通过哪些通道送多少”，最小割适合“删掉哪些容量代价才能阻断源汇”；二者数值相等，最大流结束后的残量可达集还能直接给出一组最小割划分。若每单位流量另有费用，就在可行流中继续最小化总费用。

每条原边的流量介于零和容量之间，流值是源点的净流出，也等于汇点的净流入。对任何包含源、不含汇的点集，流值都等于从该集合流出去的流量减流进来的流量，因此不超过从源侧指向汇侧的边容量和，这个和就是割容量。割只计一个方向，反方向原边不计入该割；最小割可能不唯一，但它们的容量都等于最大流。

送流后保留两种调整余地：正向残量 `容量-已送流量` 允许继续增加，反向残量 `已送流量` 允许撤回旧选择。沿一条源汇残量路径统一增加瓶颈流量，正向经过的原边加流，反向经过的原边减流，内部点仍守恒。反边不是允许物品真的逆向运输，而是让算法修正之前的方案；若原问题还允许反向运输，必须另加一条有自己容量的原边。

若已经没有源汇增广路，令 `R` 为源在正残量边上能到达的点集。汇不在 `R`，每条 `R` 到外部的原边都已满流，每条外部到 `R` 的原边都没有流量，否则其反向残量会把外部点也变成可达。于是当前流值恰等于这个割容量，同时达到所有流的上界，得到最大流最小割定理在代码中的判停条件。提前达到某个流量后停止，只能证明这个流量可行，不能据此恢复最小割。

常见建模先看限制落在点还是边。边不相交路径把相关边容量设为 `1`；点不相交路径把点拆成入点、出点，中间容量设为 `1`。二分图多重匹配按“源到左部、左到右、右部到汇”连边，源汇侧容量是点的使用次数。多源多汇加超级源汇，容量只需取真实供需上限。

点容量的完整拆法是把原点 `v` 变为 `v_in,v_out`，所有进入 `v` 的边接到 `v_in`，所有离开 `v` 的边从 `v_out` 出发，中间边限制这个点的使用量。若端点允许被多条路径共用，源汇不要限制为容量一。整数容量网络存在整数最大流，所以单位容量确实能表示选边、配对和路径条数，不会产生“半条路径”；恢复路径时仍需按正流量逐条消耗，流量分解中可能出现的环不应当作额外源汇路径。

最大权闭合子图把正权点由源连入、负权点连向汇，依赖 `u` 选中必选 `v` 时连 `u -> v` 的充分大容量边；答案是正权和减最小割。这里的“无穷”只需严格大于所有有限割的总容量，不能随手用 `LLONG_MAX` 后又参与加法或费用乘法。

闭合子图中约定源侧为选中。正权点放汇侧会割掉源边，损失该收益；负权点放源侧会割掉汇边，支付其代价；依赖边阻止“选中前者、未选后者”。例如收益 `5` 的项目依赖代价 `3` 的设备，建 `S->项目` 容量 `5`、`设备->T` 容量 `3`、`项目->设备` 容量充分大，最小割为 `3`，净收益为 `2`。若设备代价为 `7`，最优割去项目源边，表示全部不选，收益为零。题目若强制非空，需额外处理，不能把允许空集的结果直接当答案。

==== 最小割的二元状态写法

约定源侧为状态 `1`、汇侧为状态 `0`。状态 `0` 付 `c0` 时连 `S -> x`，状态 `1` 付 `c1` 时连 `x -> T`；仅当 `x=1,y=0` 付 `c` 时连原边 `x -> y`。强制为 `1` 就用充分大容量从源连入，强制为 `0` 就向汇连出。两点状态不同时付 `c` 需要两条方向相反、容量均为 `c` 的原边；`add` 自动生成的零容量残量反边不能代替第二条原边。

建模可把四种状态逐项代入检查，而不要凭“同组”“冲突”等词猜边方向。若点的两种代价可能为负，先减去 `min(c0,c1)`，将这部分记为固定常数，剩下的两种代价非负后再建源汇边；最后把常数加回最小割。割容量不能为负，直接把负收益塞进容量会改变问题或触发断言。

更一般的二元代价给出 `c00,c01,c10,c11`，分别对应 `(x,y)=(0,0),(0,1),(1,0),(1,1)`。在上述固定状态约定下，若 `c00+c11<=c01+c10`，令 `w=c01+c10-c00-c11`，可把代价写成 `c00+(c11-c01)*x+(c01-c00)*y+w*x*(1-y)`。最后一项用 `x->y` 的容量 `w` 表示，前两项分别并入两点的单点代价，再按前述方法处理负数与常数。这给出了如何从四项代价推导割边；条件不成立时该边容量为负，不能照搬这个写法，也不能断言任意二元关系都能转最小割。

恢复原边流量时，在 `add` 前保存正向边编号；若初始反向容量为零，最终反向容量就是已发送流量。恢复最小割必须先把最大流跑完，再从源沿正残量边搜索。若目标允许少选或空选，最小费用流不能盲目推到最大流，必须限制目标流量或只接受改善目标的增广。

对当前 Dinic，先 `Dinic f(n,S,T)` 建图，再调用 `f.dinic()`；它返回本次新增流量，重复调用不会重复返回历史流值。求割时清空 `vis` 后调用 `f.mincut(S)`，`vis[u]` 表示源侧；枚举自己保存的原边，端点分别在源侧、汇侧的正向边就是割边。源汇必须不同，容量、总流量和建模中叠加的固定常数都须使用足够宽的类型。

最大流一般选 Dinic，其一般图上界为 $O(V^2 E)$；EK 为 $O(V E^2)$，适合更小的网络或核对建模。费用按单位计价时用费用流，有必须达到的边下界时用有上下界流。若每条路径还要求“不能超过若干步”“必须按时间先后”，普通守恒并不自动满足这些限制，需将层数、时间等加入点的状态并重新估算图规模。
