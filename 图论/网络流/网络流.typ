==== 常见模型

#table(
  columns: (auto, auto),
  align: center,
  table.header([问题], [建图]),
  [二分图多重匹配], [源 → 左部 → 右部 → 汇，容量为各点使用上限],
  [最大权闭合子图], [正权点由源连入，负权点连汇；`u` 依赖 `v` 时连 `u -> v` 的 $inf$ 边；答案为正权和减最小割],
  [DAG 最小路径覆盖], [每点拆成左右两点，原边从左连右；答案为 `n - 最大匹配数`],
  [二分图非负点权最大独立集], [点权连源、汇，冲突边连 $inf$；答案为点权和减最小割],
  [边不相交路径], [相关边容量设为 `1`],
  [点不相交路径], [拆点，中间边容量设为 `1`],
  [供需与单位费用], [供应点连源，需求点连汇，中间边容量为上限、费用为单位成本],
  [有上下界可行流], [先流满下界，再用超级源汇补齐各点流量差],
)

==== 建图核对

- $inf$ 只需大于所有有限割的总和，避免取 `LLONG_MAX` 后加法或费用乘法溢出。
- 最大流后的残量可达集可恢复最小割；二分图匹配方案由左到右的满流边恢复。
- 最大收益通常把收益取负作为费用；若允许少选或不选，不能强制流满。
- 多源多汇增加超级源汇，连接容量取真实供需上限。

- 洛谷 P3376「网络最大流」；P3381「最小费用最大流」；P2762「太空飞行计划问题」；P2774「方格取数问题」。
