输入每条边的容量和单位费用，本板先把源汇流量推到最大，再在所有最大流中最小化总费用。运输、带容量分配和带权二分图匹配都可直接建成“每走一单位流就支付边费用”的网络；若题目只要求恰好流 `k`，必须把推流上限改成剩余需求。

总费用是各原边 `流量*单位费用` 的和，容量限制能送多少，费用决定同样流量下优先采用哪种方案。送过一条费用为 `w` 的边后，会产生费用为 `-w` 的反向残量边；沿反向边走表示撤销此前的选择并退回费用。即使输入费用都非负，后续残量图也通常有负费用边，因此不能直接在原费用上反复运行 Dijkstra。

每次沿当前残量网络中的最短费用增广路送流。若初始正容量边的费用都非负，零势能已经可行，首轮直接运行 Dijkstra；只要存在负费用边，首轮就先从源点跑一次 SPFA，以最短距离初始化可达点的势能。源点可达部分不能有负费用环，否则最短路没有有限值，SPFA 也可能不终止。本板不做负环消圈；若题目允许给最大流额外叠加任意环流，要保证初始正容量残量图中任何位置都没有负费用环，才能得到全局的最小费用。

势能可行时，源点可达的每条可用边的约化费用 `w+potential[u]-potential[v]` 非负。Dijkstra 后只给可达点执行 `potential[u]+=d[u]`，下一轮仍保持这个不变量；`pre` 记录本轮最短路，沿路发送瓶颈流量后再求下一条。稠密图自动使用 $O(V^2+E)$ 的朴素 Dijkstra，稀疏图使用 $O(E log V)$ 的二叉堆版本；这是同一套连续最短路增广，只改变取最小距离点的实现。

固定流量下，若残量图有负费用环，沿环送一段流量不改变源汇净流量，却能降低费用；没有负费用环时，任何另一组同流量方案与当前方案的差都可拆成残量环，因此不能更便宜。从无负环的零流出发，每轮沿最短费用路径增广可保持这个最优性条件，故得到的每个流量阶段都是最小费用。负边本身允许存在，需要排除的是负环；容量有限也不能让 SPFA 正确处理负环，因为最短路阶段不会逐次扣掉环上容量。

势能给每个点一个偏移量。任意 `s->t` 路径上，各边的势能项相消，整条路径的约化费用等于原费用加 `potential[s]-potential[t]`，所以所有同起终点路径都加同一个常数，最短路径不变。设本轮 Dijkstra 距离为 `d`，由 `d[v]<=d[u]+约化费用(u,v)`，更新后的约化费用就是旧约化费用加 `d[u]-d[v]`，仍非负。本轮最短路径上的边取等号，更新后费用为零，新出现的反向残量边也为零，下一轮仍可使用 Dijkstra。

更新势能后，本轮路径的原始单位费用正好是 `potential[T]-potential[S]`，所以总费用增加 `f*(potential[T]-potential[S])`。这里的 `f` 是整条增广路上最小残量；路径中途不能按每条边各送不同流量，否则会破坏中间点守恒。不可达点不更新势能，也不能把 `INF` 加进去。只要图固定、每轮仅增广，新增反向边的两端都来自已经可达的路径，不会凭空引入此前不可达的负约化边；中途追加原边则可能破坏这个条件。

`MinCostMaxFlow(n,s,t)` 的点编号为 `0..n`；`add(u,v,capacity,cost)` 自动补容量为零、费用相反的残量边，所有边应在第一次调用 `run()` 前加入。运行后读取累计的 `flow` 和 `cost`。恢复原边流量仍需在建边时保存正向编号，最后读取反向容量。

例如建边前保存 `int id=f.e.size()`，随后 `f.add(u,v,cap,w)`，运行后 `f.e[id^1].c` 就是这条原边的流量。若要求恰好 `k` 单位，把 `run` 循环条件加上 `flow<k`，并将本轮初始 `f` 从 `LLONG_MAX` 改成 `k-flow`，再沿路径取瓶颈；结束后判断 `flow==k`，不足则无解。也可加一个新源点，以容量 `k`、费用零的边连向原源点，保持原 `run` 不变。重复调用 `run` 不会清空已有状态，结果仍是累计值。

代码令 `INF=LLONG_MAX/4`，所有有限最短距离的绝对值都必须小于 `INF`。边费用取反、势能和约化费用、距离加法、瓶颈流量乘路径费用以及累计的流量和费用都不能溢出 `i64`；特别是边费用不能取 `LLONG_MIN`。

最大费用可把正向费用取负并在最后反号。允许不选或少选时，不能先求最大流再看费用，因为额外流即使代价为正也会被强制发送；应限制目标流量，或把“停止选择”纳入模型。

设实际增广次数为 $A$，堆版总时间可按 $O(A E log V)$ 估计，若初始化需要 SPFA，再加最坏 $O(V E)$；稠密图每轮改为 $O(V^2+E)$。整数容量使每次至少增加一单位流量，故 $A$ 不超过最终流量，但这个界与容量数值有关，不能把总复杂度写成只跑一次 Dijkstra。空间为 $O(V+E)$。单位容量分配常需增广很多轮，应同时估算流量和图密度。

若目标是可选任意数量配对的最大收益，可把收益取负作为费用，在其余条件成立时只接受原费用为负的增广；最短增广的边际费用不下降，所以第一条非负费用路出现后再推流不会改善费用。若还要求收益相同时配对尽量多，则零费用路也要接受。停止判断应使用路径原费用，不能使用经过势能变换后总是非负的 `d[T]`。

Library Checker 的 Assignment 把源点到每一行、每一列到汇点都连容量一的零费用边，行 `i` 到列 `j` 连容量一、费用 `a[i][j]` 的边；推满 `n` 单位流后，从这些行列边的反向容量恢复排列。

设两行两列的费用为 `[[0,1],[0,100]]`。第一轮若选了第一行到第一列，第二轮不能只贪心选择费用 `100` 的剩余边；残量增广路可以走“第二行到第一列、第一列反向退回第一行、第一行到第二列”，把旧分配改掉，总费用变成 `1`。这也是建模时不能省去反向边、不能把已选配对永久锁死的原因。

#link("https://judge.yosupo.jp/problem/assignment")[Library Checker · Assignment]
