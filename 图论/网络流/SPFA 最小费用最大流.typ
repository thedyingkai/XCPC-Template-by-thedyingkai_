这份实现每轮用 SPFA 在残量网络中求一条最短费用增广路，再沿该路发送瓶颈流量；没有势能、Dijkstra 或阻塞流。只要残量网络不存在源可达负费用环，每次得到的都是当前流量下费用最小的增广，持续到汇点不可达后便得到最小费用最大流。

`SPFAMinCostMaxFlow(n,s,t)` 的点编号为 `0..n`。`add(u,v,capacity,cost)` 自动补容量为零、费用相反的反向边；`run()` 把结果累计在 `flow`、`cost`，通常只调用一次。恰好流 `k` 时，本轮实际增广量要截成 `min(mf[T],k-flow)`，结束后再检查 `flow==k`。最大费用可把费用取负并将结果反号。

SPFA 每次只增广一条路，构造数据或单位容量大图很容易 TLE；它适合保留作短而直接的基准实现和中小规模题，性能要求高时使用势能+Dijkstra 的版本。若允许空选，不能强制流满；若存在可达负费用环，SPFA 可能无法正常结束，且“只沿源汇路增广”也不足以处理任意费用循环。

Assignment 可按“源点到行、行到列、列到汇点”的单位容量网络直接验证建模与费用；但这份逐路 SPFA 版不保证通过该题的极限构造，正式提交应优先使用势能与 Dijkstra 的版本。
