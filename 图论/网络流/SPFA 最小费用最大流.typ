这份实现每轮用 SPFA 在残量网络中求一条最短费用增广路，再沿该路发送瓶颈流量。初始正容量图没有负费用环时，从零流开始这样增广，能够在每个流量下保持费用最小；汇点不可达时便得到最小费用最大流。它不消除负费用环：源可达负环可能使 SPFA 无法结束，源不可达负环则可能通过额外环流降低总费用，因此也不能忽略。

单位费用表示多送一单位流的代价，而非整条边使用一次的费用；若启用一条边只收一次固定费用，普通费用流不能直接表达。典型模型是源点到供给点、供给点到需求点、需求点到汇点：两侧容量表示供需，中央边容量表示可运输量，费用表示单位运输代价。先最大流再最小费用会尽量送满，即使继续运输亏损也会送；只要求部分数量时必须另外限制。

已经发送 `f` 单位的原边有 `capacity-f` 的正向残量、`f` 的反向残量。反向费用是原费用的相反数，走反边可以撤销旧选择再重新分配。只要允许撤销，局部看起来最便宜的一次分配就不必永久固定；因此每轮最短路必须在当前残量图中跑，不能只扫初始有向图。`pre[v]` 记录到达 `v` 的残量边，`mf[v]` 记录这条最短路的瓶颈，汇点可达时统一沿整条路发送 `mf[T]`，中间点流量便保持守恒。

正确性可从残量环看出：同流量的两组可行方案之差可分解为环流，负费用环表示存在不增加源汇流量也能降费用的调整。零流在初始图无负环时已是流量零的最优方案，沿最短费用路增广不会产生可改善费用的负环，于是每轮仍最优。若存在负环，即使其边容量有限，SPFA 的距离松弛也没有把环容量消耗掉，仍会不断降低距离；当前板不包含负环判定或消圈步骤。

`SPFAMinCostMaxFlow(n,s,t)` 的点编号为 `0..n`，源汇必须不同。`add(u,v,capacity,cost)` 自动补容量为零、费用相反的反向边；全部建边后调用 `run()`，结果累计在 `i64` 类型的 `flow`、`cost` 中。恰好流 `k` 时，在计算本轮累计值和修改残量前，把增广量截成 `min(mf[T],k-flow)`，达到 `k` 后停止，结束后检查 `flow==k`。最大费用可把费用取负并将结果反号。

要求恰好 `k` 又不想修改增广循环时，可加新源点，用容量 `k`、费用零的一条边连接原源点；最大流不会超过 `k`，最终仍须检查是否达到。恢复方案时在 `add` 前保存 `e.size()` 作为正向边编号，运行后读 `e[id^1].c`。重复调用 `run` 保留残量和累计答案，不会从空流重算；需要比较两份不同网络时应重新建对象。

边容量非负，容量和单边费用均为 `i64`；费用不能取 `LLONG_MIN`，因为反向边要取其相反数。最短距离及费用计算使用 `i128`，不可达哨兵为 `2^120`，有限最短距离必须严格落在其正负范围内。每次增广后的累计流量、累计费用仍须能放进 `i64`，代码在相乘前检查这一范围；不能仅因最终答案可表示，就允许中途累计值溢出。将最大费用答案反号时也要确认可表示。

SPFA 每次只增广一条路，构造数据或单位容量大图很容易 TLE；它适合中小规模题，性能要求高时使用势能与 Dijkstra 的版本。若允许少选或空选，应限制目标流量或按题意决定何时停止，不能强制流满。

设实际增广 $A$ 次，单次 SPFA 最坏 $O(V E)$，总时间最坏 $O(A V E)$，空间 $O(V+E)$。容量为整数时 $A$ 至多为总流量，但大容量并不自动保证少轮数，仍可能每条路都被某条容量一的边卡住。改用势能版时，建图、反边和流量恢复方式不变，区别在于用势能保证约化费用非负后改跑 Dijkstra。

可选数量的最大收益模型通常把收益取负。若允许空选且没有其他必须满足的流量，只接受 `d[T]<0` 的增广即可在费用不再改善时停下；收益相同时还想多选，则接受 `d[T]==0`。判断必须在更新流量、费用和残量之前进行。若题目同时规定最低配对数，应先满足数量要求，再讨论能否提前停止，不能直接套这个条件。

Assignment 可按“源点到行、行到列、列到汇点”的单位容量网络直接验证建模与费用；但这份逐路 SPFA 版不保证通过该题的极限构造，正式提交应优先使用势能与 Dijkstra 的版本。
