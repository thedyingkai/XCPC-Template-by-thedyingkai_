==== 从残量网络恢复最小割

对同一个 `Dinic` 对象先跑完 `dinic()`，再调用 `mincutPartition(dinic)`。函数从源沿正残量边搜索，返回数组中 `vis[u]=1` 的点属于源侧，其余点属于汇侧。若仍有源到汇的残量路，说明最大流尚未跑完，这时得到的划分不是最小割。

任意一条原正向边若从源侧指向汇侧，就跨过这组最小割；检查时不要把自动生成的残量反边也当成原边。最小割可能不唯一，本函数只恢复当前残量网络对应的一组。二分图最大匹配的残量图中，同一次可达搜索还能给出“左部不可达点 + 右部可达点”这一组最小点覆盖。
