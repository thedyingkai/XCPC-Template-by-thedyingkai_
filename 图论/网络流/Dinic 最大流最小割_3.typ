==== 最小割划分

- 先对同一个对象跑完 `dinic()`，再调用 `mincutPartition(dinic)`。
- 返回数组中，`vis[u] = 1` 表示 `u` 在残量网络中仍可从源点到达，即属于源侧。
- 原正向边中从源侧指向汇侧的边构成一组最小割；恢复时不要检查残量反边。
- 最小割不唯一时，只返回当前残量网络对应的一组划分。

- 二分图最大匹配后，一组最小点覆盖为“左部不可达点 + 右部可达点”。
