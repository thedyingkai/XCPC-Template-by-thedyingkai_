==== 最小容量优先，再最少割边

若题目先最小化割的总容量，再在所有最小割中最小化割边数，可以把两个目标编码进一次整数容量。设原图有 `m` 条有向原边，取 `base=m+1`，把每条容量 `c` 改成 `c*base+1`。任何割的次级部分都不超过 `m`，不足以向主目标进位，因此编码容量的最小割恰好按“原容量、边数”的字典序最小。

`mincutEdgeCountResult(n,S,T,edges)` 返回 `{capacity,edgeCount}`，`mincutEdgeCount` 只取第二项。平行原边分别计数；自动生成的残量反边不计入 `m`。单边编码用 `i128` 检查后才放入 `i64`，但调用者仍要保证编码后的最大流总和也不溢出。若次级代价不是每边一个单位，把末尾的 `1` 换成该边次级代价，并令 `base` 严格大于任何割的次级代价总和。
