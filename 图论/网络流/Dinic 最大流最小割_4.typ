==== 最小割中的最少边数

- 有 `m` 条边时取 `base = m + 1`，把容量 `c` 编码为 `c * base + 1`。
- 编码后的最大流为 `raw`，则原最小割容量为 `raw / base`，其中最少边数为 `raw % base`；当前函数只返回后者。
- `edges` 中每个三元组按一条有向边计数，平行边分别计数。
- 容量最终仍存入 `i64`，需保证单边编码和最大流总和不溢出。
- 洛谷 P1344「追查坏牛奶」。
