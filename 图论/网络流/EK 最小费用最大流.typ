==== 接口与坑点

- `add(u, v, capacity, cost)` 加有向边，反向边费用自动取相反数。
- `ek()` 修改残量网络，累计答案在 `flow`、`cost`；通常只调用一次。
- 当前每轮用 SPFA 找最短增广路，要求残量网络不存在源可达负费用环；数据大时改势能加 Dijkstra。

==== 常改

- 恰好流 `k`：本轮增广量截为 `min(mf[T], k-flow)`，最终 `flow < k` 则无解。
- 最大费用最大流：正向边费用取负，最终答案反号。
- 允许少选或不选时，不能盲目增广到最大流；限制目标流量，或只走能改善答案的增广路。
- 二分图最大权匹配：匹配边费用取权值相反数。

- 洛谷 P3381「最小费用最大流」。
