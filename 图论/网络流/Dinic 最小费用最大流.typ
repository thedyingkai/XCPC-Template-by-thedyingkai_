==== 接口与改法

- `add(u,v,capacity,cost)` 自动加入反向边；`dinic()` 后读取成员 `flow`、`cost`。
- `spfa()` 求最短费用，`dfs()` 只走满足 `d[v] == d[u] + w` 的边；要求残量网络不存在源可达负费用环，否则 SPFA 可能不终止。
- 恰好流 `k` 时把 DFS 上限截为 `k-flow`；若最终 `flow < k` 则无解。
- 最大费用把正向费用取负并将最终费用反号；允许少选或空选时不能强制增广到最大流。
- 恢复原边流量需在 `add` 前记录正向边编号，最后读取反向边容量。
- 数据更大时改势能加 Dijkstra；当前距离标签不能直接当势能复用。

==== 板子题

- 洛谷 P3381「最小费用最大流」。
