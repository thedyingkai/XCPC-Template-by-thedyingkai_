给定无向带权图，最小生成树在保持全部顶点连通的前提下，使所选 `n-1` 条边权和最小。把网络铺设、若干连通块逐步合并、路径瓶颈等题转成 MST 前，要先确认目标只要求连通而不关心原图路径方向。

Kruskal 从小到大考虑边，只连接两个不同连通块；若某条跨分量的最小边不在一棵最优树中，把它加入后形成的环上必有另一条跨割边，用前者替换后不会更差。Prim 维护当前树到每个树外点的最小连接边，每次加入全局最小者，正确性来自同一个割性质。

==== 选型和接口

#table(
  columns: (auto, auto, auto),
  align: center,
  table.header([算法], [复杂度], [选用]),
  [`set` 优化 Prim], [$O((n+m) log n)$], [用 `set` 显式 decrease-key],
  [堆优化 Prim], [$O(m log n)$], [常用稀疏图实现],
  [Kruskal], [$O(m log m)$], [直接给边集；通常首选],
)

三份板子都在构造时求解，`f` 表示是否得到覆盖全部点的生成树，只有 `f=true` 时 `ans` 才是 MST 权值。两份 Prim 从点 `1` 开始，`parent[v]` 和 `d[v]` 给出选中边；无向边必须存两个方向。Kruskal 每条无向边只存一次，但会原地排序输入边集；图不连通时，它留下的是最小生成森林权值，而 Prim 只处理点 `1` 所在分量。

两份 Prim 的 `prim(s)` 可以重新指定起点并会重置内部状态。边权字段为 `int`、总和为 `i64`；若单边也可能超过 `int`，必须同步修改邻接表和 `Edge::w`，不能只改答案类型。

==== 常见变形和易错点

- 最大生成树：Kruskal 改为降序；Prim 同步改初值、比较方向和堆。
- 严格次小生成树：枚举非树边替换路径上的边；存在相同边权时需维护最大值和严格次大值。
- 最小瓶颈路等于最小生成树路径最大边；最大瓶颈路等于最大生成树路径最小边，P1967 使用后者。
- 最小生成树计数：Kruskal 按边权分组，缩掉更小边权形成的连通块后，对同权边子图使用矩阵树定理。
- 最小差值生成树：双指针枚举按边权排序后的边区间，再判能否生成树。

最小瓶颈路等于某棵 MST 上路径的最大边，但这不表示 MST 路径一定是原图最短路。生成树计数时不要逐条处理同权边，否则会把本应同时选择的方案顺序化并重复或漏计。

Library Checker 要输出原边编号。用 Prim 时需把邻接边从 `(to,weight)` 扩成 `(to,weight,id)`，松弛成功时同步记 `parentEdge[v]=id`；用 Kruskal 时给 `Edge` 增加原编号，并在合并成功时记录它。平行边不能只凭两个端点反查编号。

#link("https://judge.yosupo.jp/problem/minimum_spanning_tree")[Library Checker · Minimum Spanning Tree]
