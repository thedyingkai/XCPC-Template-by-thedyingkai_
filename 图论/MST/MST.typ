==== 三种求法对比

#table(
  columns: (auto, auto, auto, auto),
  align: center,
  table.header(
    [*算法*],
    [*实现思路*],
    [*时间复杂度*],
    [*适用场景*],
  ),
  [朴素 Prim],
  [每次 $O(n)$ 查找最小值加入树中，并更新邻接点距离],
  [$O(n^2)$],
  [稠密图（$m$ 接近 $n^2$），\
  如 $n <= 5000$, $m$ 较大],
  [堆优化 Prim],
  [用堆维护未选点到树的最短边，动态更新],
  [$O(m log n)$],
  [稀疏图（$m$ 与 $n$ 同阶），\
  如 $n,m <= 2 times 10^5$],
  [Kruskal],
  [边按权排序后贪心合并连通块],
  [$O(m log m)$],
  [通用场景，尤其适合边集已给出且不需要建图的情况；\
  注意 $m$ 可达 $2 times 10^5$ 以上],
)

==== 应用

- 次小生成树：先求 MST，再枚举非树边替换树边，求严格次小权值。

- 最小生成树计数：利用 Kruskal 排序后相同权值边的处理方式，用矩阵树定理或 Kruskal 加并查集计数。

- 最小瓶颈路：求两点间路径上最大边的最小值 — 在 MST 上作 LCA 查询。

- 最小差值生成树：要求生成树的最大边与最小边之差最小。常用双指针 + Kruskal 枚举最小边。
