==== 选型

#table(
  columns: (auto, auto, auto),
  align: center,
  table.header([算法], [复杂度], [选用]),
  [朴素 Prim], [$O(n^2)$], [稠密图],
  [堆优化 Prim], [$O(m log n)$], [稀疏图],
  [Kruskal], [$O(m log m)$], [直接给边集；通常首选],
)

==== 接口与坑点

- 三份板子在构造时从点 `1` 开始求解；只有 `f == true` 时 `ans` 才是最小生成树权值。
- Prim 的无向边存两次；Kruskal 的无向边存一次，并会原地排序边集。
- 图不连通时，Prim 的 `ans` 只是点 `1` 所在连通块的结果，Kruskal 的 `ans` 是最小生成森林权值。
- 单边权值为 `int`，总权值为 `i64`；扩大边权时要同步修改邻接表、堆元素和 `Edge::w`。

==== 常见变形

- 最大生成树：Kruskal 改为降序；Prim 同步改初值、比较方向和堆。
- 严格次小生成树：枚举非树边替换路径上的边；存在相同边权时需维护最大值和严格次大值。
- 最小瓶颈路等于最小生成树路径最大边；最大瓶颈路等于最大生成树路径最小边，P1967 使用后者。
- 最小生成树计数：Kruskal 按边权分组，缩掉更小边权形成的连通块后，对同权边子图使用矩阵树定理。
- 最小差值生成树：双指针枚举按边权排序后的边区间，再判能否生成树。

- 洛谷 P3366「最小生成树」；P1967「货车运输」；P4180「严格次小生成树」。
