树链剖分把树上任意路径拆成 $O(log n)$ 个连续 DFS 序区间，再交给线段树、树状数组等序列结构处理。它适合在线的路径修改、路径查询和子树操作；若所有修改离线后统一统计，树上差分通常更短。

每个点选择子树最大的儿子作为重儿子，沿重边形成重链。离开一条重链时，子树大小至少缩小一半，所以一条根到点路径只跨 $O(log n)$ 条轻边。DFS 序先走重儿子，既让每条重链连续，也让整棵子树保持连续，这两个不变量分别保证 `path` 和 `subtree` 的区间正确。

==== 接口和改法

`path(u,v,f)` 对每个闭区间调用 `f(l,r)`，默认包含两个端点。边权放在较深端点的位置，并传 `edge=true` 排除 LCA；此时 `subtree(u,true)` 也可能得到空区间，使用前检查 `l<=r`。预处理使用显式栈，长链不会递归爆栈。

当前接口不保留整条路径的方向。求和、最大值等交换运算可以直接合并各段；字符串哈希、矩阵乘法等非交换运算必须把 `u` 侧和 `v` 侧分开累计，并反转其中一侧的段内方向。找路径上第一个满足条件的位置时，区间结构还要支持按行走方向二分。

换根后的子树分三种：查询点就是新根时为整棵树；查询点不是新根祖先时仍是原子树；否则从整棵树中减去查询点通向新根的那个儿子子树。

#link("https://judge.yosupo.jp/problem/vertex_add_path_sum")[Library Checker · Vertex Add Path Sum]
