==== 选用

- 把树上路径拆成 $O(log n)$ 段 DFS 序区间；子树对应一段连续区间。

==== 接口与坑点

- `path(u, v, f)` 对每段调用 `f(l, r)`，包含路径两端。
- 边权放在较深端点的 `dfn` 上，并调用 `path(u, v, f, true)` 排除 LCA。
- `subtree(u, true)` 可能返回空区间，使用前检查 `l <= r`。
- 当前分段不保留整条路径的方向；维护字符串哈希、矩阵乘法等非交换信息时，要分别累计两端并处理区间方向。

==== 常改与 trick

- 换根后的子树：`u == root` 时为整棵树；`u` 不是新根祖先时不变；否则用整棵树减去 `u` 到新根方向的儿子子树。
- 找路径上第一个满足条件的位置时，线段树需支持区间内二分，并按路径方向查询各段。

- 洛谷 P3384「树链剖分」；洛谷 P2590「树的统计」。
